# Kokoro 后端闭环任务总表

## W1C-首次登录邮件链（2026-09-24，已验收隔离组合；普通 IAB 未验）

| 项 | 裁决 |
| --- | --- |
| Owner | IAM 唯一拥有注册、验证邮件及 issuer 会话；Root 只拥有跨仓隔离 smoke。IAM fixture 由 `r2e_bff_verify_review` 唯一写入，Root 不写 IAM；Root 脚本仅 Root 写入。 |
| 当前事实 | IAM `test/fixtures/web-oidc-flow-host.ts` 已能启动临时 PG/Redis 和预置 tenant/client，但未把下层已支持的 `smtpUrl` 接入；Root Product Session smoke 仅使用预验证账号。三仓当前均为 clean main，Root `510f1cc6`、IAM `c16a9bc`、BFF `a50f987`、Web `5192ff0`。 |
| 目标职责 | 证明新注册账号经过真 SMTP 邮件与 Web→BFF→IAM 验证 GET，随后可用正式 OIDC/Product Session 登录；与“空库首账号/首租户”分别报告，不混称。 |
| 目录方案 | IAM 扩展现有 fixture（采用），不在生产代码加测试开关；Root 新建 `scripts/e2e/run_web_bff_iam_first_login_smoke.py` 和对应 `scripts/tests/` 测试（采用），不把邮件/注册状态机塞入已有 1300 行 Product Session runner，也不复制 IAM owner 业务代码。 |
| 粒度 | IAM 仅添加受限 SMTP URL 环境入口及 fixture 测试；Root 新文件仅封装真 SMTP、验证和首次登录编排，复用现有 owned-resource/HTTPS 组件。 |
| 依赖 | IAM owner fixture 先提交，Root 再按 IAM→BFF→Web 固定来源重 pin；Root 不直读 IAM 数据表，不改变产品 API 或生成客户端。 |
| 数据/API | 不改生产 schema/API；测试仍复用单 PostgreSQL/Redis 实例，以临时 DB/前缀和自有 SMTP socket 隔离；邮件 token 不打印。现有 BFF policy 仅允许 `/verify-email` GET，注册仅由 Root 测试准备调用 IAM loopback，不冒称浏览器注册 UI。 |
| 删除项 | 不增加可见中转、重试页或兼容 alias；`/login` 仍只负责正式 OIDC 启动。 |
| 验证 | IAM Node24 聚焦 test + `pnpm verify`；Root Python unit、真实隔离 HTTPS 三仓 smoke、资源清理、固定 SHA/gov 门。 |

执行结果：IAM `093b7651`、BFF `7a7f3adf`、Web `0f5aec47` 已在各自 `main` 提交，Root `e4cdf055` pin 三 gitlink。IAM 真 SMTP host integration 18/18、`pnpm verify` 740/740，BFF 270/1 skip，Web full Vitest 1415/1415；Root 改造隔离 HTTPS Product Session runner 后已得到 `first_login=smtp_verified_then_oidc`、资源剩余 0，并额外证明 `/login` 直达正式 IAM 凭据表单。Root checkpoint 曾因新 BFF commit 下两份文档 blob digest 未随库存更新而 RED，已按提交 blob 精确修正；独立只读复审 P0/P1/P2=0，Root Python 全量 **686 passed/156 subtests**。最终 Root 提交后的 main-only 证据以 `docs/progress.md` 为准。本片注册/建租户是测试准备对 IAM loopback 的调用，浏览器验证链接及后续 OIDC/Product 登录是真 Web→BFF→IAM；host 启动时仍预置测试 owner、tenant、OAuth client，不宣称空库首租户，也不宣称用户正在访问的 `3310` Web-only 进程已经配好正式 IAM/BFF。

用户要求的最后旧失败入口已按既定 Web clean-slate 设计门单独删除：Web `08ef650a` 删除未被当前 UI/正式 OIDC 调用的 `/api/auth/magic-link/request` 和旧 `/api/auth/callback`，不再产出 `/login?auth=link_unavailable`；`/api/auth/callback/kokoro-iam` 保留。Root `54e18142` pin 后，以该 Web SHA 重新跑隔离真 HTTPS first-login smoke 通过。Web 聚焦 contract 57/57、architecture 33/33、全量 1417/1417，隔离新目录 `next typegen`/TypeScript/production build 通过；用户 3310 的旧 `.next` 不被测试重写。旧 `auth.ts` helper/Team 仍属其他 clean-slate 切片，不把删除两条 route 冒充全部遗留路径清零。

## W1C-Team-R2D-FIX 固定租户设计门（2026-09-24，进行中）

| 项 | 裁决 |
| --- | --- |
| Owner / 基线 | IAM `apps/kokoro-iam` 的 organization 是 Tenant/Member/Invitation/Role 唯一事实与 writer；Root 管理跨仓 Product 约束。审查基线 Root `e66e93e6`、IAM `093b7651`、BFF `7a7f3adf`、Web `08ef650a`，均 main/clean。 |
| 当前事实 | IAM 三个 current-tenant internal GET、相关 mutation 与 issuer-session 已验证收/拒邀请存在；BFF 已发布 `/v1/team/{members,invitations,roles}` 三 GET。Web Team 零消费这些 GET，仍经旧 `/api/team/*` 直连 IAM `/bff/*`、密封 team-session 与切换 UI。IAM 三设计文档首页把已发布三 GET 写成待实现，须纠正。 |
| 目标职责 | 固定一个**产品部署租户**：受信配置绑定登录/Token 与 Product admission，Team 页面只有当前租户成员、邀请、角色；不显示全局租户目录、个人/团队 namespace 或切换器。IAM 可以继续持有通用多租户事实，不把 `Member.userId` 改成全局唯一。聊天/文件仍默认个人私有，成员资格不授予访问。 |
| 目录方案 / 粒度 | IAM 扩展现有 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md`（采用），不新建 `team-config`/ADR 垃圾桶或复制 contract（淘汰）；下一实现片复用 IAM 现有 organization controller/repository 与 BFF 现有 Team route/client，再由 Web 删除旧 Team adapter/UI。 |
| 依赖 / 数据 API | IAM 三文档先区分已发布 owner 三读/已有写与尚未发布的 Product 写投影；收/拒邀请复用已存在的 verified issuer-session 边界。本人未入组 pending inbox 只有确认产品必要且冻结窄 issuer 契约后才建；不为旧切换 UI 发明跨租户目录/HMAC cursor。BFF 受信固定 `KOKORO_TENANT_ID` 与 Web RP 的绑定方式须在代码切片前定成可验证 contract，不能只隐藏 UI。无新 IAM Team 表/全局用户唯一约束、无跨 owner SQL。 |
| 删除项 | 后续 Web 原子替换旧 `/api/team/{context,switch,[...path]}`、`src/team/client.ts` 旧 wire、旧 `auth.ts` team-session/helper、namespace 切换器与相关文案/测试；不得先删有效邀请/成员写而造成业务回退。 |
| 验证 | 文档门：三设计相互一致、当前/目标分明、Root 审查与 `git diff --check`；实现门：IAM `pnpm verify`/真实 PG+Redis/contract+consumer，BFF `pnpm check`/真实 IAM HTTP，Web contract/architecture/lint/typecheck/test/build/Playwright，Root 固定 SHA 真 OAuth→Team 读写/邀请与默认私有负例、来源门/main-only。 |

本片分工：`iam_team_read_audit` 与 `web_team_read_audit` 已完成独立只读盘点；`iam_team_fixed_docs_writer` 仅改上述三文件，经独立复审指出的 P1/P2 修正与 Root 事实纠偏后，IAM `c588d86f` 三文档及 `b363554d` CURRENT 均已提交推送 main、工作树 clean。Root `pnpm contract:check`、`pnpm prisma:validate`、`git diff --check` 通过。此为 IAM 文档设计门，不是固定租户登录、Web 消费或 Team 写闭环；BFF admission 已按下一卡完成并由 Root 真 OAuth 异租户负例验证。

### W1C-FIXED-TENANT-BFF-A：普通 Product admission 固定租户闸

| 项 | 裁决 |
| --- | --- |
| Owner / 当前事实 | BFF `src/auth/user-admission.ts` 是普通 `/v1` 的唯一身份准入点；`config.tenantId` 已由 `KOKORO_TENANT_ID` 读取但当前仅 `runtime-manifest` 使用。IAM 验证的其他租户 User Bearer 目前仍能进入 BFF 普通路由。基线 BFF `7a7f3adf` main/clean。 |
| 目标职责 | 在任何普通 Product route、请求体、receipt、数据库或 owner socket 之前，要求已配置的固定 tenant 与 IAM admission 的 tenant 精确相等；配置缺失返回稳定 503，异租户返回稳定 403。service-only runtime manifest 与 browser-private `/iam` 原生协议不经过此闸，不能误伤。 |
| 放置/粒度 | 扩展既有 `src/auth/user-admission.ts` 和相邻 `test/user-admission.test.ts`、必要配置/HTTP 测试及三设计文档（采用）；另建 Team 专属检查、独立 middleware/数据库表或 BFF 租户目录均淘汰，因为所有 Product 资源必须同样绑定。 |
| 依赖与删除 | 不修改 IAM scope/contract、不放宽旧 IAM admission；不接受浏览器 tenant/header。此切片不删登录所需 `/organization/list|set-active`，删除须等 Web 固定选择流程同步。无 schema/事务变化；现有测试 fixture 必须显式固定 tenant，不能靠 `null` 绕过。 |
| 验证 | TDD：同租户成功；异租户/未配置在任何 route/receipt/owner I/O 前失败；伪造 legacy tenant header无效；service-only/issuer 仍可用。BFF `pnpm format:check && pnpm check`、Root 固定 SHA 真 OAuth A/C 跨租户负例后方可验收。 |

执行结果：BFF main `018c6176` 实施准入，`dadf9264` 顺序固定 IAM `b363554d` 来源，Web `74319fac` 消费固定 relay artifact，Root `d35c8d71` pin 三仓 gitlink 与 153 条相关来源记录。BFF 全门 272 pass/1 skip；Root 真 IAM OIDC Code+S256 A/C：A 当前 tenant 三个 Team GET 200，C 独立真实 issuer session + 其他 tenant JWT 对三 GET 全部 403 `product_tenant_forbidden`，伪造 legacy tenant/principal header 无效，自有资源0。此任务 **已验收**，但 Web 固定 tenant 登录交互及 Team Product 写仍待后续任务。


### W1C-FIXED-TENANT-BFF-B：收窄第一方 IAM tenant relay（已验 owner 门；真三仓待验）

| 项 | 裁决 |
| --- | --- |
| Owner / 基线 | BFF 是 browser-private IAM relay policy/transport 唯一 writer，IAM 仍拥有通用 Organization API。BFF `dadf9264`、IAM `b363554d`、Root `9f8d8d60` main/clean。 |
| 当前事实 | BFF policy `1.1.0` 仍放行 `/organization/list` GET 和任意 body 的 `/organization/set-active` POST；Web 旧 tenant 选择页消费二者。BFF 普通 `/v1` 的固定 tenant admission 已发布。 |
| 目标职责 | relay 删除可枚举全租户的 list；set-active 仅在 Web 受信 service + 精确 Origin + issuer cookie + OAuth signed continuation query + 固定配置 tenant 同值的窄输入上代理，不让浏览器自报 Organization ID 变成 Product tenant。缺/错配置在转发前失败。 |
| 目录方案 / 粒度 | 扩展既有 `src/http/routes/iam-protocol-relay.{policy,ts}`、相邻测试与 `contract/iam-relay-policy.json` 生成物（采用）；不创建 Team 专属代理/共享 IAM contract 副本/兼容 route（淘汰）。三设计文档同步当前/目标，先通过文档门再改代码。 |
| 依赖 / 删除 | IAM 当前允许 set-active 原生路由不变；BFF 发布 policy 后 Web 再原子消费，删除旧 list/可选表单/POST。contract version 按 browser-private breaking policy 提升；Root provenance pin 必须在 Web consumer 发布后完成。无 schema/SQL 变化。 |
| 验证 | BFF owner `pnpm format:check && pnpm check`；单元/真 HTTP 负例：list 404、伪造 tenant/额外字段/错 query/缺 issuer cookie/错 Origin/缺配置均零 IAM socket，固定 tenant 合法 continuation 通过；Root 来源门和 Web 真 OAuth 回归随后执行。 |

本任务由 BFF 唯一 writer 执行；Root 负责设计/契约放行、diff 审查和主仓重验，不把 BFF relay 收窄单独冒称固定租户登录已闭环。

交付：BFF `74ec30b` 发布 policy `2.0.0`，删除 list、收窄 set-active；Root 独立 Node22 `pnpm format:check && pnpm check` 通过。IAM-C 发布后 BFF `87f9d8d` 顺序重钉 IAM provenance，Web `23bd00e` 已按该 artifact SHA-256 `b3c23492…` 消费；Root 来源/真组合待完成。

### W1C-FIXED-TENANT-IAM-C：Web OAuth client 必经固定租户续接（已验 owner 门；真三仓待验）

| 项 | 裁决 |
| --- | --- |
| Owner / 基线 | IAM 唯一拥有 OAuth issuer 状态机及签发；IAM `b363554d` main/clean。Web/BFF 持有 Product 配置与准入，不读取 IAM 表。 |
| 当前事实 | Better Auth 1.7.3 `postLogin.shouldRedirect` 只检查 active Tenant 是否存在；已在第二 Tenant active 的第一方 Web client 可绕开 `/auth/select-tenant`，最终签发错 tenant 并被 BFF 403，造成登录死路。hook 只收到 headers/user/session/scopes；包公开的 request-local `getOAuthProviderState().query` 可取得已验证的 client ID。 |
| 目标职责 | 只对精确配置的第一方 Web OAuth client，若 active Tenant 不等于配置的固定 Product Tenant，强制进入现有 signed postLogin 续接；在 `consentReferenceId` 再次验证固定 Tenant 后方可签发。其他 IAM OAuth client 保持通用多租户行为。缺/错绑定 fail closed，不用 Origin/Referer/scope 猜 client。 |
| 目录方案 / 粒度 | 扩展现有 Better Auth config、环境 schema/fixture、相邻 OAuth 测试及三设计文档（采用）；不另造 OAuth provider、数据库表或全局单租户模式（淘汰）。IAM 配置是此 Web client 的 issuer 续接约束，不代替 Web/BFF Product 配置。 |
| 数据/API / 删除 | 无新 HTTP operation/token claim/schema；不删 IAM 原生 Organization endpoint。避免只隐藏选择页或无条件 redirect 导致循环；测试实例先注册 client 再绑定精确 ID/tenant，部署示例必须说明三方配置同值。 |
| 验证 | IAM Node24 contract/format/lint/typecheck/unit/integration/build；真实 OAuth：Web client 已 active 第二 Tenant / 固定 Tenant / 无 active、其他 client 保持原行为、未加入/disabled target、跳过/重放 continuation、prompt=none、配置缺失/不一致均符合失败边界。Root 后续固定 SHA Web→BFF→IAM 真组合。 |

本任务与 BFF relay 收窄为不同 owner，可并行实现；Web consumer 须等待两者发布后再原子切换。

交付：IAM `3231d2e` 仅对配置的第一方 Web client 绑定固定 Product tenant，沿已签名续接与 consent 边界重验；Root 独立 Node24 `pnpm prisma:validate && pnpm verify`（741 tests）与真实 PostgreSQL 聚焦 6/6 通过。Web/BFF 真组合待验。

### W1C-FIXED-TENANT-BFF-C：发布当前用户身份投影供 Web RP 验证（已验 owner 门；Web 消费待做）

| 项 | 裁决 |
| --- | --- |
| Owner / 基线 | BFF `74ec30b` main/clean；已有 `authorizeUserRequest` 在全部普通 Product `/v1` 前在线验证 IAM token 并强制固定 `KOKORO_TENANT_ID`。Web RP 当前只看 token 形状、subject/TTL，签发/refresh 前未验证 tenant 或 BFF 配置同值。 |
| 目标职责 | BFF 公布窄 `GET /v1/me` Product API，返回仅受信 admission 的 `{user_id, tenant_id}`；同一现有 service+Bearer 闸、固定租户/撤权/失效语义，不读库、不接受 client 自报 tenant。Web 在 code callback 建 Session 前及 refresh finalize 前调用，核对自身固定配置 ID 与 OIDC subject；错配不建/续有效 Product Session。 |
| 目录方案 / 粒度 | 扩展既有 public OpenAPI、HTTP route/已存在 admission 及相邻测试/生成门（采用）。不借 Team GET 或 runtime manifest 充当身份契约，不新增私有隐藏 RPC、IAM SQL、BFF 身份表（淘汰）。新 route 采用 BFF 已有最小路由目录/response envelope；若审计发现现有路由已可提供同等受信投影，先报告不重复创建。 |
| 数据/API | public `/v1` additive beta operation，由 BFF 唯一维护 OpenAPI operationId/schema/稳定错误/响应和 baseline；外部 JSON `snake_case`，`x-request-id`/no-store，权限 `self:read` 或本仓既有等价 marker。无 SQL/事务/receipt/cache 写；version/source/digest 须由 Web 固定消费。 |
| 删除/依赖 | 不保留 fallback 到 JWT decode/Team 列表；Web 消费必须等待 BFF owner commit。此卡不更改 Web 源码；BFF relay B 已独立发布，IAM Web client 续接 C 并行实施。 |
| 验证 | BFF Node22 `pnpm format:check && pnpm check`、公开 contract/breaking/错误/身份测试及真实 IAM same/foreign/revoked OAuth；Root 冻结 SHA 复验，再由 Web RP code+refresh 真组合证明错误 tenant/配置错配零 Product Session。 |

本操作是受信当前身份投影而非公开团队目录；权限与缓存语义需沿 BFF 既有 Product API 一致实现。

交付：BFF `8ca0264` 发布 `GET /v1/me`，OpenAPI SHA-256 `75ab4821…`；BFF `87f9d8d` 顺序重钉 IAM 来源。Root 独立 Node22 `pnpm format:check && pnpm check` 通过（276 pass/1 skip）；Web callback/refresh 尚未消费，不能标记全链完成。

### W1C-FIXED-TENANT-WEB-D：无选择页固定租户登录与 RP 准入（阶段 1–2 已验；RP 准入待做）

| 项 | 裁决 |
| --- | --- |
| Owner / 基线 | Web `74319fac` main/clean；`/login` 已无可见中转，但 3310 Web-only 因缺正式 RP/BFF/IAM 配置仍 503/body0。`/auth/select-tenant` 外层 302→`/iam/interactions/select-tenant` 内层（issuer cookie Path=/iam），内层仍 list/dropdown/POST 浏览器选择。RP callback/refresh 目前未核 BFF fixed tenant。Team 另有可见 switcher 与 `/api/team/switch`。 |
| 目标职责 | Web 使用 server-only 固定 `KOKORO_TENANT_ID`；`/login` 缺配置 fail closed，真实 IAM sign-in 保留；signed tenant 交互内层只用受信配置在服务端续接，不渲染选择/重试页或接受浏览器 tenant；code callback 创建 Product Session 前与 refresh finalize 前调用 BFF owner 新 `GET /v1/me`，核已验证 tenant 与 OIDC subject，错配不签发有效 session。删除 Team 可见切换器和 legacy switch mutation。 |
| 目录方案 / 粒度 | 复用现有 `src/app/auth/select-tenant` 外层 302 和 `src/app/iam/interactions/select-tenant` 内层、RP/product-session/server adapter/Team UI 原位置（采用）；不建第二登录 SPA/通用 proxy/额外身份 store（淘汰）。BFF `/v1/me` 由 owner OpenAPI 固定版本与 digest 驱动生成消费，Web adapter 终止 wire 类型；不直接读取 IAM/BFF DB。 |
| 依赖 | BFF-B `74ec30b` policy 2.0.0 删除 list、窄 set-active 已发布；先等 IAM-C 第一方 client 续接和 BFF-C `/v1/me` contract commit，再冻结 Web consumer。独立的 Team switcher/route 删除可先做；同仓仍唯一 writer。Web 与 BFF/IAM 配置不一致、已有第二 Tenant active、无 membership/disabled、恶意签名/重放、refresh tenant 变化须 fail closed 且无 Product Session。 |
| 数据/API / 删除 | Web 无数据库 owner；Product Session Redis record 不新增 tenant 事实副本，Web 只保存已验证 token/session，BFF 每次普通 Product admission 重验。删除 `/iam/organization/list` Web browser GET policy/生成快照消费、旧 tenant selector/POST/CSRF、`/api/team/switch` 与可见 switcher；旧 Team context/写读整体替换属后续 Team consumer 卡，不能让遗留切换途径可达。无兼容/备用按钮/整页重试。 |
| 验证 | Web Node22 contract/architecture/lint/full unit；真 Next HTTP + Chromium 对直接 list/set-active/旧 switch、无页面续接/登录表单/配置缺失、code/refresh 错配/私有性负例；typegen/build 在**独立安装 node_modules 的隔离副本**执行，不修改用户 3310 `.next`/进程。Root 固定 IAM/BFF/Web SHA 真 SMTP→OAuth→Product/Team 与资源0，普通 IAB 3310 是否实际可登录单独验收。 |

前置 owner contract 未发布前，Web writer 只可完成三设计文档与独立 Team switcher/route 删除，不提前编造 `/v1/me` wire 或重写 tenant 续接。

阶段 1：Web `874bb1f` 删除可见 Team switcher、`/api/team/switch`、客户端 mutation，独立隔离副本 typecheck/build 和 owner 全量测试通过。阶段 2：Web `23bd00e` 固定消费 BFF policy `2.0.0`，内层 tenant 只 GET 服务端固定续接，彻底删除候选列表/表单/POST/tenant CSRF；Root 真实 Next HTTP 51/51、HTTPS 反代 5/5、Web full Vitest 1417/1417、contract 57/57、architecture 34/34、lint 与隔离副本 typecheck/build 均通过。`/login` 可见连接/整页重试组件此前已删除，当前 3310 仅返回空 503，仍非可登录正式入口。**剩余阻塞：** Web RP callback/refresh 对 `/v1/me` 的准入与固定租户错配负例、Root 真三仓 OAuth/SMTP/Team 组合、3310 实际 HTTPS 接入；这些未完成前此卡不得验收。

状态日期：2026-09-24。本文是本轮后端闭环的**唯一任务状态表**；主控 Agent 维护状态、依赖、负责人和验收证据，子 Agent 只更新自己获准任务卡中的交付信息。

## 1. 总目标与权威入口

- Goal：按已批准设计依次完成 Wave 0–7；Root 主控负责架构裁决、派工、双重审查、集成与最终验收，子 Agent 按 owner 逐仓实施；Billing 最后处理。
- 设计事实源：[`superpowers/specs/2026-09-20-kokoro-backend-closure-design.md`](superpowers/specs/2026-09-20-kokoro-backend-closure-design.md)
- 当前执行计划：[`superpowers/plans/2026-09-23-wave-1c-web-oidc-and-same-origin.md`](superpowers/plans/2026-09-23-wave-1c-web-oidc-and-same-origin.md)
- 已验收 Wave1B：[`superpowers/plans/2026-09-22-wave-1b-bff-admission-and-privacy.md`](superpowers/plans/2026-09-22-wave-1b-bff-admission-and-privacy.md)
- 已验收 Wave1A：[`superpowers/plans/2026-09-22-wave-1a-iam-session-admission.md`](superpowers/plans/2026-09-22-wave-1a-iam-session-admission.md)
- 已验收 Wave0B：[`superpowers/plans/2026-09-21-wave-0b-hard-link-closure.md`](superpowers/plans/2026-09-21-wave-0b-hard-link-closure.md)
- 已验收计划：[`superpowers/plans/2026-09-21-wave-0a-governance-and-contract-gates.md`](superpowers/plans/2026-09-21-wave-0a-governance-and-contract-gates.md)
- 证据账：[`progress.md`](progress.md)
- 本轮启动 Root 基线：`1bc74ae536d8a2da48f76045da95c2d5c2877750`
- 范围：`apps/kokoro-app` 与八个后端 owner；`apps/kokoro-mori` 和其他前端不参与业务改造。

### 1.1 主控优先看这里

- 当前关键路径：**R2e 用户可见的正式 HTTPS IAM 入口 + W1C Team 单租户契约收敛 → Storage 能力链**。R2d 私有负例已在固定 Web `0a093f65`、BFF `eb1eb292`、IAM `e36da9ec`、Agent `520ec181` 的隔离 HTTPS 四服务组合实跑：A 的真实 Chromium 登录、DOM 首发、AG-UI 断线恢复及重载通过；B 同 tenant 成员与 C 跨 tenant owner 各自以真实 Product Session 对 A 的列表/快照/事件/写操作均不可见，BFF/Agent owner 事实未变，测试自有资源清零。B/C 是独立 Python CookieJar HTTPS 会话，**不是** Chromium 登录；显式分享的正向路径尚未由此门证明。用户的 `3310` 只有 Web，`/login` 返回空 body 503，旧可见中转 UI 已删除但正式 IAM 入口未接通。Team 设计候选经独立审查未放行，先按固定单租户重写窄契约，不扩建全租户切换。详见 Wave1C/W1D 执行卡。
- 后续按能力依赖推进 Storage → Platform → 聊天执行链 → System；支付最后。聊天的完成定义以本文第7节能力矩阵为准，包含消息落库、流式恢复、取消、HITL、附件/产物，而不是仅页面能展示文本。
- 每片交付必须是可运行代码、对应行为测试和必要契约/SQL更新；文档整理或生成客户端不单独等于功能完成。
- 当前不深入部署、生产角色隔离、镜像、SLO、网络硬化和重复全仓审计；这些进入统一发布收尾。身份/越权、事务、幂等、取消和故障恢复属于产品正确性，继续随代码验证。
- 用户本轮资源裁决：开发应用目标只复用**一个 PostgreSQL 实例、一个数据库、一套应用账号**和现有 Redis；每个数据 owner 使用独立 schema/连接 URL 与代码写入边界，表名前缀不替代 schema。不建多 role 或长期独立库。当前部分 owner 的 `public`/整库空白 installer 尚未适配，单库应用组合**未通过**；按 owner 代码切片收敛，不做运维专项。现有测试 fixture 临时库/前缀仅用于测试隔离且只清理自身，不是应用并发或数据库角色限制。

### 1.2 能力边界（不互相代写事实）

| Owner | 负责 | 明确不负责 |
| --- | --- | --- |
| `kokoro-app` | 页面、交互状态、浏览器会话、同源adapter、AG-UI视图映射 | 不直连内部owner、不保存业务数据库事实 |
| `kokoro-bff` | Conversation/Message/Share/Project/ScheduledTask、资源权限、durable AG-UI投影 | 不执行模型/工具、不读取其他owner数据库 |
| `kokoro-iam` | 登录、会话、租户/成员与身份授权事实 | 有效会话不等于能读同团队其他人的聊天 |
| `kokoro-agent` | Run、工具执行、HITL/Approval、Checkpoint、执行事件/Evidence | 不另建Conversation/Message或公开事件账本 |
| `kokoro-storage` | 上传、Blob/Asset/Artifact、对象生命周期与访问边界 | BFF/Agent不复制存储模型或绕过文件授权 |
| Capability → Platform | Skills/MCP控制面与授权能力供给；按既定切片原子更名 | 不作为任意共享代码/业务的收容仓；当前名称仍为Capability |
| `kokoro-scheduler` | Schedule/Occurrence、触发、投递、重试与receipt | 不拥有BFF产品任务或Agent Run |
| `kokoro-system` | 站点/运行控制、模型目录与路由配置 | 不作为任意key/value配置中心 |
| `kokoro-billing` | 支付、订阅、额度、账本、计量与对账 | 本轮其他核心能力开发不被支付实施阻塞 |

## 2. 工作规则

状态只使用：`待派工 → 进行中 → 待审查 → 待集成验证 → 已验收`；真正无法继续时使用 `阻塞`，并写明阻塞条件和 owner。

1. 一个子仓同一时刻只有一个写入 Agent；Root 不抢写已派出的文件。
2. owner contract 先提交并推送，consumer 再切换；每个切片删除被替代路径，不保留双轨、alias 或 fallback。
3. 子 Agent 的测试与 commit 只是待验收交付；Root 必须重新审查 diff，并在冻结 SHA 上复跑对应门禁。
4. 子仓 commit/push 完成后，Root 才提升 gitlink；精确路径暂存，不使用 `git add .` 或 `git add -A`。
5. `docs/progress.md` 只记录已执行证据。设计、计划、口头报告和历史结果不算完成证据。
6. 可并行推进**无contract/源码前后依赖**的不同子仓：各仓唯一writer，Root只管任务卡、边界、审查与集成；owner发布后消费者依次跟进。同一仓不并发写，完整门禁/真实PG与Redis集成测试默认串行，避免机器争用。
7. 不留后台开发服务或测试runner：命令使用前台可等待session、明确超时与退出码；需要真实进程时由fixture记录自有PID/资源并在成功或失败后清理，收尾检查项目进程和监听端口。复用已有PostgreSQL/Redis，不重启或清理非本任务进程，也不以系统僵尸进程推断项目残留。

## 3. 当前任务卡

| ID | 优先级 | 业务目标 | Owner / 写入 Agent | 依赖 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| W0A-0 | P0 | 建立 Goal、任务表、进度账和 Wave 0A 计划 | Root / 主控 | 已批准设计 | 四个入口互链；Root 文档门通过；当前 diff 与验证写入证据账 | 已验收 |
| W0A-1 | P0 | 统一数据库角色、协议矩阵与当前 Capability 身份的治理表述 | Root / `w0a1_governance_writer` | W0A-0 | `AGENTS.md`、架构标准、SQL 标准、CURRENT 无矛盾；治理测试锁定决定 | 已验收 |
| W0A-2 | P0 | 实现从 gitlink commit blob 校验 contract/evidence 的机器门 | Root / `w0a2_contract_verifier_writer` | W0A-1 | schema、gitlink、commit blob digest、状态与非法旁路负向测试通过；脏工作树不能影响结果 | 已验收 |
| W0A-3 | P0 | 冻结完整调用矩阵与 consumer inventory | Root / `w0a3_inventory_writer` | W0A-2 | 16 条批准 edge 与 Web→IAM 非法旁路完整登记；绑定 contract version、generator/runtime version 和 evidence digest | 已验收 |
| W0A-4 | P0 | 完成 Wave 0A 独立审查和 Root 集成验证 | Root / 主控 + `w0a_final_spec_reviewer` + `w0a_final_quality_reviewer` | W0A-1、W0A-2、W0A-3 | 规格审查与质量审查通过；Root 三项门禁和 compatibility 红门有当前输出 | 已验收 |
| W0B-0 | P0 | 冻结 W0B 执行计划与控制面 | Root / 主控 | W0A-4 | 计划 SPEC/EXECUTION 双审 `0/0/0`；task/progress/INDEX 切换完成；Root 门通过 | 已验收 |
| W0B-1 | P0 | 增加 consumer/producer 版本与精确 edge checkpoint 机器门 | Root / `w0b1_governance_writer` | W0B-0 | `.node-version`/`go.mod`/producer assertion 负例通过；三个 checkpoint 逐 edge 验证 | 已验收 |
| W0B-2 | P0 | 验证并修复 Capability 当前 HTTP owner release | Capability / `w0b2_capability_owner_verifier` → `w0b2a_capability_owner_writer` → 双审与独立复验 | W0B-1 | 四个 `/v1/*` GET、contract/runtime/docs/schema 一致；canonical `query`、参数边界与标准错误 envelope 锁定；全门通过；remote 可达 | 已验收 |
| W0B-3 | P0 | 冻结 BFF Capability 设计与 owner artifact | BFF / `w0b3_bff_capability_designer` | W0B-2 | commit-blob vendor/provenance 固定；三文档门通过；只接受 `query` | 已验收 |
| W0B-4 | P0 | 实现 BFF Capability generated consumer | BFF / `w0b3_bff_capability_designer`（续任） | W0B-3 | 四路由、身份、查询、错误、timeout/body cap 与生成漂移门通过；旧 `/bff/*` 删除 | 已验收 |
| W0B-5 | P0 | 实现 Capability↔BFF 隔离真实进程 smoke | Root / `w0b5_capability_bff_smoke_writer` | W0B-4 | 独占 DB/Redis/port/process；8 个 case 通过；失败也只清理自有资源 | 已验收 |
| W0B-6 | P0 | 集成并激活 `EDGE-BFF-CAPABILITY` | Root / `w0b6_capability_integration_writer` | W0B-5 | 子仓推送、gitlink/fan-out/version/evidence 更新；`w0b-capability` 精确通过 | 已验收 |
| W0B-7 | P0 | 验证 Scheduler owner release | Scheduler / `w0b7_scheduler_owner_auditor`（只读） | W0B-6 | `/schedules`、event header/RFC3339、幂等/重试与 docs/schema 一致；Go 全门通过 | 已验收 |
| W0B-7R | P0 | 修复 Scheduler 稳定错误机器契约与边界证据 | Scheduler / `w0b7r_scheduler_contract_writer` | W0B-7 审计 drift | OpenAPI/breaking policy/runtime parity锁定两个稳定错误；Nano与opaque key证据；完整 Go门通过 | 已验收 |
| W0B-7I | P0 | 集成 Scheduler 修复后的 owner release | Root / `w0b7i_scheduler_integration_writer` | W0B-7R | gitlink/全部 Scheduler fan-out/测试 pin/计划一致；edge 状态不变；Root checkpoint 通过 | 已验收 |
| W0B-7R2 | P0 | 修复真实 PostgreSQL 同名新键 create 的可持久化冲突结果 | Scheduler / `w0b7r2_scheduler_conflict_writer` | W0B-10 真实联调缺陷 | 新键重复 create 返回409并写receipt；重启replay；Go/PG/Redis全门；Root真实HTTP复验 | 已验收 |
| W0B-8 | P0 | 冻结 BFF Scheduler 控制/回调设计与 artifact | BFF / `w0b8_bff_scheduler_designer` | W0B-7 | control/receiver 边界、trusted tenant、opaque key + semantic digest、恢复语义确定 | 已验收 |
| W0B-9 | P0 | 实现 Scheduler generated control 与 receiver | BFF / `w0b9_bff_scheduler_writer` | W0B-8 | 旧协议删除；generated validation、专用 receipt/CAS、幂等与恢复测试通过 | 已验收 |
| W0B-9V | P0 | 修复基线已失败的两处 AG-UI fixture | BFF / `w0b9v_agui_fixture_writer` | W0B-9 全量门暴露 | 第二Run遵循admission；delete传requestId；原断言保持；独立PG复验通过 | 已验收 |
| W0B-10 | P0 | 实现 Scheduler↔BFF 隔离真实进程 smoke | Root / `w0b10_scheduler_bff_smoke_writer` | W0B-9、W0B-7R2 | 最终BFF c5e9b3c + Scheduler975dee59；真实11case通过，loopback/资源清理通过，Agent为receipt stub | 已验收 |
| W0B-5R | P0 | 修复Capability smoke同源资源/HTTP生命周期缺陷 | Root / `w0b5r_capability_smoke_writer` | W0B-10代码审查通过、writer停写 | 已创建DB/Redis确认丢失、预存保护、HTTP线程回收回归；新BFF pin；真实8case与清理通过，独立提交；最终9c88d0d/c5e9b3c监听前置及8case通过；105行fixture例外已关闭 | 已验收 |
| W0B-2B | P0 | 补齐Capability owner显式监听地址与loopback验收 | Capability / `w0b2b_capability_listener_writer` | Root发现main.ts硬编码0.0.0.0 | runtime配置/入口/测试/docs一致；实际监听loopback；release先apply后check；修复Root复现的并行receipt fixture隔离；owner门通过；新release后consumer pin刷新（Root集成另计） | 已验收 |
| W0B-10R | P0 | 修复Scheduler smoke无ownership marker的预存Redis prefix保护 | Root / `w0b10r_scheduler_prefix_writer` | W0B-5R同源审查 | 预存prefix无SET/UNLINK；未知inventory fail-closed；现有生命周期不退化 | 已验收 |
| W0B-11 | P0 | 集成并激活 Scheduler 双向 edge | Root / 主控 + 独立双审 | W0B-10、W0B-5R、W0B-2B、W0B-10R | BFF consumer generator/Node + Scheduler producer Go 证据固定；`w0b-exit` 通过 | 已验收 |
| W0B-12 | P0 | 冻结 BFF Storage fail-closed 与 W1/W2 前置 | BFF / `w0b12_14_bff_storage_preflight` 转任同仓实现 | W0B-2B 已验收；Root批准22文件与文档门 | `GET /v1/library` 固定 503；授权矩阵、IAM admission、scope/pagination owner 明确 | 已验收 |
| W0B-13 | P0 | 将 Storage 前置绑定到 Root W1/W2 | Root / 主控 | W0B-12 | task/progress 记录 BFF docs SHA 与五项验收前置 | 已验收 |
| W0B-14 | P0 | 删除 BFF `/internal/bff/library` 运行链 | BFF / 同一 Storage 子 Agent | W0B-13 | 不打开 upstream socket；旧 URL/config/projector 全删；BFF 全门通过 | 已验收 |
| W0B-15 | P0 | 集成 Storage 死链删除但不激活 Storage | Root / 主控 + 独立双审 | W0B-14 | 所有 BFF fan-out 更新；Storage edges 继续 broken；`w0b-exit` 与双 smoke 通过 | 已验收 |
| W0B-16 | P0 | W0B 双审与证据冻结 | Root / 主控 + 独立审查 | W0B-15 | SPEC/QUALITY `0/0/0`；4 active / 12 broken / 1 illegal 精确门；Root/子仓 clean main-only | 已验收 |
| W1 | P0 | IAM → BFF → Web 身份、授权与 same-origin 闭环 | IAM → BFF → Web，串行 | Wave 0 | admission、CSRF、tenant/actor/subject、越权负例与生成客户端通过；Storage消费必须先有BFF IAM admission | 进行中 |
| W1-LOGIN-UI | P0 | `/login` 无可见中转地进入唯一 IAM 签名凭据交互，正式表单对齐本仓 shadcn 设计 | Web / `web_form_owner` 唯一写入；Root 审查、提交与真浏览器验收 | BFF browser-private IAM relay 2.1.0、IAM OIDC owner；Web 基线 `6290c11` | 不建立平行凭据链；保留 OIDC state/PKCE、原始签名 query、一次性 CSRF、同源 Origin 与 issuer Cookie；真实 Next/Chromium 可见正式表单，移动端与错误态通过。Next 16 Route Handler 不支持 `react-dom/server`，本片只做 token 视觉对齐，不伪称运行时复用 React 组件；Web `942d22e` 单仓门与隔离 Next/Chromium 52 项通过；Root W1D 固定三仓并在 3310 真实登录→consent→`/app` 可见；W1E 来源重钉不改 UI，当前新 SHA 的 3310 组合尚未复验 | 已验收（W1D 固定组合；W1E 来源无 UI 变化） |
| W2 | P0 | Storage v2 完整命令、查询、幂等与数据闭环 | Storage → consumers | Wave 1 | Proto/runtime/generated drift、真实 PostgreSQL/ObjectStore、恢复测试通过；先固定default-deny caller×operation×scope、Capability scope与Agent可信ExecutionIdentity映射、Library分页，且依赖W1 admission | 待派工 |
| W2-DB-Storage | P0 | Storage 从 `public`/整库空白安装改为单应用库的 `kokoro_storage` owner schema | Storage / 唯一子代理writer；Root审查、提交与组合验收 | 已发布的 Storage F1，不依赖 Team UI；不激活 Storage consumer | 同库已有其他 owner schema 时只创建/检查自己的 schema；空 owner schema 安装、重入拒绝、catalog/drift 与实际 Prisma 读写均通过；不碰其他 schema、无需多角色；状态：owner切片已验收，Storage main `38be74ef7fb0b1ddd687c67434d898f8628068fb`；两轮独立终审P1（PG operator判空、默认应用/镜像smoke仍指旧专库）均TDD修正，Root独立Node24 format/lint/typecheck/Prisma validate/build/contract-check、architecture48及隔离真PG 70文件577测试通过，自有测试库已删除；Root gitlink/库存集成验证见进度记录。仅owner schema，不代表Storage F2/消费者已激活 |
| W3 | P0 | `kokoro-capability` → `kokoro-platform` 原子切换 | Platform → Agent/BFF → Root | Wave 2、IAM workload auth | remote/path/package/service/env/Proto/数据库/Redis/consumer 同一窗口切换；旧身份删除 | 待派工 |
| W4 | P0 | BFF / Agent / Scheduler 事实 owner、投影、outbox 与恢复闭环 | BFF → Agent → Scheduler | Wave 3 | Conversation/Message 唯一归 BFF；Run/Evidence 唯一归 Agent；调度重复投递可恢复 | 待派工 |
| W5 | P1 | System generated HTTP client 与模型目录/运行控制闭环 | System → BFF/Agent | Wave 4 | owner contract、runtime parity、consumer pin 和 SQL 门通过 | 待派工 |
| W6 | P1 | Billing 最终契约、账本、支付与对账闭环 | Billing → BFF | Wave 5 | 唯一 runtime contract、幂等账本、webhook/reconciliation 和失败恢复通过 | 待派工 |
| W7 | P0 | Root 组合 E2E、release manifest 与最终验收 | Root / 主控 | Wave 1–6 | fresh clone、main-only、全 owner 门禁、跨仓 E2E、digest/image/SHA 清单全部绑定 | 待派工 |

## 4. W0A-0 当前变更范围

| 项 | 结论 |
| --- | --- |
| Owner | Root 治理；唯一 writer 为主控 Agent。 |
| 当前事实 | 旧 `docs/task.md` 混合数月历史，`.superpowers/**/progress.md` 被 Git 忽略，缺少可追踪的当前证据账。 |
| 目标职责 | `task.md` 只维护任务状态与依赖；`progress.md` 只维护执行证据；实施细节进入版本化 plan。 |
| 目录方案 | 采用已存在的 `docs/` 与 `docs/superpowers/plans/`；淘汰被忽略的 `.superpowers/` 作为权威台账，也不新建第二个 task 目录。 |
| 粒度 | 两份短期控制文档职责不同，计划单独版本化；不把全部内容塞回 `CURRENT.md`。 |
| 依赖 | 只链接 Root 权威文档；不复制 owner contract、Schema 或业务 DTO。 |
| 数据/API | 本切片不改变运行数据、Schema 或 API。 |
| 删除项 | 删除旧任务文件中的历史混排；历史仍可由 Git 查阅。 |
| 验证 | Markdown 链接检查、`verify-repository-topology.py`、`pytest scripts/tests`、精确 diff。 |

## 5. 每次交付必须写入的证据

```text
任务：<ID / owner / Agent 角色>
状态：已提交 / 部分完成 / 被阻塞
基线：<绝对路径 / main / 起始 SHA>
commit：<子仓 SHA；Root 集成后再补 Root SHA>
修改文件：<绝对路径清单>
验证：<命令 -> 实际退出码与通过/失败数>
审查：<规格审查 / 质量审查 / 对应 SHA>
未完成与风险：<明确列出>
后续 owner：<仓库 / Agent / 主控>
```


## 6. Wave 1 启动任务卡（2026-09-22）

批准依据为整体设计 Wave1；Root 基线 `136e12d7f751b7b77c5a16f8a36fa1df32f97b50`。以下为已完成盘点的冻结任务卡；当前执行授权以 Wave1A 执行卡和精确计划为准，不扩展总架构或恢复 W0B 旧工作。

| ID / 目标 / 优先级 | Owner / Agent / 模型 / 模式 | 基线与绝对工作目录 | 范围 / 依赖 / 验证 / 交付 |
| --- | --- | --- | --- |
| W1-P1：确认 IAM admission/workload 契约与 SQL 差距；P0 | IAM / `w1_iam_owner_auditor` / gpt-5.6-sol high / 只读，Root 审查 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-iam`；main `35d868a410c06731362bd1e8bcc3e602d01875f8`，clean | 读取三文档/contract/schema/身份代码与测试；不改文件/Git/基础设施；依赖批准设计和三大手册；输出精确 operation、缺陷证据、最小实现文件集及无破坏验证命令；Root 后续唯一提交人 |
| W1-P2：核实 Web same-origin 与核心聊天验收面；P0 | Web / `w1_web_chat_auditor` / gpt-5.6-sol high / 只读，Root 审查 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-app`；main `ce4e466c960c4b40a87a7be38b5a56f265f7a12f`，clean | 只读 session/CSRF/AG-UI/聊天/工具/审批/历史/附件/测试；排除 Mori；不改文件/Git/基础设施；与 P1 独立并行；区分真实、替身、缺失，输出浏览器验收矩阵与文件证据；Root 后续唯一提交人 |
| W1-P3：确定 BFF admission 及交接边界；P0 | BFF + Root / 主控 / 当前模型 / 只读分析，Root 控制文档唯一 writer | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff`；main `c5e9b3cc8eb134ff72e37f56ac1f95ebec4f42e7`，clean | 核对 auth/routes/projection/contract 与真实 owner 依赖；P1/P2 完成后冻结 Wave1 精确实施计划；现阶段不改子仓，不启动共享服务 |

验收方向：用户核心是可运行的完整对话体验。后续统一验收矩阵覆盖发送/流式/取消/重连/历史、结构化工具进度与结果、HITL 审批/恢复、附件/产物、错误与跨 tenant 隔离；W1 先交付可信身份入口，AG-UI/执行/Storage 完整闭环仍按 W2–W4 依赖实施。对照体验不等于宣称复制任何外部产品全部能力。

用户已确认（2026-09-22）：聊天和文件默认个人私有，显式分享才开放。当前验收同时覆盖同 tenant 不同用户与跨 tenant 负例；Project/Share 不自动授予 Run control/HITL/未分享文件访问。详见批准设计 §1.1。


### Wave1A 执行卡

| ID | 目标 / 状态 | Owner / Agent / 模型 | 基线 / 范围 / 验证 / 交付 |
| --- | --- | --- | --- |
| W1A-1 | 发布用户session admission；已验收 | IAM / w1_iam_owner_auditor / gpt-5.6-sol high；Root审查 | main35d868a4，绝对目录及精确文件集见当前计划；P1/P2盘点完成，682测试基线；不写Root/BFF/Web/schema；真实撤销矩阵+全门；Root唯一Git提交人 |
| W1A-2 | Root review/组合；已验收 | Root主控 + 独立审查 | 依赖W1A-1停写交付；计划Task2精确Root集成范围，edge状态不变，main-only/remote/clean真实审计 |

## 7. 核心聊天体验验收矩阵（P2 盘点，不是完成证据）

| 面 | 当前可验证代码事实 | 必须交付的真实闭环 | 后续owner |
| --- | --- | --- | --- |
| 身份入口 | Web旧magic-link直连IAM，无Auth.js/OIDC；CSRF缺Origin放行 | 当前issuer→BFF→Web同源，cookie/refresh/logout、无origin拒绝、身份撤销 | W1 IAM/BFF/Web |
| 发送与流式 | 已有AgUiChatTransport和durable BFF Chat；现验收主要fixture | 真实用户发送→Agent执行→持久消息→逐帧UI；刷新后结果一致 | W4 BFF/Agent/Web |
| 取消与恢复 | 本地stop、retry存在；UI缺完整queued/resuming/cancelling/reconnecting态 | 后台真正取消、竞态/重复、断网恢复、cursor过期回补，不生成重复消息 | W4 |
| 工具与HITL | mapper/多项staging有代码，主要fake测试 | 工具输入/进行中/结果、全部pending一次resume、刷新与重复审批、权限负例 | W3/W4 |
| 附件/产物 | 输入消息无attachment字段；文件展示主要fixture | 私有上传/扫描/下载、产物关联与恢复、同tenant他人拒绝、显式分享撤销 | W2/W4 |
| 编辑/重新生成 | 当前未实现，不能当现有能力 | 单独冻结branch/重生成语义与幂等、历史，后续契约切片实施 | W4 |
| 隐私 | BFF chat已有owner predicate；Project查询/缓存、ScheduledTask列表仅tenant，尚未个人隔离 | 同tenant A/B 与跨tenant列表/详情/事件/控制/文件/项目负例全通过 | W1/W2/W4 |
| 浏览器体验 | Playwright主要preview/login/axe/viewport | live聊天桌面/移动端、键盘IME、loading/error/partial/disabled、无障碍与恢复 | W4/W7 |

W1A-R：只读规格/质量审查，由 `w1a_task_reviewer`（gpt-5.6-sol/high）执行；范围为 IAM 基线35d868a4至冻结43文件，manifest/diff位于本计划scratch；不写代码/Git，不启动共享服务。Root并行复跑owner门，审查报告后统一裁决。

W1A-F：最终组合只读审查，由 `w1a_final_reviewer`（gpt-6-astra/high）执行；范围为Root当前计划集成diff与IAM35d868a4→30f7dbf两提交，重点consumer发布证据、gitlink/inventory和未完成边界；不重跑owner全门、不写仓库、不改index。

### Wave1B 执行卡（先文档门，再逐片实现）

当前节奏（用户2026-09-22裁决）：以功能代码、针对性测试和最小真实联调推进；运维加固、部署、镜像、SLO及重复全仓审计后置。身份/私有资源负例与数据正确性继续作为开发验收，不扩张成运维项目。

Owner为BFF；前置IAM release `259a66e6a569889c030734f380e99685d8b9e21c`、OpenAPI0.2.0已可固定消费。主控先冻结BFF三文档与精确文件集，再派同仓唯一负责人。目标是Node22 generated admission、删除自报header身份来源、明确public share/Scheduler服务边界，并承接已确认的同tenant私有资源权限缺口；Web OIDC/CSRF接线随后推进。不重复已验收W0B/W1A，也不提前激活edge。

| ID / 目标 | Owner / Agent / 模型 / 模式 | 基线与范围 | 完成条件 / 交付 |
| --- | --- | --- | --- |
| W1B-P：私有资源访问矩阵复核 | BFF / `w1b_privacy_reviewer` / gpt-5.6-sol high / 只读 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff`，main `c5e9b3cc8eb134ff72e37f56ac1f95ebec4f42e7`，clean；Project/Task/ScheduledTask/Chat/Share/AG-UI 的路由、repository、schema、cache、receipt、测试；排除其他仓写入与所有 Git/基础设施操作 | 每个缺口绑定具体文件和调用路径，给出最小修改集、同 tenant / 跨 tenant 负例以及服务回调例外；Root 定义最终架构与审查；状态：已验收（只读审查交付，不代表缺陷已修复） |

Root 并行负责 IAM admission、bootstrap/service 例外与 Node22 generated client 的实施设计；这里只扩展已有任务台账，不改变业务职责或授权运行代码重写。

| ID / 优先级 | Owner / Agent / 模型 | 基线 / 范围 | 验收 / 状态 |
| --- | --- | --- | --- |
| W1B-1 / P0 | BFF / `w1b_bff_owner` / gpt-5.6-sol high / 唯一写入 | main c5e9b3c → a898c90fe2b5447178a76fb04b0fedf9fa98e0d5，绝对目录同上；64个精确文件，Root提交 | SPEC/QUALITY通过；Root最终227标准/35真实integration、静态门通过；代码切片已验收，跨仓组合归W1B-3 |
| W1B-1R / P0 | BFF / `w1b1_task_reviewer` / gpt-5.6-sol high / 只读；Root集成验证 | c5e9b3c + 最终冻结64文件，与a898c90提交字节一致；未操作Git/基础设施 | 首轮2 Important均已修复；增量复核SPEC Compliant/QUALITY Approved，0未决；状态：已验收 |
| W1B-2 / P0 | BFF / 原`w1b_bff_owner`额度中断后Root唯一写入、`w1b2_privacy_review`只读复核 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff`，main a898c90 → `6238599667110fbfbc2d5ef3a9d53731f2623cfe`，35文件，已推送、clean；原Task2范围加批准的旧fixture修复 | Root Node22 format/lint/typecheck/contract25/标准231/schema4/build/fresh PG+Redis integration37全绿；独立审查2条P2已修复复核无新阻断；自有DB回收、Redis8不变；BFF owner切片已验收，跨仓组合归W1B-3 |
| W1B-3 / P0 | Root主控 + 独立审查 | IAM/BFF owner及Root `7b6e486b…`组合已推送main，旧smoke均接线且通过 | 真实IAM↔BFF 7/7、Capability8/8、Scheduler11/11、System组合PASS；新checkpoint5/11/1与main-only/clean PASS；状态：已验收（仅本组合切片，W1/Web未完成） |
| W1B-3P / P0 | IAM测试入口 / `w1b3_iam_fixture_reader` / gpt-5.6-sol high / 只读；Root审查 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-iam`，main 259a66e6；仅既有HTTP fixture/consumer测试/API入口，不写文件/Git、不启动服务、不操作数据库；与BFF Task2独立 | 已定位真实PKCE/session/在线membership与自有资源fixture；Root可复用本地测试CLI，不需IAM部署改造；状态：已验收（只读预检，不是联调通过） |
| W1B-3A / P0 | IAM测试入口 / `w1b3_iam_host_owner` / gpt-6-sol / 唯一写入；Root复验提交 | `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-iam`，main 259a66e6→`b2ad9dd6906b73f275b96d570dad66eae86e97e9`，两文件，已推送clean；不碰生产入口/schema/contract/lockfile | NDJSON真实IAM host与3项聚焦integration、Node24 typecheck/format/lint均通过；Root独立资源快照确认零新DB/Redis键；独立复审P1/P2均关闭。IAM测试入口已验收，Root消费仍归W1B-3B |
| W1B-3B / P0 | Root真实IAM↔BFF组合 / `w1b3_root_iam_smoke_writer` / gpt-6-sol / 仅两脚本写入；Root审查与Git唯一负责人 | Root `77c9c5db`→`1d1c4df85fd7b7d39a8465d9746ac2be59b5ba4d`，IAM gitlink→`b2ad9dd…`，BFF `6238599…`不变；两脚本、台账与106 BFF/3 IAM机械fan-out已推送 | Root提交后无过渡参数真实HTTP **7/7**，SHA/clean/gitlink均匹配，w0b-exit checkpoint通过；独立P1/P2已关闭，自有资源清理通过。IAM edge仍broken待W1B-3C/D；状态：已验收 |
| W1B-3C / P0 | Root旧Capability/Scheduler smoke准入改造 / `w1b3_legacy_smoke_writer` / gpt-6-sol / 唯一脚本writer；Root审查、Git与台账负责人 | Root `1d1c4df…`→`0295fbdda439a4008cb114c8d726cf893694fc4b`已推送main；9个获授权脚本/测试，新增共享IAM wire stub，未改子仓/System | Root聚焦**110/110**、全scripts **524/524**，真实Capability **8/8**、Scheduler **11/11**及自有资源清理均通过；独立审查2项P2已修复复核无新阻断，w0b-exit与topology PASS。stub只供旧owner回归；状态：已验收 |
| W1B-3D / P0 | Root System跨仓smoke新准入与路径修复 / `w1b3_legacy_smoke_writer`（续任）/ gpt-6-sol / 唯一脚本writer；Root审查与Git负责人 | Root `0295fbdd…`→`9fa6d2cd68d06dbd9dc235da520ba4c15089b1e1`已推送main；仅System runner与对应测试，子仓不改 | Root聚焦**23/23**、全scripts **525/525**、真实System/BFF/Agent组合PASS、独占PG/Redis/临时文件零残留；独立审查1项P2 gitlink门已修复复核无新阻断；状态：已验收 |
| W1B-3E / P0 | Root激活BFF→IAM精确契约edge / Root唯一writer + `w1b3_edge_release_review`独立只读审查 | Root `9fa6d2cd…`→`7b6e486b630a40ff825736299ef02720cbfbef6a`已推送main，IAM `b2ad9dd…`/BFF `6238599…`clean；只改Root inventory/checkpoint/test/文档 | 唯一IAM edge broken→active，31个consumer blob与IAM0.2.0 contract/旧vendor字节一致，2项版本断言；checkpoint **5 active/11 broken/1 illegal**、聚焦81/81、全Root526/526、topology及main-only（Root+11仅main/clean）PASS；独立SPEC/QUALITY0/0。状态：已验收 |
| W1C-P / P0 | Web同源登录依赖预检 / `w1c_web_auth_reader` + `w1c_iam_oidc_readiness` / 只读；Root裁决 | Web `apps/kokoro-app` main `ce4e466c…`，BFF `apps/kokoro-bff` main `6238599…`，IAM `b2ad9dd…`；仅读Web auth/session/cookie/CSRF route及BFF/IAM协议，不改文件/Git/资源 | 已确认IAM Code+PKCE与精确allowlist已运行；BFF尚无`/iam` relay，Web旧IAM直连且多数BFF代理缺Bearer；两份只读报告已交付，未运行测试；状态：已验收（只读预检） |
| W1C-1 / P0 | BFF受控`/iam`原生协议relay / `w1c_bff_relay_owner`实现、来源重pin由Root集成 | BFF `cd1c2600…`、IAM `b838853a…`及Root `bb60a6a…`已推送main | 精确path/method服务例外、原生HTTP/cookie/redirect保持；BFF Node22标准248/248、contract25/25；当前pin真IAM7/Capability8/Scheduler11/System及首次OAuth15/15均复验，自有资源0；1H脚本已发布。状态：IAM→BFF relay切片已验收，Web浏览器链另验 |
| W1C-1F / P0 | IAM test-owned Web OIDC首次流fixture / `w1c_iam_flow_owner`唯一IAM writer；Root审查 | IAM `f0bb18e6…` fixture、`c9a27721…`退出URI修正均已推送main；仅测试文件，生产API/schema/contract未改 | HTTPS公开origin、未预consent RP client、真实sign-in→authorize首次重定向；Root聚焦5/5、退出URI修正后5/5，自有DB/Redis零残留，独立初轮复审0 P1/P2；完整Code+S256/tenant/consent/token/userinfo/logout仍归Root组合，状态：已验收（测试入口） |
| W1C-1G / P0 | Root从gitlink commit blob核验BFF relay↔IAM发布证据 / `w1c_root_relay_verifier`实现、独立审查，Root集成 | Root脚本 `070b0589…`、新IAM/BFF/Web gitlink及库存 `a5ca0afc…`已推送main | 固定IAM allowlist/snapshot与BFF policy的commit/digest、route/method子集/disabled负例；最新pin真实CLI/checkpoint/topology PASS，Root全量545 passed+10 subtests，独立库存审查0 P1/P2；状态：已验收（来源门，不替代OAuth正向行为） |
| W1C-1H / P0 | Root真实IAM→BFF relay首次OAuth正向组合 / 原脚本writer与首修writer、`w1c_oidc_runner_fix2`唯一Root脚本writer；Root与独立审查 | IAM `b838853…`、BFF `cd1c260…`及Root `bb60a6a466c73940ce7e1f19d7efcb0742571dea`已推送main | 第二轮四项P1/P2均修复、独立复审0 P1/P2；Root独立真HTTP首次无session登录→签名continue→tenant→consent→Code+S256/token/userinfo/revoke/logout **15/15**，Root全scripts **563 passed、56 subtests**，自有资源0；发布后relay policy/checkpoint/topology PASS。无hint HTML logout只单测，归1J；状态：已验收（仅IAM→BFF，不是Web浏览器闭环） |
| W1C-1I / P0 | IAM test-owned OIDC host精确public JWKS回环 / `w1c_iam_flow_owner`唯一IAM writer；Root审查/Git | IAM `b838853a81ff34bd0f7a079ccc75ba6abd61d1ec`已提交推送main，BFF机械policy来源`cd1c260…`已发布 | 仅IAM两测试文件，精确issuer `/jwks`→fixture loopback；Root独立Node24真实登录→带hint退出聚焦5/5、format/lint/typecheck、资源0；独立审查0 P1/P2，非阻断P3 restore直接断言缺口；生产代码/contract/schema不变。Root组合15/15已复验；状态：IAM owner切片已验收 |
| W1C-1J / P1 | BFF relay无hint退出metadata只读审查 / `w1c_bff_logout_metadata_reader`；Root裁决 | BFF main `cd1c260…`、IAM `b838853…`、Root `85e06c53…`，未改BFF/Web | Better Auth在无hint且非导航时返回400；BFF/Web已保留`Accept`但不透传`sec-fetch-*`。Web带server-only `id_token_hint`的主路径已真HTTP15/15；无hint HTML确认为单独可选验收，不阻断2B/3，不为此改BFF。状态：只读审查已验收 |
| W1C-2 / P0 | Web Auth.js RP、同源adapter与Product Session / Web单仓分片唯一writer；Root审查/Git | Web `bbd8f0806e3c040200af9d478828232baae57ff2`；BFF `2d951e1a56b5720431963d728b74f662e2379999`；IAM `606d9090c2282e13370e17a20379a32629df9722`，Root `8e8cd88458af6f193b3bd7a28f6f2a1223a1d65a` | 三页同源交互/CSRF与真实HTTPS RP-only 14/14已验；Product Session、全代理Bearer、旧IAM直连删除未完成。状态：部分完成，不是登录闭环 |
| W1C-2A / P0 | Web固定policy来源与同源只读`/iam`入口 / `w1c_web_get_relay_owner`唯一Web源码writer，Root审查与Git负责人 | Web `38683a5…`→`85b4bad25769efcca8f417e0232fdaa6c485bf01`、Root `6f7b31c29f019f1306e6ebd03b7ccca725c729b3`已推送main；BFF `cd1c260…` policy digest `457909cd…`字节相同 | 两轮审查发现的P1/P2全修，增量独立复审0 P1/P2；Root独立Node22 `pnpm check`（1259 tests）与Playwright6/6、Root 563脚本、policy/checkpoint/topology通过，真实Next HTTP含chunked拒绝/零BFF及隔离目录清理。Root发布后main-only/topology/policy/checkpoint PASS、全12仓clean。GET窄子集已发布；Auth.js/POST/userinfo/end-session仍未接线，浏览器登录不成立；状态：Web与Root集成均已验收 |
| W1C-2B-1 / P0 | Web交互POST/CSRF与最小真实续接 / `w1c_web_post_foundation_owner`唯一writer；Root审查/Git | Web `85b4bad25769efcca8f417e0232fdaa6c485bf01`→`d619f2c06951cb2decdb1eeac48547e3bcf40361` 已推送main且clean；27文件，含Redis server-only依赖/lock、env/CI与现有原生Location策略定点扩展 | 独立复审原1 P1/2 P2已修复0 P1/P2，P3 302回归亦补；Root Node22 `pnpm check` contract52/architecture32/标准1297与lint/typecheck/build、Playwright6/6，Redis9 0→0。仅sign-in交互，Auth.js/Product Session/旧IAM直连仍待后续；Root gitlink/库存已随`20112fcb…`集成；完整Web→BFF→IAM浏览器登录仍待RP。状态：Web owner与Root来源集成已验收 |
| W1C-2B-2 / P0 | Web Tenant选择与Consent两页安全续接 / `w1c_web_post_foundation_owner`唯一writer；Root审查/Git | Web `d619f2c…`→`14e23e602a5631009584d84f58871e51d32b821c`，17文件已推送main且clean；BFF `a4dbc333…` policy原字节、IAM `6bc9b19…`来源 | 独立初审1 P1（issuer cookie Path）及终审2 P2（删除cookie后CSRF、HEAD/OPTIONS）均按TDD修复，最终0 P0/P1/P2。Root Node22 `pnpm check`：contract52、architecture32、标准1320、lint/typecheck/build均PASS；Playwright6/6。真实Next+Path-aware CookieJar/session-required BFF fixture证明三页续接、静态路由、删除cookie及405零BFF/Redis；RP callback受控503，非真实IAM/RP闭环。Root gitlink/库存已随`20112fcb…`集成；状态：Web owner与Root来源集成已验收，RP/真浏览器组合待办 |
| W1C-2C / P0 | Web Auth.js RP + server-only token/userinfo / `w1c_web_post_foundation_owner`唯一writer；Root审查/Git | Web `14e23e6…`→`c71aa3f5130ae52c7f76356ac38bdabaf551d4e9`，22文件已推送main且clean；BFF `a4dbc333…`/IAM `6bc9b19…`来源不变，Root 9处库存/gitlink同步 | 固定issuer/client/URI/resource、显式验证型`client.callback`、EdDSA、Redis state一次消费；真实Next+BFF fixture含签名、重放、CSRF、body/响应限额、在途/预取消。独立审查最终0 P0/P1/P2；Root Node22 contract52/architecture32/标准1339、lint/typecheck/build、Playwright6均PASS。成功仅受控503且无新session；真实IAM签名组合与Product Session未完成。状态：Web owner切片已验收，非登录闭环 |
| W1C-2C-Proxy / P0 | Web 固定公开来源与 Next 反代误拒修复 / 原writer容量错误后`w1c_proxy_web_owner_sol`接手，Root审查/Git | Web `c71aa3f…`→`12e07eb5874b085be989364606d485dce0722249`，13文件已推送main且clean；只改Web IAM/RP同源入口、共用Host/Origin准入、文档与真实Next代理式测试 | 五处内部URL origin误用清除；固定Host、POST精确Origin、GET可选Origin，路径/query/CSRF/跳转不退化，不信X-Forwarded；独立复审0 P0/P1/P2，Root Node22 contract52/architecture32/标准1344、lint/typecheck/build、Playwright6均PASS。HTTP代理式fixture非真实TLS，后者归2D。状态：Web owner与Root来源集成已验收 |
| W1C-2D / P0 | Root真实 HTTPS Web→BFF→IAM RP-only 组合 / `w1c_root_smoke_hardening_sol`唯一Root脚本writer、`w1c_iam_fixture_identity_sol`唯一IAM测试fixture writer，Root审查/Git | 固定Web `bbd8f0806e3c040200af9d478828232baae57ff2`、BFF `2d951e1a56b5720431963d728b74f662e2379999`、IAM `606d9090c2282e13370e17a20379a32629df9722`，均已推送main并由Root `feab1c7…`精确gitlink/库存pin；Root两脚本已随`519d5a924b9b0d54a97edc760f82a965e366d1ea`提交推送 | IAM实际三键`code/state/iss`严格验、IAM test fixture预分配UUID+owner marker与SIGKILL定向清理、Root runner两轮审查最终0 P0/P1/P2；Root独立Web1345/Playwright6、IAM verify704+真实PG/Redis13、BFF252/253(1 skip)+真实schema6、Root pytest588/78均PASS。真实HTTPS三服务RP-only **14/14**、自有资源0；旧 IAM→BFF新pin15/15。终态仍受控503无Product Session、旧直连与Product/AG-UI未闭环；Root发布后main-only 12/12仓PASS、policy/checkpoint/topology PASS，静态规范9仓130 violations未清。状态：已验收（仅RP-only三仓组合） |
| W1C-2E-P / P0 | Product Session与Team切换前置只读审查 / `w1c_product_session_reader`、`w1c_team_api_reader`并行；Root裁决 | Web `c71aa3f…`、IAM `6bc9b19…`、BFF `a4dbc33…`；仅读三仓技术/API/数据文档、旧直连、generated client、UI调用与有效ADR，无写入/资源 | 已定位IAM ADR要求加密refresh存Redis+败者恢复，而Web三文档为双CAS fail-closed且不存token，两目标不能并存；旧Team UI依赖IAM `/bff/*` 无机器契约、BFF无Team Product API且现RP scope不含成员/邀请write。实施前Root定唯一session语义、IAM owner先补所需读契约、BFF发布窄Product projection，再Web原子切换；默认个人私有/显式分享不变。状态：只读预检已完成，设计/代码待裁决和派工 |
| W1C-2F-D / P0 | 会话语义冲突收敛 / Root裁决，IAM/Web各仓唯一文档writer | Web `ebd7703a1adfccf0f6c56e746d9735ac47d63021`三文档；IAM `65b0fd969989d4044fae640a8414d9c2dcf41c3b`四文档/ADR；Root集成 `251284c053b68271bb3599625aec43ad8ae67e5d`，均推送main且clean | V1双CAS/单次refresh/旧generation拒绝；Redis Web加密当前refresh，active logout取当前credential，pending只tombstone不revoke旧token；败者不撤销赢家。独立审查2个P2已修；Web contract52/architecture32、IAM contract check、Root tests588、真实三仓RP-only14/14、checkpoint/policy/topology/main-only新pin均PASS。状态：已验收（仅设计/来源） |
| W1C-2F-P / P0 | 新IAM来源→BFF→Web policy provenance闭环 / BFF与Web各仓串行唯一writer，Root集成 | IAM `65b0fd9…`；BFF `eb7ded2386efd9a10905843a7a5aedff9ac72df6`；Web `5da730426faaca54a9f0003fa1e7fd99f4db6f00`；Root `251284c053b68271bb3599625aec43ad8ae67e5d`，均推送main且clean | BFF4文件、Web7文件机械pin；policy原字节一致，SHA-256 `05e2068376ef79b6aba8eff0f170a3a2bd0a0a5b31bc6836b3de9f682ff86a10`；Node22聚焦contract/architecture通过，未改路由语义；Root inventory149 tuple/唯一新digest、checkpoint/policy/topology/main-only、scripts588、真实RP-only14/14新pin通过。状态：已验收（仅来源） |
| W1C-2F-S1 / P0 | Product Session store、callback与logout/refresh基础代码 / Web `w1c_web_cutover_audit`唯一源码writer，Root审查/Git | Web `5da7304…`→`1ba0498…`→`0e0ec3a…`→`c3d81bf16fc62cef59ab33f6cc61a6677a38383c` 已提交推送main且clean；BFF/IAM新来源已消费；Root `a59bc736…` gitlink/库存已跟进 | callback创建加密HttpOnly会话；Redis双CAS、单次refresh、active-current revoke/pending tombstone；退出只返回待浏览器确认的issuer URL，实际confirm才删IAM session。首次真HTTPS暴露Product Cookie Secure不应依赖production，已按固定HTTPS origin修复。独立终审0 P0/P1/P2；Root Node22 contract52/architecture32、Vitest1369、lint/typecheck/build、Playwright6均PASS。固定三仓真实HTTPS Product Session、refresh、旧generation、current revoke与issuer确认已通过且自有资源0；普通代理Bearer/旧路由删除为S2。状态：S1已验收，非Web总体闭环 |
| W1C-Team-D / P0 | IAM当前tenant roster/invitation/role窄读三文档门 / IAM `w1c_session_semantics_audit`唯一文档writer，Root审查/Git | IAM `65b0fd9…`→`f240bd7d5f542bb152c7eb929074c96b6c290ea8` 已提交推送main且clean；仅三设计文档；独立复审0 P0/P1/P2，Node24 contract check、Prisma validate通过 | 三个user-only read scope、持久权限、最小字段/分页/索引/issuer pending边界已收敛；仍无runtime/schema/OpenAPI/generated SDK，不冒称Team行为可用。状态：设计文档门已验收 |
| W1C-Team-P / P0 | IAM文档新来源→BFF relay机械pin / BFF唯一writer、Root审查/Git | BFF `eb7ded2…`→`1d1f42775e0fa4464de6b08ee9d2b9cd82911a71` 已提交推送main且clean；Web `0e0ec3a…` 已消费；policy字节digest `bbd86696e1b36a82c1ebd35262dba3950a35d56d7d63856df217f397d8b48819` | BFF仅四处来源pin；Node22 policy聚焦4/4、contract check通过；Web已消费pin，Root `a59bc736…` 最终gitlink/库存已集成且S1真HTTPS通过。状态：已验收（仅来源） |
| W1C-2F-S1-HTTP / P0 | Root真实HTTPS Product Session组合验收 / 首版`product_smoke_plan`脚本writer、Root续写/审查/Git | Root `594358ba…` 首版、`0c829a0…` HTTPS cookie pin、`a59bc736…` 固定Web `c3d81bf…`/BFF `ddb462e…`/IAM `f240bd7…`；最终runner/台账已随Root `86f2f0dd…` 提交推送 | 两轮真实RED分别定位Web Secure cookie与BFF Node fetch cors；最终真HTTPS流程 **PASS**：callback→HttpOnly session→单次refresh→旧代GET/signout拒绝→当前revoke→IAM无hint确认POST→issuer失效，自有资源0。独立终审0 P0/P1/P2；Root全scripts **597 passed/108 subtests**、聚焦9/30、Ruff通过。只证明S1，不证明普通Product Bearer/Team/AG-UI。状态：S1三仓组合已验收，Root main-only发布后PASS |
| W1C-S1-HTTP-BFF / P0 | 无hint退出浏览器导航跨BFF relay / `bff_logout_navigation_fix`为BFF唯一源码writer；Root审查/Git | BFF `1d1f427…`→`ddb462e6ab3a7270a3dab248ba7ee887b0ec9ba2` 5文件已提交推送main且clean；Root `a59bc736…` gitlink/库存已跟进 | Node22 Undici即使显式设置navigate也强制发送cors（TDD红）；只对已准入`/oauth2/end-session` GET走固定IAM origin的有界原生HTTP，内部合成navigate，其他路由仍fetch；独立审查0 P0/P1/P2。Root Node22 `pnpm check` 255 pass/1既有skip、lint/typecheck/contract/build通过；真HTTPS固定三仓S1流程PASS、自有资源0。状态：BFF owner与Root S1组合已验 |
| W1C-S1-STALE / P0 | 旧generation Cookie不得退出当前会话 / `web_stale_signout_fix`为Web唯一源码writer；Root审查/Git | Web `0e0ec3a…`→`c3d81bf16fc62cef59ab33f6cc61a6677a38383c` 10文件已提交推送main且clean；含BFF `ddb462e…`来源机械pin；Root `a59bc736…` gitlink/库存已跟进 | Redis Lua原子CAS失配不删最新session、不revoke、不返回issuer退出引导；独立初审P2发现旧响应清同名cookie会删除并发新代cookie，已修为stale响应无Set-Cookie，终审0 P0/P1/P2；单测+真实Next HTTP覆盖旧cookie signout后新cookie仍有效。Root Node22 contract52/architecture32、Vitest1371、lint/typecheck/build、Playwright6均PASS；Root真实HTTPS runner已补stale负例，已在新pin真HTTPS流程执行PASS、自有资源0。状态：Web owner与Root S1组合已验 |
| W1C-2F-T / P0 | Team owner-first契约和消费闭环 / IAM→BFF→Web逐仓唯一writer | IAM先定当前tenant roster/invitation窄读、user-delegated scope/permission和issuer-only pending邀请；owner OpenAPI+runtime测试发布后BFF生成窄Product投影，最后Web删除旧`/bff/*`和team-session | 不伪造scope或以membership授予聊天/文件访问；未加入tenant的邀请经issuer-session窄relay，不经Product admission。各仓contract/行为/真实HTTP后Root验收。状态：待派工 |
| W1C-Team-R1 / P0 | IAM当前tenant成员/邀请/角色三个窄读GET的owner runtime、canonical索引、OpenAPI/SDK与真实验收 / `iam_team_read_owner`唯一IAM writer；Root审查/Git | 基线IAM `f240bd7d5f542bb152c7eb929074c96b6c290ea8`、`main`、clean；三文档 `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-iam/docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 的W1C-Team-D设计门已对齐；机器契约/SDK与运行端点尚无。只写IAM `src/modules/{auth/oauth,auth/better-auth,auth/internal-access,tenancy/organization}`相邻源及测试、`src/modules/tenancy/tenancy.module.ts`仅DI、`src/http`既有no-store路由matcher及固定INVALID_ARGUMENT cursor提示/测试、既有`sdk/src/` facade/types与`sdk/package.json`版本、`scripts/generate-contracts.ts`仅三GET生成/版本、`prisma/schema.prisma`、owner生成contract/SDK与IAM CURRENT/ACCEPTANCE；排除Root/Web/BFF、其他IAM业务 | 三个user-only scope不进共享`TENANT_SCOPES`或machine；Guard显式list读分支，同一RepeatableRead快照验证当前membership/role/permission/tenant再查询；固定权限矩阵+动态角色、HKDF cursor、keyset分页、三索引、损坏依赖503、跨tenant/机器/失效401/403，真实Code+PKCE/refresh与PG/Redis测试，fresh schema/零FK/EXPLAIN，生成OpenAPI/SDK+consumer；`pnpm verify`、integration、consumer、build。Agent交付文件清单不动Git index/commit，Root在冻结树独立重跑并提交、推送、提升gitlink。状态：IAM owner已验收：`68aa0da`推送main、Root独立verify739/integration220/clean consumer2/2、独立审查0/0/0；Root gitlink/库存待下游pin同步 |
| W1C-Team-R2 / P0 | BFF IAM Team 三读的公开Product投影与generated consumer / BFF单仓唯一writer；Root审查/Git | IAM owner `68aa0da` 已提交推送main，OpenAPI/SDK 0.3.0、真实PG/Redis与clean consumer已验；BFF从`ddb462e6ab3a7270a3dab248ba7ee887b0ec9ba2`分两片发布：generated consumer `e79dda6111427a477d83cf1ac7783b106af6fb9c`，公开Product三GET `fd74202e69e4d40beaef9d3f9ab9b871365589a8`，均main/clean/已推送。Root持Git。 | `/v1/team/{members,invitations,roles}`已使用已准入用户Bearer调用IAM；无自报tenant/subject、无跨owner DB，严格limit/cursor、错误/Retry-After/no-store/请求ID、取消和超限。Node22 `pnpm check` 261通过/1既有skip，format、contract、typecheck/build通过，fake IAM HTTP六用例通过。真实 IAM HTTP、Web OAuth 三读scope/fixture、relay/Web来源re-pin及Root gitlink仍待验，故R2只是BFF单仓切片已交付，不算跨仓闭环。 |
| W1C-Team-R3-IAM / P0 | Web OIDC测试客户端授予已发布的三个Team只读scope / IAM fixture唯一writer、Root审查/Git | IAM `68aa0da259df1f1ea9030936b8d5a46acba8c6ab`、main/clean；范围仅 `test/fixtures/internal-http-application.ts`、`test/integration/web-oidc-flow-host.test.ts` 及必要的IAM测试文档。复用IAM现有 `ORGANIZATION_READ_SCOPES`，不修改owner生产权限/contract/schema；Root独占index/commit。 | 真实测试客户端注册、授权请求和token校验包含 `iam:member.read`、`iam:invitation.read`、`iam:role.read`；IAM焦点测试先红后绿。只运行前台、可等待的测试，不单独启动长期服务、不清理共享PG/Redis；完整IAM全门与真实隔离integration由Root串行验收。状态：IAM main `c0f6068731b8a506cd2d3554e72719aa7327f2be` 已精确2文件提交推送且clean；Root Node24 `pnpm verify` 739/739与真实隔离PG/Redis integration 220/220通过，Test-owned Web OIDC client三读scope有效。 |
| W1C-Team-R3-Web / P0 | Web Product OIDC申请Team三读scope / Web唯一writer、Root审查/Git | Web `563c9f25a538d3ae39d79d662388f26544543e79`、main/clean；范围仅 `src/lib/server/oidc-provider.ts`、对应单元/contract测试及必要的Web设计文档。固定scope顺序与IAM常量一致，不改Team UI/旧直连/relay policy；Root独占index/commit。 | 授权请求与回调严格scope断言包含三个Team只读scope，不添加写权限；焦点测试先红后绿。只运行前台、可等待的测试，不启动Next dev/共享服务；完整Web门与真实三仓HTTPS由Root在IAM发布并re-pin后串行验收。与R3-IAM分仓并行写入，二者不共享文件/构建资源。状态：Web main `c12eba173e54f48c4f3bc816f6a2addbd1cfe7aa` 已精确4文件提交推送；Root Node22 `pnpm check` contract52/architecture32/Vitest1384、lint/typecheck/build通过。后续Web `670f0ff` relay pin单独提交；真实三仓待验。 |
| W1C-Team-R3-Pin / P0 | 新 IAM fixture 来源的 BFF→Web relay policy provenance 与 Root 组合 pin / Root 串行唯一writer、按owner先后提交 | IAM `c0f6068731b8a506cd2d3554e72719aa7327f2be` 已发布；BFF `fd74202e69e4d40beaef9d3f9ab9b871365589a8`、Web `c12eba173e54f48c4f3bc816f6a2addbd1cfe7aa` 均已发布且clean。范围限 BFF policy TS/生成JSON/精确test/API_CONTRACT/CURRENT；随后 Web 只读generated snapshot/provenance TS/test与相应文档；最后 Root 三gitlink、consumer inventory、验收脚本/台账。 | 先核 IAM allowlist/snapshot blob digest 未变；BFF生成 artifact 后Node22 contract/check，提交取得新SHA；Web再消费固定BFF SHA+artifact digest并验contract/check，提交取得新SHA；Root库存按最终commit blob全量更新并运行policy/checkpoint/topology/tests/真实三仓HTTPS。禁止并行写相同来源、禁止仅修改Markdown伪装pin通过。状态：policy/组合pin已验收，非Team业务闭环。BFF `1917f9097d08a38128ed5f4087c826356142c548`、Web `670f0ff77ec71aaa03c15097587cc1071f85cfd9`及IAM `c0f6068731b8a506cd2d3554e72719aa7327f2be`均main/clean；Root `7cc0803194d355f68e94f6e764a1b97301c08406`已提交推送三gitlink/库存/runner。policy/checkpoint/topology/main-only、Root pytest598/108均PASS，固定三仓真实HTTPS S1通过且自有资源0；真实Team HTTP/Web Team消费另片待验。 |
| W1C-Team-R4-HTTP / P0 | 固定发布SHA的真实IAM→BFF Team三读与拒绝路径 / Root唯一脚本writer、独立只读审查 | Root `ff560b2f6d2095a67bf962f43526fb1e752747c4`、IAM `c0f6068`、BFF `1917f90`、Web `670f0ff`均main/clean；只改现有`scripts/e2e/run_bff_iam_oidc_smoke.py`与`scripts/tests/test_bff_iam_oidc_smoke.py`及Root任务/进度账，不改业务仓或共享基础设施。 | 在已有自清理OAuth fixture取得真实user-delegated access后，经BFF public `/v1/team/{members,invitations,roles}`验证IAM真实返回、当前tenant绑定、no-store/request-id、无Bearer拒绝、伪造tenant/actor头不越权；TDD聚焦pytest先红后绿，固定SHA真实HTTP通过且自有资源0，Root scripts测试与进程检查；Web Team消费仍另片。状态：已验收；真实19 case、自有资源0、独立复审0/0/0、Root scripts测试601通过/119 subtests，Ruff、policy、checkpoint、topology通过；Root commit与main-only见本切片发布记录。 |
| W1C-Team-R4-Web-Audit / P0 | 盘点旧Web Team路由/UI至BFF Product的剩余迁移与IAM owner契约缺口 / Web只读审查Agent，Root裁决 | Web `670f0ff`、BFF `1917f90`、IAM `c0f6068`冻结；只读`docs/CODEBASE_MAP.md`、三个owner三设计文档、旧`/api/team`路由与调用UI、IAM 0.3机器契约；不改文件/Git/DB/Redis/进程。 | 输出每个入口→owner operation→scope→BFF Product投影→可删旧路径矩阵，明确三GET不能覆盖本人未入组pending邀请、Team mutation/switch；给出最小owner-first切片、所需负向测试及路径，不能将旧直连静默删除。状态：已验收（静态盘点）；三GET只覆盖当前租户只读列表，旧Team route均不能整文件删除；本人pending邀请须IAM issuer契约，切换须OIDC重新授权，写操作须BFF Product投影；不冒称运行验收。 |
| W1C-S2-A / P0 | 普通Web→BFF代理原子切为在线Product Session Bearer，明确service-only例外 / `web_product_bearer_owner`为Web唯一writer；Root审查/Git | 基线Web `c3d81bf16fc62cef59ab33f6cc61a6677a38383c`、BFF `ddb462e6ab3a7270a3dab248ba7ee887b0ec9ba2`；Web S2只读审查已定位七个普通adapter、shared与runtime-manifest例外及其现有测试；IAM Team与本片无源码依赖。先核对Web三文档门并修改Web同源adapter/相邻server helper、登录UI/会话探针/两个退出入口及对应测试/必要文档；不得改Team路由/UI或其他仓 | 七个普通代理只取`currentProductSession`在线generation与`claims.access`，仅一Bearer+service身份；不透传runtime_jwt、namespace/principal/tenant、自报Authorization/Cookie；旧generation/tombstone/pending和Redis故障fail-closed且零BFF请求；mutation Origin、SSE/cursor/取消、幂等、stream/body limit不退化。shared/runtime-manifest保持service-only且不读旧信封；受保护响应固定private/no-store，即使BFF回public。用户可见登录用Product RP，session探针只认可Product Session；两个退出入口必须CSRF保护调用Product signout并完成issuer确认导航，不仅仅换路由。Node22 contract/architecture/Vitest/lint/typecheck/build、Playwright与真实Next HTTP负例由Root复验；本片不声称旧Team直连或旧登录端点已删除。状态：Web owner源码已验收并发布 main `563c9f25a538d3ae39d79d662388f26544543e79`，Root独立 Node22 contract52/architecture32/Vitest1375/lint/typecheck/build 与 Playwright 9通过/1移动端桌面侧栏跳过；独立终审1 P1/2 P2均由Root定点TDD修复。Root gitlink/库存及真实三仓普通Bearer组合仍待验，S2总体未闭环 |
| W1C-S2-HTTP / P0 | 真实 HTTPS 浏览器 Product Session → 普通 Web Chat 代理 → BFF listSessions / Root唯一脚本writer、独立审查 | Root起始 `e682bab689db0aaca815f90af6c0e594e03e6b76`、IAM `c0f6068`、BFF `1917f90`、Web最终`7c0ab9833aa120601b13ff9ea003d39474d006cd`；只改现有 Product Session smoke及其单测和Root台账，不改BFF/IAM业务子仓 | 匿名/旧代/退出后均401且零BFF Chat请求；在线cookie经Web `/api/session/sessions`访问BFF `/v1/sessions`返回200、private/no-store、request ID，浏览器自报Authorization不覆盖在线Bearer。状态：已验收本切片；TDD聚焦11通过/41 subtests，Root `e6f44c86e2ee8debae1faa71f295ff51433b42c2`固定pin后真HTTPS返回`product_chat_proxy=verified`、`active_then_ended`、自有资源残留0；不代表首条聊天消息和Agent执行完成。 |
| W1C-S2-Web-Cache / P0 | Chat 同源代理最终响应保持 `private, no-store` / Root定点小修、真实Next HTTP回归 | Web起始`670f0ff`；先修Chat route成功JSON分支，再修Next中间件覆盖响应头；只改相邻route/proxy、测试和CURRENT，不改BFF/IAM/Storage。 | 两次固定SHA真HTTPS分别发现route和middleware缓存缺private；均先RED再修复。状态：已验收；Web最终main`7c0ab9833aa120601b13ff9ea003d39474d006cd`，真实Next HTTP与route聚焦9/9、Node22 check contract52/architecture32/Vitest1384、lint/typecheck/build、Playwright9通过/1既有skip，Root最终三仓真HTTPS通过。 |
| W1C-Web-Public-Entry / P0 | `/`公开首页、`/login`独立Product登录、`/app`受保护工作台；删除旧预览及HomeGate / Root作为Web唯一writer | Web `7c0ab98`→`678396e486d39d203eb50361d108c2a9dbf4695a`，Root固定gitlink `3b332c59fb8fa482b014c650b4a547de4e7d7ba1`；仅Web路由、LoginPanel、相邻测试/文档与Root库存 | 登录页不请求System runtime manifest，System 503时仍渲染；`/`为真实首页、`/preview/marketing`为404，Product OIDC/CSRF真实路径保留。TDD先红后绿；Node22 `pnpm check` contract52/architecture32/Vitest1383、lint/typecheck/build通过，Playwright11通过/1已知skip；固定SHA真HTTPS Web→BFF→IAM Product Session/Chat list通过、自有资源0。状态：已验收本片；Web Team旧直连与首条聊天/Agent执行仍未闭环。 |
| W1D-Web-Gate / P0 | 已认证核心工作台独立于 System presentation，删除“配置不可用”整页 / Root作为Web唯一writer | Web `8479c8563351928d056aae605a588407f5bfe2a3`→`3074f9bba7dc1f95737b41e50ae8ad152bb5e417`；只改 `ui/auth`、runtime presentation hook/产品默认值、旧死文案/测试与相邻文档，不改会话或BFF API | Product Session 仍唯一认证闸；System 503/加载中不挡 live Chat、不切 preview，旧皮肤在失败时清除；TDD 2 RED→GREEN、Node22 contract52/architecture32/lint/typecheck、串行 Vitest1385、build、Playwright11通过/1既有skip，真实浏览器认证探针+System503显示`/app`工作台；Web main与Root `cc7c9d8bb03daeaebd5e6caf0eb48754b9cac03d`已推送；固定SHA真HTTPS Product Session/Chat列表通过、自有资源0。状态：已验收本片；首消息Agent执行仍待B2及组合。 |
| W1D-Web-Login-Entry / P0 | 登录入口历史切片（已被 R2f 正式入口替代） / Web唯一writer，Root审查/Git | Web `5e3b27af` 当时实现过自动连接/整页重试，但用户最终明确要求删除；当前固定 Web `9794a286` 已删除 `LoginPanel`、CSS、旧测试，`/login` 为服务端 OIDC 启动路由，真实凭据只在 IAM 表单提交。 | R2f 固定来源真 HTTPS Chromium `/login`→IAM凭据/tenant/consent→`/app` PASS；Node22 `pnpm check` contract54/architecture32/Vitest1401/lint/typecheck/build PASS。状态：旧设计已删除，R2f 代码与隔离浏览器已验收；3310 完整可见入口另归 R2e，未验收。 |
| W1D-Chat-A0 / P0 | Agent owner `createRun` 202 / `replaySessionEvents` 200 的 typed envelope 与真实序列化一致 / `agent_typed_contract_owner` 为 Agent 唯一writer，Root审查/Git | Agent main `741c928dfc11313a25064a905d77d4ad371f5534` clean；先核三设计文档、Python/API手册；只改 Agent 既有 OpenAPI operation/schema、生成/contract checker/对应测试/必要文档，不改 BFF/Web/Root | 复用已有 `LaunchReceipt`/`ReplayPage`，无泛型 `{}` 回退；wire字段、状态码、认证/幂等/replay语义不变；TDD、contract/ruff/pyright/pytest/build，Root在冻结树复验再提交、推送；BFF消费者等待 owner SHA。状态：已验收；Agent main `b8db4352b7bb39c853701335a7c329cc8752e7e5` 已推送，Root 复验 1097 passed/6 skipped、contract/Ruff/Pyright/build 均通过；非空 replay 与 nullable `chat_message_id` 已补测。 |
| W1D-Chat-A1 / P0 | Agent 真实执行对空文本最后segment仍发布权威 `message.completed {content:""}`，避免BFF保留工具前草稿 / Agent唯一writer，Root审查/Git | Agent `b8db4352`→main `520ec181a101298b4f336aad273ce003b2735955`，Root固定 owner gitlink/库存；既有 `MessageCompletedPayload.content: str` 允许空，修 emitter 抑制逻辑，不改机器契约字段/版本。 | 真实 DeepAgents v3 三段流经 enqueue/claim、lease/outbox、PG/Redis、HTTP replay；工具与空最终段序列、stale lease 零写入均验。Writer 全门 1101 passed/6 skipped，Root 独立聚焦 unit/contract 93 与隔离 PG/Redis acceptance 2、Pyright/Ruff/contract PASS，临时 schema/Redis DB14 均 0。状态：Agent owner 已验收；BFF 对空最终段的固定SHA跨仓消费待 B2/组合。 |
| W1D-Chat-B1 / P0 | Web 本地新会话首发消息在 BFF 同事务建立 Conversation、两条 Message、Agent outbox/expected-run；重试与越权不重复建 / BFF 唯一writer待派工，Root审查/Git | BFF main `1917f9097d08a38128ed5f4087c826356142c548` clean；与 A0 无字段/源码前后依赖，可分仓并行，B1 不消费新 Agent typed artifact；只改 BFF Chat public contract描述/相邻 admission→事务 repository→测试/三设计文档；不改 AG-UI 投影或其他仓 | Web `conv_*` 首发 `POST /v1/sessions/{id}/messages` 202；同一PG事务插入 owner-scoped conversation/user/pending assistant/outbox/consumer；existing-active继续，foreign/deleted ID与非法Project fail closed，不跨owner SQL；同 key重试原receipt，同 ID并发不多建；真实PG隔离测试、Node22全门、Root复验。状态：已验收；BFF main `17d28502912bafe3b1887a5fdff9ca55dcf1be15` 已推送，Root Node22 全门 261 passed/1 skipped 与独占真 PG/Redis 38/38 通过。 |
| W1D-Chat-B2 / P0 | Agent source→BFF durable AG-UI 时同步更新 assistant `bff_message`，reload snapshot仍可见最终正文 / `bff_first_message_owner` 为 BFF 唯一writer，Root审查/Git | BFF `17d2850`→main `8dedcb2510d8c5e3917561b9c979e7aa6ce8b8ae`，Root固定 gitlink/库存；Agent owner A1 `520ec181` 已发布空完成事件。 | source-event去重与Message同事务；run/tenant/owner/outbox绑定，旧run/失败outbox不复活；多段最终正文可为空，reload后 Message 与 AG-UI watermark 同快照，最新100条稳定升序。Root Node22 format/check/schema PASS：contract26、单元262 pass/1 skip，隔离PG/Redis integration42/42，临时库0、Redis DB15为0。状态：BFF owner 已验收；固定SHA真Web首发与Agent worker跨仓 smoke 仍待办。 |
| W1D-Chat-B3 / P0 | BFF 消费固定 Agent `createRun`/`replaySessionEvents` typed owner contract，删除对应手写 wire 判断 / `bff_first_message_owner` 为 BFF 唯一writer，Root审查/Git | Agent main `520ec181a101298b4f336aad273ce003b2735955` 的 `http_contract` v1.1.0 digest `2b9c7aad6f38db3e20200b037e4818ae932209ba3deecabf8fc984db6bcec492` 已发布；BFF main `8dedcb2510d8c5e3917561b9c979e7aa6ce8b8ae` 的 B2 已验收。下方放置门与切片已裁决；本仓单 writer，不与 Root 抢写。 | 固定 owner SHA/digest 与生成漂移门；launch/replay 成功和错误结构在 client 边界严格解析，保持服务身份、deadline、幂等、分页/连续性和响应上限，不把 generated 类型穿透业务；删除被替代手写 wire/fallback；Agent↔BFF 合同及真HTTP回归，Node22 owner全门。状态：BFF owner 已验收，当前固定 `84a560a`；Root 真 HTTP/独立 CLI worker 已验，Chromium 发消息仍归 R2c。 |
| W1D-Web-Message-Wire / P0 | Web首发消息按BFF public契约发送只含业务字段的JSON，Idempotency-Key只在header；删除同源adapter的旧body提升双轨 / `web_message_wire_owner`为Web唯一writer，Root审查/Git | Web main `3074f9bba7dc1f95737b41e50ae8ad152bb5e417`、clean；`src/engine/client.ts:169–176`发送`{idempotency_key,content}`，`src/app/api/session/[...path]/route.ts:80–94`只提升header不删body；BFF `message-create-input.ts:11–19,40–46`严格拒绝多余键，真Web首发当前必然400。固定BFF canonical请求，不改BFF放宽allowlist。与B2分仓独立写入，Root已停3310 Next dev避免共享构建目录争用。 | TDD先用Web同源端到端HTTP断言BFF收到body`{content}`且稳定Key header，重试仍复用同Key、不同内容不复用；移除旧body提升路径与对应测试/文档，保持CSRF、在线Product Bearer、body上限/取消与no-store；Node22 Web全门与固定SHA真首发HTTP，随后AG-UI/Agent worker组合。状态：Web切片已验收：main `42e7067cb5a6eff7beacc83e5845f987d1d5cde4` 已推送且clean，Root pin `a5e809ee`；独立审查3项P1/2项P2由同writer TDD修正，Root Node22 `pnpm check` contract54/architecture32/Vitest1392、lint/typecheck/build与Playwright11通过/1既有skip。固定SHA真Web首发与Agent worker仍待Root组合验收。 |
| W1D-Chat-P0 / P0 | 首条消息建立会话并让Agent结果成为持久可重载的assistant消息 / Agent契约owner→BFF实现→Web消费，逐仓唯一writer | 当前 Agent A0/A1 `520ec181`、BFF B1/B2 `8dedcb2`、Web首发wire `42e7067` 已逐仓发布；只读审计基线BFF`1917f90`/Agent`741c…`/Web`7c0ab98` 的两处源码缺口已修。 | 新会话Web首发202；BFF同事务Conversation+user+pending assistant+outbox，Agent真worker delta/completed/failed，AG-UI与Message同事务且reload一致；跨tenant/重复/崩溃负例、固定SHA真Web→BFF→Agent/PG/Redis smoke。状态：部分完成；B3 generated Agent client和固定SHA真worker smoke已验，Web全量 generated BFF consumer、真Chromium消息/SSE、私有负例与恢复仍待完成。 |
| W1C-DB / P1 | 单开发数据库的 owner-schema 代码适配 / 各数据owner逐仓唯一writer，Root按依赖集成 | 用户明确一个应用库/一套账号；BFF `a4dbc33…`与IAM `6bc9b19…` owner代码已发布，Root同库双schema installer已验；其他数据owner尚未完成 | 每仓连接只指向自己的schema、fresh install只检查本schema，统一同一物理应用库真HTTP组合；不建多角色/长期子库，测试临时资源仍归fixture。状态：BFF/IAM部分完成，Root组合及其余owner待办 |
| W1C-DB-BFF / P1 | BFF单应用库owner-schema首个代码切片 / `w1c_bff_logout_metadata_reader`唯一writer；Root审查/Git | BFF `cd1c2600…`→`06a478403c92eeede0c54ed1f53022f0ff60d79e` 已推送main且clean；29文件，canonical SQL/OpenAPI/package/lock未变 | 独立复审0 P1/P2；Root Node22 format/check 253/253、真实schema 6/6、独占PG+Redis integration 37/37通过，自有DB已删、Redis8 0→0。Root gitlink/库存已集成、真IAM准入/OIDC/Capability/Scheduler/System组合通过；全量catalog drift另片，不宣称所有owner单库完成；状态：BFF owner及Root组合已验收 |
| W1C-DB-BFF-ROOT / P1 | Root跨仓smoke消费BFF新schema / `w1c_oidc_runner_rereview`脚本writer；Root修复/审查/Git | Root `762e7a6…`上五runner、窄URL helper及测试候选；BFF pin更新`a4dbc333…`，仅Root文件，其他owner URL不变 | Root独立聚焦**152 passed/56 subtests**、Ruff/diff-check；审查0 P0/P1/P2。发现并修复`psql`不能使用含`?schema`应用URL：观测保留raw URL、BFF环境用owner URL，直接SQL限定`kokoro_bff`。Root `20112fcb…`已发布，首次Scheduler smoke暴露case SQL未限定owner schema，已定点修复并加回归；当前IAM准入7/7、OIDC15/15、Capability8/8、Scheduler11/11、System组合PASS，自有资源清理通过。状态：Root脚本/五条真组合已发布复验，Scheduler新SQL发布后11/11 |
| W1C-DB-IAM / P1 | IAM owner-schema适配前置只读设计审查 / `w1b3_iam_fixture_reader`；Root裁决 | IAM main `b838853a81ff34bd0f7a079ccc75ba6abd61d1ec` clean；只读postgres-url/Prisma/fresh installer/runtime/fixture/CI与三设计文档 | 已交17 Prisma model/public、整库installer、readiness与raw SQL差距及四项真实验收方案；不改文件/Git/资源。状态：只读审查已验收，不等于IAM代码可同库运行 |
| W1C-DB-IAM-D / P1 | IAM单库目标三文档门 / `w1b3_iam_fixture_reader`唯一writer；Root审查/Git | IAM `b838853a…`→`d415ddbe565ec6c09e432945624e72c4b86d7a78` 已提交推送main且clean；仅三设计文档 | 固定`kokoro_iam` schema、Prisma canonical、事务化DDL/owner-only fresh gate、运行查询/ready与四项真实测试；API wire无变化明确。Root独立审查diff与一致性、diff-check通过；文档仅目标态不冒称代码完成。状态：设计文档门已验收 |
| W1C-DB-IAM-C / P1 | IAM单库owner-schema代码实现 / `w1b3_iam_fixture_reader`唯一writer；Root审查/Git | IAM `d415ddbe565ec6c09e432945624e72c4b86d7a78`→`6bc9b190c359b8109238626ff689ce9839e858b5`，38文件已推送main且clean；canonical 17 models/固定URL/installer/readiness/fixture/CI文档，API wire未变 | 独立复审原3 P2定点修正后0 P0/P1/P2；Root Node24 Prisma validate、`pnpm verify` **704/704**、串行真实integration **183/183**，测试DB/Redis1 0→0；Root同库双schema install已真测BFF16表+IAM17表/0FK。默认并行integration旧全局库存fixture竞态仍须单独修，不归因应用并发；Root gitlink/库存已集成、真IAM→BFF准入7/7及OIDC15/15通过；同库installer证据不等于所有owner应用同库运行。状态：IAM owner及当前组合已验收 |
| W1C-DB-IAM-BFF-PIN / P1 | BFF relay政策来源机械re-pin / `w1c_bff_logout_metadata_reader`唯一writer；Root审查/Git | BFF `06a4784…`→`a4dbc3339448c7ee8763b0f82d1c0ae4c213bf87` main clean；IAM `6bc9b19…`；Web `14e23e6…`已复制BFF policy原字节 | IAM allowlist/snapshot blob digest不变；BFF/Web policy同为SHA-256 `ba1e63083b4b2ed0f3eb42308e632bc502cb4f07fcb99a2ea04586f7faa123ad`。Root独立BFF Node22 contract25/标准253/真实schema6与Web contract52；Root gitlink/库存与跨仓真HTTP已集成复验。状态：owner来源pin及Root组合已验收 |
| W1C-3 / P0 | Root Web→BFF→IAM真实登录组合 / Root主控+独立审查 | W1C-1与W1C-2 release | 登录/刷新/退出/失效/越权真HTTP、隔离资源清理与固定SHA/digest；本片不激活尚缺Product generated consumer的EDGE-WEB-BFF；状态：待派工 |
| W1D / P0 | Web全量BFF Product OpenAPI generated consumer与单一AG-UI接线 / Web唯一writer；Root集成 | W1C-3；BFF public contract先冻结 | Web消除手写Product wire和旧网络协议，固定BFF contract/generator/runtime/digest；Web owner全门+组合验证后才激活EDGE-WEB-BFF；状态：待派工 |

W1C-1设计门通过报告（实施前，不等于代码验收）：当前BFF commit `6238599667110fbfbc2d5ef3a9d53731f2623cfe`，唯一writer修改 `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff/docs/TECHNICAL_DESIGN.md`、`/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff/docs/API_CONTRACT.md`、`/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-bff/docs/DATA_MODEL.md`；独立设计审查初轮1 P1/2 P2均修正复核0 P1/P2。未决项为IAM既有`/iam/error`未开放（仅错误分支，不以BFF通配修补）、Web OAuth client注册URI与Auth.js版本/`resource`行为待W1C-2实测。本阶段`git diff --check`通过；机器`pnpm contract:check`、`pnpm schema:check`与真实IAM HTTP尚未运行，留W1C-1实现门。批准写入仅限BFF现有三文档，新增`src/http/routes/iam-protocol-relay.{policy,transport}.ts`与`src/http/routes/iam-protocol-relay.ts`、`contract/iam-relay-policy.json`、`scripts/generate-iam-relay-policy.mjs`、对应`test/iam-protocol-relay-{policy,transport}.test.ts`及独立真实HTTP integration fixture/test、`src/bootstrap/server.ts`、`src/config/runtime.ts`、`package.json`脚本、必要`docs/CURRENT.md`/`contract/README.md`。首轮全量test **241/242**，唯一失败为旧`test/contract-governance.test.mjs`精确断言`contract:check`脚本字符串；Root批准仅调整此测试的对应断言，将新`contract:check:iam-relay`纳入机器门，其他断言不放宽。`database/schema.sql`、IAM/Web、lockfile、BFF public OpenAPI禁止改；如确需越界先报Root。Root保留Git index/提交/推送/gitlink；BFF writer先RED→GREEN并交文件清单、命令与真实结果。

W1C-1代码首轮审查（未验收）：writer报告Node22标准242/242、contract25/25、architecture27/27、schema4/4及真实IAM基础HTTP1/1；Root只读代码/测试，独立审查定级1 P1/3 P2：真实IAM正向Code+PKCE/三页签名续接/logout未证明，超限上游未取消、入站slow body无截止、policy provenance仅硬编码自比。writer按RED→GREEN修两个超时/取消P2；本地复制IAM私有源码的vendor方案虽可做SHA门，但独立审查指出跨owner源码耦合。Root最终裁决**撤销该vendor扩围**：BFF只维护自身policy→JSON字节漂移，IAM固定来源/route子集由W1C-1G Root gitlink commit-blob机器门验证。P1需IAM test-owned fixture/Root真实组合，不让BFF复制IAM源码或把错误authorize/token当成功。未复验前W1C-1保持进行中。

W1C-1G放置门与替代裁决：Root拥有跨仓gitlink与证据，不拥有IAM OAuth字段或BFF policy。候选A在Root既有`verification/contracts`机器门旁新增窄`iam_relay_policy`治理helper/CLI/测试（采用，只用于跨repo固定blob核验，不变更16-edge公共API矩阵）；候选B由BFF vendor并动态import IAM私有`src`（淘汰，破坏只消费owner contract的边界）。IAM `auth-routes.constants.ts`与Better Auth snapshot只由Root `git --no-replace-objects show`固定IAM gitlink commit读取做来源证据，BFF单一TS policy派生只读JSON，Root固定BFF gitlink读取JSON并核对声明SHA及IAM route/method子集/disabled。新增Root `scripts/governance/iam_relay_policy.py`、`scripts/verify-iam-relay-policy.py`、`scripts/tests/test_iam_relay_policy.py`，不建新顶层目录，不修改业务schema/contract；失败应保持非零而不访问基础设施。Owner/依赖方向与允许文件集已确定；Root writer只写三脚本，Root主控负责task/progress/index/Git/集成。实际BFF/IAM gitlink发布前预期真实CLI红，不冒称闭环。

W1C-1H放置门：Root只拥有跨仓真实HTTP验收，IAM拥有OAuth用户/client/session/consent事实，BFF拥有relay策略与传输。当前IAM test-owned `web-oidc-flow-host.ts`提供独立临时资源和NDJSON凭据，Root已有`run_bff_iam_session_smoke.py`进程/清理模式；Web交互页源码尚未实现，因此本门仅验证IAM→BFF native协议，不冒充Web浏览器E2E。候选A在Root现有`scripts/e2e/`新增窄`run_bff_iam_oidc_smoke.py`及`scripts/tests/test_bff_iam_oidc_smoke.py`（采用：OAuth链和原admission七用例各有变化原因）；候选B把OAuth步骤塞入原session smoke（淘汰：同一文件混合两条独立协议状态机）；候选C在BFF复制IAM测试fixture（淘汰：跨owner事实与临时资源被consumer持有）。新增两个文件、不新建顶层目录；只通过IAM host管道及HTTP访问owner，不import子仓源码、不写IAM SQL，不新增生产schema/contract/generated或长期数据库。按精确gitlink验证进程，复用现有PG/Redis实例但只建/删fixture自有临时资源；先TDD验证协议/错误/清理，再实跑首次Code+S256/tenant/consent/token/userinfo/logout，明确已证明与Web待证明的边界。Root保留Git index/提交与最终组合验收。

W1C-1I放置门：IAM拥有测试fixture中的issuer和退出验收入口，Root/BFF不应替IAM伪造JWKS事实。当前`test/consumer/auth-flow.consumer.test.ts`已在测试范围对精确issuer `/jwks`映射本地fixture fetch，生产API/Schema未改；真实跨仓host目前只声明不可回环的公网HTTPS issuer，故带hint logout报`invalid_token`。候选A仅在现有`web-oidc-flow-host.ts`建立同一精确URL的测试私有fetch映射，配套现有host integration测试并在stop/EOF/启动失败恢复（采用，复用owner已有模式）；候选B在Root runner捕获401后跳过logout（淘汰，假PASS）；候选C更改生产issuer/JWKS行为或部署DNS（淘汰，扩大边界）。只有该host进程内部可命中，其他URL仍走原fetch；不新增数据库/契约/generated、不改生产代码；聚焦Node24测试必须覆盖映射和资源/全局状态清理，Root再用真实BFF relay链验证。IAM当前单仓临时DB/Redis由fixture自行清理，不触碰预存应用数据。

W1C-2A放置门与任务卡：owner `kokoro-app` Web同源adapter，唯一writer为Web源码Agent；基线Web main `38683a5…` clean，Root `a5ca0afc…`原锁BFF `1fae01e3…`/IAM `c9a27721…`，本片最终目标重pin BFF `cd1c260…`/IAM `b838853…`，无原有未提交Web变更。当前`src/lib/server/upstream-http.ts`会合并多个`Set-Cookie`，不能作为原生IAM中继；Web尚无`/iam` route，旧IAM直连/旧Product会话保持原样但不在本片扩写。候选A把BFF固定policy只读快照放现有`src/generated/`，校验与来源manifest归`src/lib/server/`、原生HTTP传输与路由分别归`src/lib/server/`和`src/app/iam/[...path]/`（采用：已有server/route/generated边界，变化原因独立）；候选B复用`upstream-http.ts`（淘汰：多cookie语义丢失）；候选C把policy放Web `contract/`当可编辑owner契约（淘汰：BFF才是来源owner）。允许Web只改`src/generated/iam-relay-policy.json`、相关只读provenance、`src/lib/server/iam-relay-*.ts`、`src/app/iam/[...path]/route.ts`、对应`tests/server|app|contract|system`聚焦测试及`docs/CURRENT.md`/`INDEX.md`；另批准对既有`docs/TECHNICAL_DESIGN.md`、`docs/API_CONTRACT.md`、`docs/DATA_MODEL.md`的来源SHA/digest与确实变化的当前状态作精确修正，并同步相邻`src/lib/server/INDEX.md`。独立审查后追加允许全部5份`.env*.example`的固定server-only Web origin配置、`docs/RUNBOOK.md`/`docs/SECURITY.md`/`docs/deployment.md`准确安全边界及新`tests/system/iam-relay-next-http.integration.test.ts`；不能从请求Host推导可信origin。该真实Next测试必须隔离构建输出和自有资源、启动失败可清理；若Next/Fetch入站raw target/body在路由前已规范化，测试与文档必须诚实记录可见边界、不得以伪造`request.url`充当真实HTTP证明，也不为此新建运维系统。新增文件仅因policy/protocol/route的不同变化原因，不新建顶层业务模块/数据库。唯一依赖是固定BFF policy字节，禁止Web直连IAM、子仓源码相对import或绕过BFF；GET只开放明确无mutation且无需浏览器Bearer的policy交集，userinfo/end-session与所有browser POST先拒绝，不能把后续CSRF/RP计划伪装成已实现。入站只透传符合IAM快照的issuer cookie，出站保持合法多Set-Cookie、status/Location、安全头，不透传Product/Auth.js cookie、浏览器Authorization/secret；服务身份由Web server提供，路径/编码/大小/超时/取消fail closed。无SQL、事务、generated client或Product API改动，旧IAM直连由后续Auth.js切片删除，不建立第二套兼容代理。验收Web Node22`pnpm lint/typecheck/test/build`、policy digest与BFF固定commit blob对照、unknown/POST/cookie/header/多Set-Cookie/超限/断连负例；Root审查后由Root串行提交，真实浏览器登录仍留W1C-2B/3。

W1B-3A放置门：owner为IAM test fixture、唯一writer为本片负责人；当前已有`test/fixtures/internal-http-application.ts`真实PKCE/Nest/独占资源，Root无IAM SQL写入权。候选A在IAM `test/fixtures/`扩展本地管道入口及`test/integration/`验证（采用：只变化测试联调生命周期），候选B在Root `scripts/e2e/`复制身份引导/数据库操作（淘汰：跨owner事实与双实现）。新增host与聚焦测试两个文件而非模块；只依赖既有fixture，不引入生产服务/跨仓源码import。测试删除Member仅作用于fixture自建IAM库并显式标注失效注入；无Schema/HTTP contract/generated变化、无旧路径需保留。验证为Node24脚本启动/命令/清理、IAM typecheck与聚焦integration、Root经真实HTTP观察BFF状态和拒绝后无业务副作用。

W1B-3B放置门：Root拥有跨仓行为验收，不持有IAM/BFF业务事实。当前三条旧smoke的认证fixture还依赖旧header；本片先独立做真实IAM组合，不让替身smoke冒充激活证据。候选A在Root既有`scripts/e2e/`增加窄组合runner与`scripts/tests/`行为测试（采用，代码地图已有Root组合职责），候选B塞入BFF或IAM单仓测试（淘汰，不能证明两个真实HTTP owner运行组合）。两个文件不建新模块；Root只通过IAM host管道和HTTP通信、只对自有BFF DB安装schema和读断言，不导入子仓源码或直接访问IAM数据库。无生产Schema/contract/generated变化、无双轨兼容；先验证实际联调，再按固定vendor/consumer版本决定checkpoint edge与既有smoke更新。

W1B-3C放置门：Root拥有组合smoke；现有Capability/Scheduler runner共用BFF `c5e9…` pin和自报header身份，BFF `623859…`已改为在线IAM准入，原8/11行为需要保持。候选A在Root `scripts/e2e/`放一个窄的受信IAM admission stub并由两个现有runner复用（采用：一处wire契约、分别保留其owner集成）；候选B在每条runner复制HTTP stub（淘汰：协议漂移与重复生命周期），候选C让两个owner smoke均启动真实IAM（淘汰：本片已由W1B-3B单独证明真实IAM，重复基础设施使旧owner回归成本过高）。新增共享fixture文件与聚焦测试，不建新顶层目录；只映射固定token到测试tenant/user，严格回`data.allowed/tenant_id/user_id/session_id/client_id`、`Cache-Control:no-store`和合法request-id，关闭线程；不读取IAM SQL、不变更业务contract/schema/generated、不保留旧header身份兼容。Capability/Scheduler原case与回调独立服务凭据不删。本机hostname只解析RFC1918而不解析loopback，旧callback固定loopback前置不符当前本机事实；采用在已有`scheduler_bff_smoke_http.py`仅让response-drop proxy绑定hostname解析且本机拥有的单一精确私有IPv4、/32 allowlist，原BFF upstream仍loopback，拒绝公网、多私有地址歧义与0.0.0.0，不改系统hosts或共享网络。验证为聚焦pytest、真实8/11及自有DB/Redis/进程清理。System旧smoke另列W1B-3D串行修复。

W1B-3D放置门：Root已有`scripts/e2e/run_system_owner_smoke.py`及聚焦测试，但其`ROOT/owner`路径和BFF旧header身份与当前九仓gitlink/BFF IAM准入相冲突。候选A只扩展现有System runner与现有测试，复用3C已提交共享wire stub（采用，System/BFF/Agent进程及资源生命周期已有位置）；候选B在System子仓复制Root组合测试（淘汰，跨仓进程/资源不属于System owner），候选C再建新runner（淘汰，双轨）。不新建目录/Schema/contract或第二IAM fixture。System owner admin/BFF/Agent服务凭据保持独立；仅BFF普通models请求使用测试IAM固定token身份，runtime manifest维持受信server tenant而非header自报；错误/空结果及发布release断言不删。验证聚焦pytest、真实独占PG/Redis HTTP组合、全部自有资源清理与Root集成门。

W1B-3E放置门：Root是跨仓依赖清单、checkpoint与当前组合文档owner；IAM拥有0.2.0机器contract，BFF拥有vendor/generated client与runtime，不在Root复制可编辑业务contract。当前IAM/BFF各自main及真实7case、Capability8/Scheduler11/System组合均已通过，inventory仍将唯一待激活的`EDGE-BFF-IAM`标broken。候选A增加`verification/contracts/checkpoints/w1b-iam.json`并在现有inventory更新该edge（采用，历史w0b-exit保持不变）；候选B直接改w0b-exit历史checkpoint（淘汰，会篡改已验收基线）；候选C新建跨仓contract目录（淘汰，违反owner唯一事实源）。新增一个checkpoint文件而不建目录/模块；只读取owner commit blob及consumer commit blob固定digest/version，禁止引用脏工作树或另仓相对import。无数据库/运行API变更，不保留旧broken reason；`docs/CURRENT.md`和`scripts/INDEX.md`同步真实当前态。验证compatibility期望只余11个broken+1非法、w1b-iam精确checkpoint、focused与全Root测试、真实smoke证据、topology/main-only及最终提交后清洁状态。

## W1D-Chat-B3：Agent HTTP typed consumer 放置门与执行切片

| 项 | 已裁决方案 |
| --- | --- |
| Owner | Agent 独占 `contract/openapi/v1/openapi.json`；BFF 只拥有本仓出站 adapter 与不可编辑的固定来源生成物。BFF `8dedcb2510d8c5e3917561b9c979e7aa6ce8b8ae` 是单一写入基线；Agent `520ec181a101298b4f336aad273ce003b2735955` 的 HTTP v1.1.0 digest 为 `2b9c7aad6f38db3e20200b037e4818ae932209ba3deecabf8fc984db6bcec492`。 |
| 当前事实 | `outbox-delivery.ts` 接受任意 2xx、仅检查 receipt 的 run/session；`projector-source.ts` 经通用 `normalizeUpstreamResponse`/`dataOf` 接受裸数据回退；`projection.ts` 手验 replay event 且未要求 owner 必需的 `source_index`。但 BFF 自有的顺序连续性、source 去重、lease/fence、退避与有界 transport 是真实业务不变量。现有 IAM/Capability/Scheduler vendor+generator 链可复用；`contract/external/kokoro-agent/control-receipt.v1.json` 属于另一条 control wire。BFF `docs/CURRENT.md` 的 B2“工作树候选”描述已过期，必须同片修正。 |
| 目标职责与目录比较 | 采用 `contract/vendor/kokoro-agent/<固定SHA>/` + `contract/dependencies/agent-http.json` 固定完整 owner blob/版本/digest，`openapi-ts.agent.config.ts` 仅筛 `createRun` 和 `replaySessionEvents`，`scripts/generate-agent-http-client.mjs` 双次确定性生成/漂移校验，`src/generated/agent-http/` 只放生成物，`src/infrastructure/clients/agent/` 承接窄解析/映射。淘汰继续在 `contract/external/` 手抄两条 schema 与 receipt/page parser：会形成第二份可漂移字段来源；也淘汰放在 `src/application` 或 Root 的生成物：错误 owner/依赖方向。 |
| 粒度与依赖 | 生成配置、manifest、vendor、脚本是既有 generator 模式的一个自洽来源单元，不建新一级业务模块/进程。只允许 Agent 出站 adapter import generated Zod/type；Application port 继续用本地 `AgentDispatchDeliveryResult`/`AgUiSourcePage`，不泄漏 wire/SDK/HTTP Request。Node22、pnpm11.25.0、@hey-api/openapi-ts0.99.0、Zod4.5.4 使用本仓现有固定版本，不顺手升级。 |
| 数据/API/删除 | 不改 Agent owner contract、BFF public OpenAPI、PG schema/事务、Redis namespace。`createRun` 仅 202 + 完整 `LaunchReceiptEnvelope` 成功，`replaySessionEvents` 仅 200 + 完整 `ReplayPageEnvelope` 成功；拒绝缺失/未知字段、错误 `meta`、错误 `source_index`/enum/时间值，空 `assistant.completed.content` 合法。删两条调用上的宽松 `acceptedReceipt`、手写 wire 形状判定与 `normalizeUpstreamResponse` 裸 body 回退；保留 BFF 的 expected run/subject/outbox、seq 连续性/gap、有限重试/lease 截止、服务身份、request cancellation/timeout/响应上限/错误脱敏。Agent 运行时 replay 400 与机器契约未列、x-request-id/error retryable 规范差异另列 owner 契约缺口；BFF 对未声明状态 fail closed，不自行发明 owner wire。 |
| 验证 | 先 TDD：固定 owner digest/operation filter/生成两次相同与篡改失败；202 receipt/replay 200 的完整 envelope、缺 `meta`/`replayed`/`source_index`、未知字段、204/裸数据/恶意错误码、空最终内容与 gap/retry/lease 负向；然后 Node22 `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm contract:check`, `pnpm test`, `pnpm test:architecture`, `pnpm build`, 独占临时 PG+Redis `pnpm test:integration`，Root 真 Agent HTTP 组合复验。现有对裸 page 的纯 `classifyAgentEventPage` 单测可保留为 BFF 连续性测试，transport 入口必须强制 envelope。 |

执行顺序：①更正 BFF CURRENT 的 B2 已发布事实并固定 Agent provenance；②在本仓已有 generator 约定下写失败的 drift/strict-wire 测试；③生成两条 operation 的只读类型/运行时 schema，接入既有有界 transport；④删除两条运行调用的旧宽松 wire 回退，保留业务连续性与调度语义；⑤完整 BFF 门和真实隔离集成；⑥Root审查、提交、pin 后做固定SHA跨仓 HTTP/worker smoke。BFF 单一 writer 只交文件与证据，Root 独占 Git index/commit。

## W1D-Web-Login-Entry：单租户登录过渡页收敛

用户已批准：访问 `/login` 直接启动固定 IAM OIDC 流程；页面只保留 Kokoro 品牌、连接中状态和失败重试，移除营销导航与多余的中转按钮。此改动只改变 Web 登录入口交互，不改变 IAM 凭据/租户事实、Web RP/Session/OIDC contract 或 Product API。

| 项 | 放置与边界 |
| --- | --- |
| Owner/当前事实 | `kokoro-app` 拥有 `src/app/login/page.tsx` 和 `src/ui/auth/login-panel.tsx`/CSS；当前 `LoginPanel` 复用 `MarketingTopBar` 并等待用户点一次同义按钮，随后 `beginProductSignIn` 才发 CSRF+固定 provider POST。`/login` 已独立于 System manifest；3310 只有 Web 时 CSRF 为503 `rp_unavailable`，不能伪造在线登录。 |
| 位置比较/粒度 | 采用修改现有 `login-panel.tsx`/CSS 与相邻 i18n、UI/E2E 测试，不建路由或新组件；淘汰把自动跳转放在全局 middleware（会误触发首页/循环）和直接把账号表单放 Web（复制 IAM 凭据边界）。保留 `/login` 作为可重试的明确入口。 |
| 目标交互 | 首次挂载启动一次固定 Product OIDC；启动时 `role=status` 说明正在连接，不显示重复 CTA；CSRF/发起失败后停在同页 `role=alert` 且按钮显式重试，**不自动循环**。重复 effect/双击均只发起一次；成功提交后保持跳转状态。品牌入口只回 `/`，不带营销能力/FAQ/另一个登录链接。 |
| 依赖/数据/删除 | 只调用既有 `beginProductSignIn`，不请求 System，不将 tenant/client/secret 放浏览器，不放宽 Host/Origin/CSRF。删除登录页营销顶栏依赖、旧大按钮中转状态/样式；复用既有 token 与无障碍语义，移动端/暗色正常。服务未启动仍按真实错误显示；这个 UI 切片不能算当前3310在线登录闭环。 |
| 验证 | 先改失败测试：首次自动发起一次、严格模式重复 effect 无重复、失败停止并重试一次、无营销导航/无 System 请求；更新浏览器 E2E 对固定 CSRF/provider POST 的断言；Node22 Web format/lint/typecheck/contract/unit/build 与 desktop/mobile Playwright；Root 浏览器验 `/login` 首屏及实际3310诚实故障态。固定SHA真 HTTPS 登录 smoke 另验，不能把 mock CSRF E2E 当真 IAM。 |

独立审查补充放置门：原范围漏掉两处真实用户路径。候选A在 Web 既有 `product-auth-client.ts` 给自动 CSRF 尝试加取消信号，并在现有 RP `signin` 分支将浏览器HTML表单失败303回固定 `/login?auth=sign_in_failed`（采用）；正常配置时保留 canonical Host/Origin/credential 检查，配置完全缺席时也只回固定站内失败页。候选B把安全敏感的OIDC表单改为fetch/manual redirect（淘汰，浏览器 opaque redirect/Set-Cookie 语义不可靠）；候选C忽略POST失败继续展示JSON（淘汰，不满足失败重试）。`/login` 现有 page 只把精确失败 query 转成初始错误态，不自动循环；API客户端/JSON请求的原错误 status/envelope 不变，成功302与OIDC状态/cookie不变。另在既有CSS停 reduced-motion 旋转、失败标题、现有Web tests 增加离页迟到CSRF与浏览器POST失败导航。仅扩展既有 Web auth 文件职责，不建新模块/数据/API/兼容入口；Web单writer已交付候选并停写，Root接管审查定点修复。

W1D-B3 状态：BFF `9b8c7af6383541cf8ffcaa66c8cffdddaeae9864` 已发布 main；Root 修复独立审查指出的 `source_index`/负例后复验 Node22 静态门、标准测试267通过/1跳过、真实隔离PG+Redis integration 42/42。BFF owner切片已验收；固定SHA真Agent worker组合仍待执行。Root将此 SHA 与Web登录入口一起pin。

W1D-Web-Login-Entry 状态：Web `5e3b27af4ddfd1a1cd37287e702ea51d271298f4` 已发布 main；Root先补5项RED，再修取消/浏览器POST失败/失败态，聚焦20/20，完整 `pnpm check` contract54、architecture32、unit1396、lint/typecheck/build通过，Playwright桌面+移动13通过/1既有skip。固定SHA真HTTPS登录组合、3310实测与Root pin放行仍待后续验收。

W1D-Web-Login-Entry / B3 Root集成：Root `ded9efd5ce87f795529e4191204197575d05a02d` 已pin Web/BFF，真实独占HTTPS Product Session smoke与 Root 603/130、checkpoint/topology/policy均通过。旧runner对浏览器错误状态的断言已按固定303重试页修正；3310仅Web进程，在线联调仍为后续开发切片，不将隔离smoke冒充常驻服务。下一优先级仍是真Web首消息→BFF→Agent worker→durable assistant reload的固定SHA组合，支付后置。

## W1D-Chat-R：固定 SHA 真实 worker 组合门（2026-09-24）

只读 Agent 与 Web/BFF 双审已核对当前 Agent `520ec181`、BFF `9b8c7af`、Web `5e3b27a` 的实际代码链：首发、outbox、Agent HTTP/worker、PG chat replay、BFF AG-UI/Message snapshot 各有 owner 实现，但此前所有组合证据停在登录+Chat 列表，尚无真实 CLI worker 跑完首条消息。System 旧 runner 只验证 route，不执行 inference；不能代替本门。先交付 R0 来源复验与 R1 真 worker 首消息，再接 R2 Web/IAM 浏览器重载和 R3 真实网关/provider；这只是依赖顺序，不缩小最终完成条件。

| 项 | 裁决 |
| --- | --- |
| Owner / 当前事实 | Root 只拥有跨仓 runner、其测试和组合证据；BFF/Agent/System/Web 各自继续独占 API、业务与 SQL。Root 现有 `scripts/e2e/run_system_owner_smoke.py` 固定旧 BFF/Agent SHA，`seed_control_plane` 用唯一 `chat.<run_id>`，实际 Agent catalog 只接受 `chat`；该脚本明确 `inference:not-executed`。Agent requests stream/group 名固定，需独占 Redis logical DB，随机 run ID/Schema 不足以隔离。 |
| R0 / 已执行 | 将既有 System smoke 精确 re-pin 当前 BFF/Agent，给 seed 新增仅允许 `chat` 的可选参数，默认旧唯一 key 不变；保持原脚本仍只证明模型 route。测试先 RED、修后绿；当前固定 System/BFF/Agent 的真实 HTTP smoke `status=PASS`、`inference:not-executed`、自有资源已清理。 |
| 目录比较 | R1 采用新 `scripts/e2e/run_bff_agent_worker_smoke.py` 和同名 `scripts/tests/test_*.py`：保持 System catalog read smoke、登录 session smoke 和 worker execution smoke 各自单一职责，复用现有 Root 进程/PG/Redis/身份 fixture。淘汰把执行断言硬塞进 System smoke（会把 read-only route 伪装 inference），也淘汰复用旧 `run_stage2_owner_health.py`（共享状态不隔离）。不建立跨仓源码/Schema/contract 目录。 |
| R1 目标与边界 | 固定 Root gitlink/child SHA/clean；复用本地一个 PostgreSQL 实例、一套账号，为测试创建自有临时数据库并用 `kokoro_bff`/`kokoro_agent` owner schema；Agent HTTP+独立 CLI worker 共用它。BFF 使用 IAM test-owned admission stub。System route 与 OpenAI-compatible模型网关为**测试专属确定性 HTTP fixture**，worker仍走生产 `SystemModelClient`、`AgentFactory`、DeepAgents、PG lease/outbox/Redis、Agent HTTP，禁止生产 fake 开关或伪造 Agent 202/replay。Agent Redis URL 必须指向运行前为空的独占 logical DB；BFF Redis另用隔离 namespace/DB。预设有限 token/run/time budget、无外部 provider 费用。 |
| R1 断言 / 清理 | BFF 首条 `conv_*` 202→同 key/同 body receipt重放、同 key/改 body 409；确认 BFF PG conversation/user/pending assistant/outbox，Agent PG dispatch/terminal/chat_event，BFF 从真 Agent GET replay 后 AG-UI frame、watermark 与 completed assistant Message 同快照；重开 snapshot 唯一且 BFF/Agent 身份边界负例。所有子进程只杀本 runner 进程组，临时库只删自建，Redis只删/核对本 run 所有键，不重启/清空共享实例。CLI/API/SQL/日志当前事实必须记录；失败保留安全日志摘要但不泄凭据。 |
| R2/R3 后续 | R2 把同一执行链接上真实 IAM Product Session、Web HTTPS 页面首发、断流重连与刷新，并覆盖同 tenant B/跨 tenant C 私有负例；另切 HITL 多项审批。R3 经真实 System 发布 route 与已配置 LiteLLM/真实 provider 证明 inference，不能将 R1 fixture 称为真实 provider。上线 Billing 最后。 |
| 验证 | R1 先写来源、exclusive Redis、cleanup、错误路径单测 RED；完整 Root `pytest scripts/tests`、topology/checkpoint/policy/main-only；真正启动本地自有 PG/Redis 数据与 BFF/Agent HTTP/worker进程，逐条记录 status/证据/自有资源0；由 Root复跑、审查并提交。 |

任务卡：`W1D-Chat-R1` / P0，执行者为 Root 原生子 Agent（唯一 Root 脚本 writer），Root 保留 `docs/task.md`、`docs/progress.md`、Root Git index/commit/推送与最终验证；基线 Root `bab11bc283adb47b5fecf2fd35d97a64d96da082`，BFF `9b8c7af…`、Agent `520ec181…`。允许写 `scripts/e2e/run_bff_agent_worker_smoke.py`、`scripts/tests/test_bff_agent_worker_smoke.py` 及确需复用的 Root runner helper；不得改子仓、inventory、lockfile、共享脚本入口或启动非自有服务。前置 R0 精确固定与 fixture 边界已裁决；发现越界/真实 provider 需求先报告。交付包括文件清单、测试命令与实际结果、资源/进程证明、未覆盖场景；Root 提交并集成验收。

R1 来源更新（2026-09-24）：上述基线 SHA 保留为任务派发历史，不再作为执行输入。IAM 文档状态修正触发严格 provenance 级联：IAM `b35a9a5301219654ea344c03407fd355f58c481e`、BFF `84a560abeac5b7a63f32d7064abdde849ab33cf9`、Web `210ddfdd77f24143a0ed0617e2ecf1089bcf513c` 已分别由 owner 发布；Agent 仍为 `520ec181a101298b4f336aad273ce003b2735955`。R1 runner 必须按 Root 新发布 gitlink 核验这些精确来源；此更新只改变 policy/source digest，不扩大 R1 边界，也不把 IAM 文档提交当作 IAM 行为变更。

R1 集成状态（2026-09-24）：Root `b55379884aa3152be0d5e7bfd04b67f745763297` 已固定上述来源。真 BFF HTTP→Agent HTTP→独立 CLI worker→System/模型严格 fixture 已由 Root 独立复跑 PASS，Agent 4 个执行/聊天事件、BFF 4 source/5 AG-UI frame、assistant completed、重载与幂等/同 tenant 他人隔离均已验证；测试自有 PG 库、Redis14/15 与进程最终为 0。独立审查 P1 Redis ownership 竞态、P2 ignored `.env` 注入已在原两文件 TDD 修复并复审为 0 P0/P1/P2；Root `scripts/tests` 621 passed/130 subtests。R1 不证明真实 provider、Web/IAM 浏览器首发、跨 tenant 或 HITL；R2/R3 仍是独立待办。

## W1D-Chat-R2：真实浏览器、IAM 与 worker 同链（2026-09-24 设计门）

当前 Root `34c71d902a1fe99c07130bc29d030d4d5642cca5`、Web `210ddfdd`、BFF `84a560a`、IAM `b35a9a5`、Agent `520ec181` 均 clean；登录 HTTP smoke 的 `run_browser()` 实际是 Python CookieJar，不执行 Web JavaScript；R1 的 IAM 是 admission stub。两个绿门相加不等于真实页面首发。生产 Web `/api/session`→BFF→Agent 路径已有，无需先造新业务 API。R2 是把原有 owner 一次置于同一受控链，再由真浏览器验证。

| 切片 | Owner、位置与取舍 | 完成条件 |
| --- | --- | --- |
| R2a / Root 原有门修正 | Root `run_bff_agent_worker_smoke.py` 唯一writer；拆 `_final_sql_evidence` 为 BFF/Agent 各自独立 SQL，再在 Python 合并。淘汰同一查询跨两个 owner schema；只改原 runner/测试，不改业务库。 | 架构负例防跨 owner SQL、聚焦测试与真worker回归、独占资源0、Root复审。 |
| R2b / 一用户真实组合 | Root 在 `scripts/e2e/` 新建窄 Web+IAM+BFF+Agent worker runner/测试；复用现有 IAM test-owned host、隔离 Next/TLS、R1 原子 Redis ownership、真实 Agent HTTP/CLI 与严格 System/模型 HTTP fixture。淘汰在登录 runner 内增加 worker 状态机、也淘汰复制 R1 整个 runner；仅提取有第二调用者且可独立验收的 helper。 | 一个真实 IAM 用户在 Web Product Session 下首发，BFF/Agent owner-local PG、AG-UI、重载、幂等及资源0；不把 Python CookieJar 称为浏览器。 |
| R2c / Chromium 页面 | Root 窄 `.mjs` 浏览器驱动消费 Web 自有 Playwright 依赖，浏览器专属 hostname→127.0.0.1 映射和自签测试TLS信任；测试 origin/实际 TLS port/Host 完全一致。需要 TLS proxy 有界 SSE 透传，不能用现有全量缓冲代理证明流或断线。淘汰 mock cookie、`route.fulfill`、伪造 Agent 帧。 | `/login`→IAM sign-in/tenant/consent→`/app` 真DOM composer→202/AG-UI→刷新后恰好一对持久消息；受控断流后 `Last-Event-ID` 恢复且无重复。 |
| R2d / 私有矩阵 | IAM test fixture owner 才能添加 A/B/C真实账号及同/跨 tenant membership，优先扩展既有 host+integration test，不让 Root 写 IAM SQL；Root 消费 owner 发布的 test-only fixture，BFF/Agent不改身份模型。 | B 同tenant、C跨tenant 都以自己的真实Product Session拿不到 A 的列表/snapshot/events/mutation，且BFF/Agent无副作用；固定IAM SHA及来源级联复验。 |
| R2e / 当前可见预览 | `3310` 当前只起 Web，`oidcRpConfig()` 因缺 RP server-only env 为 null，`/api/auth/csrf` 503，所以自动登录落失败页；这不是单租户未配置。测试 IAM host强制HTTPS且拒绝IP/localhost，不能把 `http://127.0.0.1:3310` 假称为可用RP。优先复用R2真实HTTPS编排做有界、可停止的本地可见入口，不改生产Host/Origin/cookie校验、不造假登录。 | 交付实际可打开的HTTPS地址和明确启动/停止/清理流程；用户浏览器能进入真实IAM登录页。若仅运行Web，页面仍应诚实反映依赖未就绪。 |

R2d-IAM-ABC 当前任务卡（2026-09-24）：IAM `main` 基线 `b35a9a5301219654ea344c03407fd355f58c481e`、工作树 clean；IAM 唯一 writer 仅修改 `test/fixtures/auth-database.ts`、`test/fixtures/internal-http-application.ts`、`test/fixtures/web-oidc-flow-host.ts`、`test/integration/web-oidc-flow-host.test.ts` 及必要相邻测试，不改生产 `src/`、canonical schema、OpenAPI/SDK、锁文件、Root 或其他仓，不碰用户3310。Owner 是 IAM 测试 fixture 的真实 Identity/Tenant/Member 事实；A 现有 owner、B 同 tenant 非 owner、C 独立 tenant，不借 Root 直接写 IAM SQL。现有 host 已提供 A 的测试账号和 OIDC client；目标是显式一次性测试协议交付 B/C 凭据与 tenant，保留原 ready 事实和命名资源清理。比较方案：扩展现有 IAM fixture（采用，复用真实 Better Auth/PG/Redis、唯一清理边界）对比 Root 自造 IAM SQL/新一套 fixture（淘汰，越 owner 且重复生命周期）。粒度只扩现有 fixture/test；不新建业务模块。依赖方向仅 Root 测试消费 IAM fixture 交付，生产 API/数据模型不变；不得为便于测试放宽 Host/Origin/cookie/OIDC 校验。先用真实 integration 写 RED，随后 GREEN；验证 Node24 `pnpm verify`、串行 PG/Redis `pnpm test:integration`、资源0。IAM 提交并由 Root 独立验收后才更新 Root pin/消费者并做 B/C Product Session→BFF 私有负例；本片不冒称 R2d 全部完成。

R2e 用户可见入口方案校正（2026-09-24，只读诊断，未实施）：隔离 HTTPS fixture 的 `.example.test` 只在专用 Chromium 内映射，普通 IAB 不解析；临时 test tenant 也不是正式持续账号。正式入口要以 IAM/BFF/Web 源码进程接通，在一份开发数据库/Redis上依 owner 正式 schema、issuer/operator、OAuth client/credential、单租户账号顺序开通，BFF/Web 配置精确相同 Web origin 并去掉 preview 模式。IAM/BFF/Web 内部进程可 loopback 监听，但**Web 对浏览器公开的 RP origin 必须满足 OAuth client 的 HTTPS、非loopback hostname**；下方入口协议修正为最终裁决。比较方案：正式源码+普通IAB可达的HTTPS入口（目标，真实可复用身份）对比短时测试 host+专用 Chromium（只用于测试）。不重启用户3310、不降低 production HTTPS/Host/Origin/cookie 校验；此处不标 R2e 完成。

R2e-IAM-LISTEN 执行卡（2026-09-24）：IAM `main` 起点 `ef5f9358555ae666970b4fd186a551d0205a27bc`、工作树 clean，Root 负责 Git index/commit/复验；IAM 唯一writer仅可改 `src/config/environment.schema.ts`、`src/main.ts`、`scripts/start-issuer-bootstrap.ts`、相邻环境/源码进程测试及 `docs/RUNBOOK.md`/`docs/CURRENT.md`，不碰 contract、SQL、生成SDK、锁文件、其他仓或用户3310。Owner 是 IAM 进程监听边界，现有两个启动入口均强绑 `0.0.0.0`；目标给正式/issuer bootstrap 同一受检 `IAM_HOST`（开发显式 `127.0.0.1`），用真实源码进程验证仅 loopback 可达且正常优雅退出。比较“共用环境 schema + 两入口读取”（采用，单一事实）与脚本私有 host/临时代理（淘汰，双轨且不能证明正式服务监听）；扩现有文件而不建模块。此片无新 API/数据/tenant/contract，保留生产 HTTPS/Host/Origin/cookie 约束；验证 Node24 `pnpm verify`、真实 PG/Redis `pnpm test:process` 及 bootstrap 对等回归、资源0。完成它仅解除本机暴露风险，不等于 R2e 用户可见登录；Root 随后串行做正式单库单租户 IAM→BFF→Web 引导和 IAB 实测。

R2d-ROOT-ABC 执行卡（2026-09-24）：Root `main` 基线 `2d9f9450`，IAM A/B/C fixture owner 已提交 `ef5f935`（Root gitlink尚未升级）；Root 唯一脚本writer仅可改 `scripts/e2e/run_web_bff_iam_product_session_smoke.py`、`scripts/e2e/run_web_chat_worker_smoke.py` 与各自现有 `scripts/tests/` 相邻测试，Root 自留 task/progress、库存/gitlink、Git index/commit；不碰 IAM/BFF/Agent/Web 源码或用户3310。既有单次四服务 runner 拥有 A 的真 Product Session→Chat/Agent→持久事实，目标在同一隔离运行向 IAM host 一次性索取 B/C，以各自真实 Product Session 读取 A 资源时 fail-closed，同时证明 BFF/Agent 无副作用。比较扩展已有 runner 与窄登录后探针（采用，复用进程/资源生命周期）对比新建第二套四服务 runner（淘汰，重复清理与身份流程）；只扩现有文件。consumer 不访问 IAM/BFF/Agent SQL 以外 owner 的库，SQL证据按 owner 分开取；wire经Web同源Product Bearer而非注入token/cookie。先 RED→GREEN 聚焦单测，IAM最终SHA与Root inventory/pin由Root串行固定后，独占真实PG/Redis跑 B/C list/snapshot/events/mutation 404/空列表、A事实及 Agent执行数不变、资源0；不得在 IAM 真实集成运行时并发压同实例。此片不改分享模型，也不把同租户成员等同有权读私人对话。

R2d-ROOT-ABC 真组合第一轮修正：固定 Root `024767fe` 的四服务真运行在 B/C 前三种 GET 和 messages POST 之后，测试TLS代理对 DELETE 返回501（BFF观测最后仅POST），不是服务已证明的404；自有PG/Redis/进程清零。根因在既有 `scripts/e2e/run_web_bff_iam_oidc_smoke.py` 的 `ProxyHandler` 只实现 GET/POST。允许 Root 定点增加该**测试代理**的 DELETE 转发和 `scripts/tests/test_web_bff_iam_oidc_smoke.py` 相邻真实HTTP代理回归，不改变生产 Web/BFF、登录协议、IAM owner或前述资源清理；先RED后GREEN，再重跑同一真组合。

R2d-ROOT-ABC 组合验收（2026-09-24）：测试代理 DELETE 已先RED(501)后GREEN(404)；Root 在精确 gitlink Web `0a093f65`、BFF `eb1eb292`、IAM `e36da9ec`、Agent `520ec181` 先跑 Python CookieJar A/B/C 四服务组合 `status=PASS`，再跑真正 Chromium A + 独立 CookieJar B/C 同一四服务组合 `status=PASS`。B/C 的 Product Session 对 A 的列表为空、快照/事件/消息/删除/运行控制均 404 `session_not_found`，BFF/Agent owner SQL 与 System/模型调用计数前后相同；A 的浏览器 Chat POST202、AG-UI 五帧、受控断流一次与原 cursor 的 `Last-Event-ID` 恢复、刷新后一用户一完成助手均通过。两轮报告各自测试自有 PG 库、Redis DB7/14/15 keys、子进程剩余 0。Root 六个受影响 Python 文件 Ruff format/check、全 `scripts/tests` **679 passed/139 subtests**、topology/checkpoint/relay policy 门通过；代码与账本提交为 `77c2702a88c0bc14628984a7bd98a89e9bb180a0`，该提交后 main-only 及上述四个静态门、全测试复跑通过，所有组合仓与 Root 均 clean。System/模型仍为严格 deterministic fixture，不宣称真实 provider、显式分享正向路径或用户当前 3310 可登录。

R2e-IAM-LISTEN owner 交付状态：IAM main `e36da9ecf8d62a364182949817431a8e2329d50a` 已提交；Root 独立 Node24 `pnpm verify` 86文件/740项、串行真实PG/Redis `pnpm test:integration` 33文件/225项通过，自有临时库/Redis前缀0；只读复审0 P0/P1/P2/P3。此片仅让正式/issuer bootstrap 两个源码入口受检 `IAM_HOST=127.0.0.1` 时限本机监听，并非用户当前3310已可登录。R2d-ROOT-ABC 四脚本/测试经先RED后GREEN与独立两轮审查达到0 P0/P1/P2/P3，Root pin、真组合和提交仍待完成。

R2d-BFF-IAM-PIN 执行卡（2026-09-24）：BFF `main` 基线 `84a560abeac5b7a63f32d7064abdde849ab33cf9` clean、IAM owner `e36da9ecf8d62a364182949817431a8e2329d50a` 已提交；BFF 唯一writer只可定点修改 `contract/iam-relay-policy.json`、`src/http/routes/iam-protocol-relay.policy.ts`、`test/iam-protocol-relay-policy.test.ts`、`docs/API_CONTRACT.md` 与 `docs/CURRENT.md` 中对 IAM 当前 owner SHA 的事实，不改路由/业务/生成client/schema/lockfile/其他仓或Git index/commit。Owner 是 BFF relay policy 的 producer provenance；当前 Root `verify-iam-relay-policy` 因 IAM gitlink已升级而报 `iamOwnerCommit != IAM gitlink`。采用同一现有 policy JSON+TS+测试+文档精确换 SHA，淘汰 Root 放宽 verifier 或跨仓兼容旁路；IAM allowlist/snapshot blob digest及contract digest不变，API/DB/权限不变。先 RED复现再GREEN，Node22 BFF `pnpm check`/相邻测试及 Root固定gitlink政策门；Root独立复验、提交BFF并提升gitlink后才进行R2d真四服务组合。不得与其他BFF writer并发。

R2d-WEB-RELAY-PIN 执行卡（2026-09-24）：Web `main` 基线 `9e2eb7385ccd18f7fc0a139702d388c4f2fb6825` clean，BFF relay owner 新提交 `eb1eb2926d08b8a3779898b2c31e604a8585ec8b`；Web 唯一writer仅可更新 `src/generated/iam-relay-policy.json` 的 BFF 原始发布字节、`src/lib/server/iam-relay-policy.ts` 固定 BFF/IAM来源和SHA、`tests/contract/iam-relay-policy.test.ts` 与 `docs/{API_CONTRACT,TECHNICAL_DESIGN,DATA_MODEL,CURRENT}.md` 相关当前来源。Web不修改 BFF/IAM policy语义、路由、登录UI、SQL/SDK/锁文件或用户3310；Root自留库存/gitlink/测试脚本 pin和Git操作。目标是从 BFF commit blob消费唯一只读policy，不通过Web手写新事实；比较“精确发布字节+digest+provenance”（采用）与放宽校验/保留旧snapshot（淘汰，来源漂移）。Node22 `pnpm check`/policy contract/Playwright由Root独立复验，实际三仓/四仓 HTTPS在新固定组合再跑，不把旧截图挪用。

R2e 入口协议修正（2026-09-24）：此前“`NODE_ENV=development`可直接把 Product Web RP 放在 `http://127.0.0.1:3311`”的推断**被正式 OAuth client 注册约束否定**。固定 Better Auth OAuth provider `1.7.3` 将 IAM managed `user_delegated` client 默认视为 `application_type=web`，`validateClientRedirectUri()` 要求 callback 为 HTTPS 且 hostname 非 localhost/loopback；IAM owner没有设置弱化例外。正式可见入口因此应是普通 IAB 可解析的**HTTPS非loopback hostname**，经本机受控TLS到隔离 Web 进程，再由 localhost IAM/BFF owner 服务支撑；不能凭 IAM runtime 允许开发HTTP就注册HTTP loopback callback。用户3310继续不动；Web隔离工作目录避免其 `.next/dev/lock`。先证明 hostname解析/证书/IAB可达，再走owner正式schema、SMTP验证、operator/OAuth client/单租户创建与浏览器验收；不扩Web IAM relay signup/verify/admin路由，也不造测试fixture账号冒充常驻账号。

R2e 下一代码切片只读调查卡（2026-09-24）：Root `7fdda80d153d1bb773f992c65145bf15b4496283`、IAM `e36da9ec`、BFF `eb1eb292`、Web `0a093f65` 均 clean/main。`r2e_iam_boot_audit` 只读 IAM 的正式 `kokoro_iam` schema installer、issuer/operator/client/tenant/账号/验证入口，给出单数据库、单应用凭据下可复用的最小正式开通序列和真正需要改的代码；`r2e_web_bff_boot_audit` 只读 BFF/Web 配置、源码启动、同源 RP 和现有隔离 TLS runner，列出固定 HTTPS origin 的精确 env/依赖与最小代码缺口。Root 负责协议裁决、对两份结论交叉核查和下一 owner 写入任务卡；所有人不修改文件、Git、共享数据库/Redis或用户3310。仅只读查源码/文档和无需服务的静态命令；汇报需区分已存在能力与确实缺失能力，不以测试 host/专用 Chromium 替代正式入口。调查不是 R2e 验收；后续仍按 IAM owner→BFF/Web consumer→Root 普通 IAB 真浏览器顺序，不能跳过单库正式账号和严格 HTTPS callback。

R2e-IAM-VERIFY-RELAY BFF 设计门任务卡（2026-09-24）：Root `7fdda80d`、BFF `eb1eb292`、IAM `e36da9ec`、Web `0a093f65` 均 clean/main；BFF 唯一 writer、Root 审查/Git。**Owner**：IAM 已发布原生 `GET /verify-email`（`src/modules/auth/ingress/auth-routes.constants.ts`）及邮箱验证状态，BFF 仅拥有 `browser-private` 精确 relay path/method，Web 后续消费其发布 policy。**当前事实**：BFF `src/http/routes/iam-protocol-relay.policy.ts`、生成 `contract/iam-relay-policy.json` 和 `test/iam-protocol-relay-{policy,transport}.test.ts` 只转发登录/OIDC；Web 当前 GET 集合也缺验证邮件路径，因此正式 issuer 邮件链接 `${WEB_ORIGIN}/iam/verify-email?...` 当前 404。**目标职责**：只准入 `GET /verify-email`，保持 IAM 有期签名 token 与 emailVerified 幂等语义、no-store、同源受限 Location 和现有上下游预算，不开放 sign-up、组织写入或通配路由。比较扩展现有 BFF relay policy（采用，唯一可审计机器事实）与在 BFF 建新通用 auth proxy/由 Web 直连 IAM（淘汰，破坏准入方向）；只扩既有文件，不建模块/目录。**依赖/数据/API**：BFF 不创建身份/Session/SQL/Redis事实；仍 `Browser→Web→BFF→IAM`，BFF policy 作为版本化 browser-private artifact 发布，Web 在 BFF 提交及 Root 审查后精确固定 blob/digest；不修改 BFF public OpenAPI、IAM owner contract/schema、Product Session 或 Host/Origin/cookie 规则。**删除/验证**：没有旧路径或 alias 可保留；先更正本仓 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 使三文档一致并通过 Root 文档门，再在后续代码切片对现有 policy/生成物与相邻单测/真实 IAM HTTP 增加正向及外域/编码/错方法负例；Node22 `pnpm format:check && pnpm check`、Root 来源政策门、Web consumer 及最终正式邮件点击/登录组合。Phase 1 仅文档，不改运行代码/机器契约；Phase 2 由同一 BFF writer 续任并明确提交范围。首次 operator/固定 tenant 的 IAM owner 开通仍独立，不把 verify relay 视作完整 R2e。

R2e-IAM-VERIFY-RELAY Phase 1 Root 文档门（2026-09-24）：BFF 唯一 writer 已仅改三份设计文档；Root 检查 diff、IAM 原生 allowlist 与现有 BFF `allowedLocation`、请求/响应及零数据写入方向，三份文档当前/目标区分一致，`git diff --check` 通过，**放行 Phase 2 代码**。BFF writer 续任唯一写入，范围扩至 `src/http/routes/iam-protocol-relay.policy.ts`、由脚本生成的 `contract/iam-relay-policy.json`、`test/iam-protocol-relay-policy.test.ts`、`test/iam-protocol-relay-transport.test.ts`、`test/iam-protocol-relay.integration.mjs`（只在现有 IAM fixture 能产生真实验证链接时使用）、与此片必要的 `docs/CURRENT.md`；不改 IAM/Web/Root、BFF public OpenAPI、SQL/Redis、其他路由、Git index/commit。添加精确 GET `/verify-email`，policy version 因浏览器私有可见面扩展由 `1.0.0` 升 `1.1.0`，保持 IAM SHA/policy 其余边界不变；测试先RED后GREEN，尤其 raw query token不变、无授权头/Product cookie、302同源可达/外域拒绝、错方法与编码 alias 零上游 socket。Node22 `pnpm format:check && pnpm check`，Root 独立复验后提交 BFF；Root 再派 Web 唯一 consumer 更新来源和 GET allowlist。真正邮件令牌/SMTP及普通 IAB 登录由 Root 后续组合门证明，不以假 IAM transport test 冒充。

R2e-IAM-VERIFY-RELAY 独立审查扩卡（2026-09-24）：候选经只读 reviewer 发现 verify-email token GET 的上游无缓存头/恶意 `public` 会被 BFF 原样回传，且同源 302 可能把含 token URL 放入 Referer；另外文档误称“一次性消费”。Root 裁决只对已准入 `route.path === '/verify-email'` 在 BFF relay writeHead 前覆盖 `Cache-Control: no-store` 与 `Referrer-Policy: no-referrer`，其余 route 语义不动。唯一 writer 写入集增加既有 `src/http/routes/iam-protocol-relay.ts`；在既有 transport test 加无头/`public` 200/302 RED→GREEN，三文档/CURRENT 更正为 IAM 有期签名 JWT、emailVerified 幂等语义。Root 复审并重跑门禁后才提交；不扩 IAM、Web、其他 BFF 路由或 SQL/Redis。

R2e-IAM-VERIFY-WEB consumer 执行卡（2026-09-24）：BFF producer `main` 已提交 `928ada2880f222b4406b13144f7dfc7be43c8099`，`contract/iam-relay-policy.json` SHA-256 `67e40a5a034d27f205492cb57c3c8a84b3d10ef2096bc8447f5014dbcb69b6d6`、version `1.1.0`；Web `main` 基线 `0a093f65bdc4990b956b10ae534198e3b4b5c3b5` clean。**Owner**：IAM 拥有验证 token/邮箱状态，BFF 拥有 browser-private relay policy，Web 只拥有浏览器 GET 准入/同源响应。**当前事实**：Web `src/lib/server/iam-relay-policy.ts` 只认 v1.0.0/BFF旧SHA且 `BROWSER_GET_PATHS` 缺 `/verify-email`；`src/lib/server/iam-relay-response.ts` 仅依 BFF artifact `responseHeaders` 转发，当前会丢 BFF 专为 token GET 合成的 `Referrer-Policy`；Next `/iam/[...path]/route.ts` 已通用消费精确 GET，不需改公共路由。**目标**：固定复制 BFF 已提交 artifact/provenance并仅新增浏览器 GET `/iam/verify-email`；对该精确 path 的浏览器响应无论 BFF 上游 header 缺失/恶意缓存值均固定 `Cache-Control:no-store`、`Referrer-Policy:no-referrer`，其他 GET 保持原样；保留原始 query、同源 302 限制、cookie/Authorization 隔离，拒绝编码 alias/错方法/外域。**位置比较**：扩既有 policy/response/现有 Next HTTP 测试（采用，一处固定消费者），另建 auth proxy 或修改全局 security header（淘汰，扩张无关路径和系统边界）。**数据/API/依赖**：Web 不建 SQL 或新 Redis key，不更改 BFF/IAM owner contract，Web 只消费 BFF 1.1.0 固定 blob；source `Browser→Web→BFF→IAM`，不开放 sign-up/admin。**删除/验证**：替换旧 v1.0.0 artifact pin，不保留双读/fallback；Node22 `pnpm contract`、`pnpm test:architecture`、`pnpm lint`、`pnpm typecheck`、相关 unit/真实 Next HTTP 测试与 `pnpm build`；独立真同源 302/Referer 行为及最后邮件点击另列组合门，不把 fixture 当真实 IAM。唯一 Web writer 范围：`src/generated/iam-relay-policy.json`、`src/lib/server/iam-relay-policy.ts`、`src/lib/server/iam-relay-response.ts`、`tests/contract/iam-relay-policy.test.ts`、`tests/system/iam-relay-next-http.integration.test.ts`、与此切片必要的 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL,CURRENT}.md`。先三文档一致的当前/目标设计门，Root 审查后代码 RED→GREEN；writer 不碰 Git index/commit、用户3310、Web共享 `.next`/其他仓。Root 负责单独 build/集成与提交、policy来源门、浏览器验证及 Root 总文档。

R2e-IAM-VERIFY-WEB Phase 1 Root 文档门审查（2026-09-24）：Web 唯一 writer 只改三文档，Root 审核 owner/版本/数据/API/失败边界一致、`git diff --check` PASS；发现现有 `nativeIamResponse` 尚无 route path 参数，若仅改 response helper 无法区分单一路径。为实现精确特例，Phase 2 写入集增加既有 `src/app/iam/[...path]/route.ts`，只把已准入的 `route.relativePath` 传给响应 helper，不改公共路由分发/新建目录/放宽其他路径。三文档中“无需改 route.ts”更正为“仅透传已准入 route identity”，然后放行代码 RED→GREEN。Web owner 与 Root 不并发改同仓文件，Root 保留 Git index/commit 与最终复验。

R2e-IAM-VERIFY-WEB 真 Next HTTP RED 扩卡（2026-09-24）：Web writer 的隔离 Next HTTP 测试发现 Route Handler 合成的 `Referrer-Policy:no-referrer` 被现有 `next.config.ts` 全局 `strict-origin-when-cross-origin` 覆盖；因此仅 helper 单测绿色不是浏览器证据。Root 允许唯一 Web writer 在原写入集上增加既有 `next.config.ts`，仅为精确 `/iam/verify-email` 在最终响应保留 `no-referrer`；优先检验全局规则后精确规则覆盖，若 Next 实际不按预期，选择排除该精确 path 并由原有全局安全头其余项/Route Handler 保持覆盖，不改变其他路径/全局安全策略。必须以真 Next HTTP 及 Chromium 同源 302 Referer RED→GREEN 证明，文档记录该框架行为；不修改 `src/proxy.ts` 或泛化所有路由，也不碰用户3310/共享 .next。

R2e-IAM-VERIFY-WEB 第二真框架 RED 裁决（2026-09-24）：实际在 `next.config.ts` 追加精确 header 后，真 Next HTTP 仍输出 `strict-origin-when-cross-origin`；来源是 `src/proxy.ts` 的 `securityHeaders()` 响应覆盖。Chromium 现测得 token URL 同源后续 request 的 `Referer` 含完整 token，P2 有实证。Root 扩写入集仅增加既有 `src/proxy.ts`，对精确 `/iam/verify-email` 在 `NextResponse.next` security headers 写入后固定 `Referrer-Policy:no-referrer`（缓存仍由 route helper/现有 proxy 缓存策略保证），其余路径原样；若此修改使真 Next 最终安全头正确，删除刚才无效的 `next.config.ts` 特例、保留原全局配置，减少双轨。必须真 Next/Chromium RED→GREEN，且 README/三设计文档准确描述由 proxy 层控制最终 header，不以单测冒充。

R2e-IAM-VERIFY-WEB query 平台差异裁决（2026-09-24）：真 Next 16.2.6 会在 Route Handler 前将交错重复 query key 聚组重排；因此 Web 不能承诺任意原始 query 字节顺序，而 BFF/IAM owner 不应被迫为此增加旁路或私有 header。IAM Better Auth 1.7.3 `GET /verify-email` schema 只有必需 `token` 和可选 `callbackURL`。Web 对该精确路径收窄为只接受一个非空 `token`、零或一个 `callbackURL`、不得有其他/重复 key；在原有 `resolveBrowserIamGet` 中只针对该 path 做原始 query key 检查，重复/额外/编码别名本地拒绝，其他 relay GET 不变。文档与测试改称“支持的唯一键和值语义保持，Next 可能规范化 query 编码”，不再声称任意重复/畸形 query 的原始字节完全保留；真 Next 用正常 IAM 形状 token+callbackURL 测值，重复键测零 BFF socket。无新代理层或通用 query parser。

R2e-IAM-VERIFY-WEB 编码 query key 实测修订（2026-09-24）：真 Next 在 Route Handler 前把 `%74oken` 解码为 `token`，因此纯函数看到原始字符串时会拒绝编码键，但浏览器真 HTTP 中该拼写已不可见且被当作相同参数。Root 不为编码拼写另加 raw-target header/代理或新进程：这不是 path/method/身份绕过，IAM 实际处理的仍是同一唯一 `token` 参数。保持**规范化后**唯一 `token`、可选唯一 `callbackURL`、无其他 key 的语义准入，若字面/编码混用导致重复仍拒绝；文档和测试区分纯函数原始输入与 Next 真 HTTP 观察，不声称编码 query key alias 在框架前被 Web 识别并拒绝。路径 alias、外域、错方法、Bearer 的零 BFF socket 负例仍必须保留。

R2e-IAM-VERIFY-WEB 开发日志发现扩卡（2026-09-24）：独立 reviewer 定位 Next 16.2.6 `pnpm dev` 默认将完整 incoming request URL 写入 stdout；`GET /iam/verify-email?token=...` 会泄露 IAM 有期签名 JWT，Route Handler 和 proxy 的响应头无法保护这条框架日志。Root 允许唯一 Web writer 再将既有 `next.config.ts` 纳入精确写入集，使用 Next 已有 `logging.incomingRequests.ignore` 只忽略 `/iam/verify-email` 本身及 query（锚定 path，不能宽泛匹配）；同时在隔离真 Next HTTP 测试检查 `nextOutput` 不含实际 token。此前无效的 response-header 特例不恢复。三文档/CURRENT 明确该开发日志治理只覆盖 Next 自带 incoming log；TLS前置 access log、浏览器历史、外部邮件系统另由组合/部署边界核查，不冒称所有路径全局不记录敏感 query。

R2e-IAM-VERIFY BFF→Web 切片验收记录（2026-09-24）：BFF producer 已在 main `928ada2880f222b4406b13144f7dfc7be43c8099` 提交，Root 独立 Node22 format/check PASS（270 pass/1 skip）；Web consumer 已在 main `24445a17614c6d3ed96c3faef40bff1f36538292` 提交，Root 聚焦真 Next/登录/contract 13/13、contract 56/56、architecture 32/32、lint、`tsc --noEmit`、全量 Vitest 1414/1414 PASS，独立 reviewer 对两切片均 0 P0/P1/P2。Root inventory/gitlink/CURRENT pin 已准备；提交后仍需重新验证 Root source policy/checkpoint/main-only 与全 Root tests。下一 owner 是 Root 的正式 IAM 单租户账号/SMTP邮件点击/普通 IAB入口组合验收（不复活中转 UI），以及 Web 可隔离的 Next typegen/build；TLS access log 只作为敏感 URL 核查点，非运维拓扑调试。Billing 保持最后。

R2e-IAM-VERIFY Root 集成门（2026-09-24）：Root 已将 BFF/Web gitlink、来源 inventory 150 条引用和 CURRENT 作为 `94789df52baed3555f4e7e27af8b715c8a19aae8` 提交；提交后 relay source policy、contract checkpoint、topology、main-only 均 PASS，全 Root `scripts/tests` 679 pass/139 subtests，工作树 clean/main。R2e 当前状态是 **代码/静态来源与 fixture 浏览器门已闭，正式入口/邮件运营组合未闭**；下一片只处理真实 IAM 单库账号/邮箱验证/正式浏览器与可隔离 Web build，不借旧可见中转/重试页掩盖 3310 空503。已有 ten-repository-standard 的 130 条全局不合规另按 owner 切片处理，不作为本片绿灯。

依赖顺序：R2a→R2b→R2c；R2d 可在 R2b 后由 IAM 唯一writer独立推进，再由 Root 串行集成；R2e 使用同一真实入口，不为可见性分叉协议。每个写入任务先记录精确基线/允许文件集、先RED再GREEN、Root提交并复验。R3真实System发布route和真实provider仍独立，不让 fixture 代替。

R2a 验收状态：唯一writer已把最终证据查询拆成 BFF/Agent 各一条 owner-local SQL，Python 严格字段合并；独立复审 0 P0/P1/P2，Root 聚焦 18/18、全Root **622 passed/130 subtests**、Ruff、真 worker smoke PASS，临时资源为0。本条与代码由 Root 同切片提交，R2b随后推进。

R2 当前执行卡（2026-09-24）：Root `a65c2f87`、Web `210ddfdd`、BFF `84a560a`、IAM `b35a9a5`、Agent `520ec181` 为固定来源。R2b 的唯一 Root 脚本 writer 在现有 Product Session runner 加**可选**登录后 action/JSON/header 传输，并新增 `run_web_chat_worker_smoke.py` 与聚焦测试；Root 保留任务/进度文档、Git index、最终复验和提交，不在该 writer 编辑期间修改脚本。当前共享 helper 聚焦 36 passed/63 subtests；第一次真组合已启动 IAM、Web 隔离 Next、BFF、Agent HTTP/CLI 与 System/model fixture，在 Web workspace 链接解析处停住；随后修复 runner 自有隔离依赖及 worker 启动顺序，再实跑并核对临时资源为0。第一次未通过，不记为 R2b 闭环。

Web 登录可见体验由 Web 唯一 writer 在独占 detached worktree `/tmp/kokoro-app-r2c-wire` 从 `210ddfdd` 提交 `155c1bd2`（首次 `/login` 不在挂载时请求 CSRF，用户点击唯一账号继续按钮才进入固定 OIDC；连接/失败重试）。该 UI 片已交付待 Root 集成与 Node22 复验；独立审查要求追加 auth INDEX 当前描述与真实303→失败重试页 E2E。**同一 detached worktree 的前序 `4b933870` 已被 Root 和独立审查否决，不得集成：**Web210 原 `createMessage` 已把 `idempotency_key` 与 body 分离；BFF 当前 canonical parser 确实接受 `model/agent/thinking/pinned_skills/project_ref`。`4b933870` 错误丢弃这些字段，会把专案首消息写成 direct 且静默丢失用户选项。R2b 应直接使用 Web210 正确的已发布 body，不把误报 P0 当修复。登录交互将旧“自动触发”决策改成显式意图，原因是单独预览 Web 时首屏立即出错且用户明确指出界面不合理；不新增营销页、Web 凭据表单或第二条认证路径。Web writer 的独占工作树曾错误将其 `node_modules` 链到 main，导致 3310 Turbopack tsconfig 解析失败；已改为独立实体依赖，Root 恢复 main `@kokoro/*` 三个 ignored workspace 链接。为避免 Web `next-env.d.ts` 被 dev 改写阻断 R2b 固定来源 preflight，已临时停止3310并恢复该文件，待 R2b 后恢复。后续并行工作禁止共享/重链主工作树 `node_modules`、`.next` 或 pnpm workspace 链接。

R2d IAM 身份矩阵在独占 detached worktree `/tmp/kokoro-iam-r2-matrix-9c71` 提交 `2441845`（父 `b35a9a5`，仅测试 fixture/integration），显式 `IAM_TEST_IDENTITY_MATRIX=abc` 时提供真实 A/B/C 用户/租户；Node24 owner `pnpm verify` 739/739、真实 host integration 16/16，自有临时资源清理通过；另一只读审查 0 P0/P1/P2。Root 仍须等 R2b 旧固定来源放行后再审查/集成该提交并完成严格 IAM→BFF→Web provenance 级联；矩阵本身不证明 Product 隔离或 BFF/Agent 零副作用。

R2b Root 集成验收（2026-09-24）：六个 Root 脚本/测试由唯一writer交付；独立只读审查 P0/P1=0、两项P2（重复Content-Length framing与stop失败后数据清理）已 RED→GREEN 修复。Root 复跑六文件 Ruff format/check、聚焦 **48 passed/67 subtests**、全 `scripts/tests` **641 passed/134 subtests**、topology、IAM relay policy、w1b-iam checkpoint 全 PASS。固定 Web `210ddfdd`/BFF `84a560a`/IAM `b35a9a5`/Agent `520ec181` 的真实HTTPS一用户组合由 Root 独立复跑 PASS：Product Session→Web同源Message POST 202/重放202/改body409→BFF→真实Agent HTTP及独立CLI worker→System/模型严格fixture各1次→Web重载snapshot、AG-UI 5帧；BFF outbox succeeded/assistant completed/source4，Agent terminal=true/chat4/assistant1；临时PG库、Redis DB7/14/15、owned进程均0。此门是Python CookieJar HTTPS客户端，不是Chromium/真实provider。Root `verify-ten-repository-standard.py` 同次诊断仍有既存130条跨仓规范违例，contract inventory仍11 broken+1 illegal；它们不因R2b通过而关闭。R2c实时SSE/Chromium与R2d隔离负例继续，随后R2e可见入口。Web UI三提交待R2b固定来源提交后串行集成，废弃4b933消息误修。

Web UI 放行与 R2b 新来源复验（2026-09-24）：Web main 从 `210ddfdd` 仅集成登录 handoff 的 `bd728a0`、RP303回归 `7c7b38e`、显式locale/当前证据 `ebde06d`，再由Root修正跳过误修后 unit 计数并发布 `2211020b10e5a57b9b0e55367179844e52238dfb`；`src/engine/client.ts` 与 Web210 字节相同，未集成 `4b933870`。Root 在 Web main Node22 复跑 `pnpm check` contract54、architecture32、unit1396、lint/typecheck/build PASS；桌面+移动 `pnpm test:e2e` 15通过/1既有skip。Root精确校验库存5个Web blob digest未变，将9处来源commit和Root gitlink pin到新SHA；topology、checkpoint、policy、main-only与全Root **641 passed/134 subtests** 通过；新固定SHA真HTTPS Product Session+Web首发+真Agent CLI worker组合再次PASS、临时资源0。3310 恢复唯一Node22 Web dev，IAB实际首屏为简洁品牌/单按钮 handoff，`/login` HTTP200，首屏不再自动显示失败；仅Web运行时点击仍因RP/BFF/IAM未常驻而CSRF503，这不是在线登录完成。后续优先R2c真实Chromium页面+有界SSE、R2e可见完整HTTPS入口，再接R2d私有矩阵与其他能力。

R2c 真 Chromium 放置门与任务卡（2026-09-24）：Owner 为 Root 跨仓 E2E，Web/IAM/BFF/Agent 仍各自拥有业务事实；基线 Root `553c3c3b`、Web `2211020b`、BFF `84a560a`、IAM `b35a9a5`、Agent `520ec181`，子仓均 clean。当前 R2b 是 Python CookieJar、TLS Proxy 响应整体缓冲 SSE 且虚拟 origin 不带监听端口，不能借其证明真实 DOM/流式恢复。候选A 在 Root 现有`scripts/e2e/`建窄 Chromium 编排与 `.mjs`驱动，复用R2b独占PG/Redis/IAM/Web/BFF/Agent启动/清理边界，仅增加测试自有真浏览器与有界SSE流代理（采用）；候选B让Web Playwright普通preview测试注入假cookie/假Agent帧（淘汰：不跨真实owner）；候选C修改生产 Host/Origin/cookie、写系统DNS或对证书警告人工跳过（淘汰：扩散信任边界）。唯一Root脚本writer可新增 `scripts/e2e/run_web_chat_chromium_smoke.py`、`scripts/e2e/web_chat_chromium.mjs`、`scripts/tests/test_web_chat_chromium_smoke.py`，确需提取第二调用者helper时先报告；Root保留 `docs/task.md`/`docs/progress.md`、Git index/commit与最终复验。先使 `web_origin=https://web-<run>.example.test:PORT`、TLS监听port、浏览器URL、Host/Origin、IAM callback、Web RP/Next配置完全一致，Chromium仅本进程使用host resolver和测试自签证书配置；代理SSE逐块有界转发并即时flush，断流后Web重连须携带`Last-Event-ID`、无重复。不新建生产API/Schema/角色/兼容路径；Web UI使用已发布唯一CTA→真IAM Email/Password、tenant、consent→`/app` composer→真worker回复→刷新恰好一对消息。先TDD端口/代理framing/断线/异常cleanup，后Node22已安装Playwright 1.62.1的真Chromium执行，观测PG/Redis/浏览器/进程归零；只按实测宣称，System/模型fixture与真实provider分开。当前独立预审识别P1：旧代理完整缓冲SSE、无端口虚拟origin；R2c必须解决两者，而不是把R2b改名浏览器测试。

R2f 正式登录入口（2026-09-24，代码与隔离浏览器已验）：用户确认彻底删除可见“连接中／整页重试”中转设计，直接呈现 IAM 邮箱/密码表单。Web `175a6d80` 已删除 `LoginPanel`、CSS 与旧测试，将 `/login` 改成服务端 OIDC 启动路由；真实凭据仍只在带签名 query 的 IAM `/auth/sign-in` 提交。IAM 同源交互表单改为品牌+窄列，无双区巨大装饰；浏览器错误在表单内保留邮箱、清空密码并签发新 CSRF。固定该 Web SHA 的真 HTTPS Chromium 已从 `/login` 经真实凭据/tenant/consent 进入 `/app` 且资源清零；后续 `9794a286` 只更新 Web 当前态文档，Root 已重钉并复跑同一真 HTTPS 组合 PASS。3310 仍只有 Web，不是完整 IAM 环境，不能把其 HTTP 503 当作可用登录。下一步是用户可见、可停止的真实 HTTPS 入口和 Chromium Chat/SSE；不分叉协议或伪造登录页。

R2f 登录失败页删除补片（2026-09-24）：Web `f8650fc` 将 `/login` 启动失败时残留的共享 IAM 风格整页 HTML 一并删除；成功的固定 OIDC 302 路径不变。失败只返回空 body 的 HTTP 503 和服务端阶段日志；没有可见“连接中／整页重试”或伪造登录表单。Web Node22 `pnpm check` contract54、architecture32、unit1401、lint/typecheck/build及当前3310真浏览器聚焦5/5通过；3310仍缺RP/BFF/IAM常驻配置，因此当前并非可登录入口。可见真IAM入口仍归 R2e，不能用空503冒充闭环。

R2c 浏览器门新增真实 blocker（2026-09-24）：Web `1ecacef4` 已恢复已批准的自动登录和全视口布局，Root `9366b3ae` 固定 gitlink；Chromium 在独占 HTTPS origin 确认 CSRF1/signin1 后停在 `/iam/oauth2/authorize` 的 HTTP200 JSON redirect，旧 Python CookieJar 代读 JSON 因而漏检。先由 Web 唯一writer把**仅 authorize GET** 的精确/同源受信 JSON redirect 转成浏览器可执行 HTTP 302，补 Web contract/route/Chromium 回归；Root 在 Web 提交+gitlink/来源 pin 后复跑真实 Chromium 到 IAM 表单。此门通过后再推进浏览器凭据/tenant/consent、消息/AG-UI/SSE，不把目前的 200 JSON 当页面成功，也不让错误 UI 隐藏协议缺陷。

R2c 当前状态（2026-09-24）：Web `e531f0af` 已将精确 authorize GET 的合法 JSON continuation 转为 HTTP 302，`c9fcfcc1` 将 IAM sign-in/tenant/consent GET 页面统一到响应式认证布局；Root gitlink 已固定至 `c9fcfcc1123ddecf726002b69c78bcd9f7050662`。真 Chromium 在测试自有 HTTPS origin 从 `/login` 自动进入 IAM 邮箱/密码表单，CSRF/signin 各一次；随后独立 CookieJar 走完 Product Session、Web 首消息、BFF/Agent CLI worker、AG-UI 持久回归，两次连续 PASS，资源清零。**未完成项**：同一个 Chromium 中提交凭据、tenant/consent、`/app` 首消息、实时 SSE 断线恢复和私有负例；3310 仍只有 Web dev，不是可用的完整登录入口。下一片先补浏览器整链，而非继续美化错误页。

R2c 凭据与会话增量（2026-09-24）：Root 唯一脚本writer将 IAM 测试 host 的 `ready.email/password/tenant_id` 经子进程 stdin 交给真 Chromium；同一 BrowserContext 原生提交三张带签名 query/CSRF 的表单，进入 `/app` 后浏览器同源请求 `/api/auth/session` 严格检查 projection 和 Product Session cookie属性。先写聚焦RED、后GREEN，隔离真HTTPS组合 `status=PASS`，自有PG/Redis/进程0。由于首个 Chromium grant 改变 IAM consent 持久事实，后续独立 CookieJar仅在 R2c 明确接受已授权路径直达RP callback；默认 R2b 首次授权路径保持不变。**下一门**：同一 Chromium 从 composer 发送首消息、真实Agent CLI worker、SSE流式与断线 cursor 恢复及reload；当前CookieJar首消息只作独立回归，不计作Chromium Chat。

R2c-Chat 当前执行卡（2026-09-24）：Root 基线 `54e3d38c21588b4fcfa83c31f370323cc5dcfdd6`，Web `9794a286`、BFF `84a560a`、IAM `b35a9a5`、Agent `520ec181`，开始时各仓 clean。Root 原生子 Agent `r2c_chromium_chat_writer` 是本片唯一脚本 writer；允许写现有 `scripts/e2e/run_web_chat_chromium_smoke.py`、`scripts/e2e/web_chat_chromium.mjs`、`scripts/tests/test_web_chat_chromium_smoke.py`，因真浏览器提交必须在 Agent worker 已启动的时序下执行，已追加授权 `scripts/e2e/run_web_chat_worker_smoke.py` 与对应聚焦测试做窄 callback 注入；禁止改子仓、台账、Git index/commit、用户 3310 与非自有资源。Root 独立负责跨仓边界、diff审查、资源检查、全门、真浏览器复验与提交。交付门：同一 Chromium 完成真实表单→DOM composer 首发→Agent worker→AG-UI→刷新后持久消息；SSE 断线 cursor 恢复如不能同片证明，单独留红，不把 R2b CookieJar 证据挪用。并行只读 `team_bff_read_audit` 审查 IAM Team owner→BFF 下片文档/契约，不操作资源或写文件。

R2c-Chat-Web-SSE 当前切片（2026-09-24）：Root 真 Chromium 在已固定 Web `f8650fc`/BFF `84a560a`/IAM `b35a9a5`/Agent `520ec181` 的隔离 HTTPS 中，真实 IAM 登录、`/app` DOM POST202 后浏览器只显示一条用户消息和失败卡，助手 DOM 为0；同一 owner snapshot 已有一条 completed assistant、event watermark 与 BFF AG-UI 五帧，SSE 首次 HTTP200 后浏览器记录请求失败。Web `src/app/api/session/[...path]/route.ts` 使用普通上游 HTTP 15 秒总 deadline 是已定位的长流缺陷，但是否唯一根因须以真实帧/错误证据判定。Web 唯一写入负责人 `web_agui_stream_owner`，允许写 Web SSE adapter/transport/相邻测试与必要 CURRENT，Root 不并发写 Web；Web 先 RED→GREEN 证明帧可流式消费、断线后 Last-Event-ID 恢复，不改 IAM/BFF/Agent/Team、3310 或安全边界。Root 保留 Chromium runner、跨仓验证与提交；浏览器 assistant/reload 与受控断线未通过前 R2c 保持未验收。

R2c-Chat-Web-SSE owner 交付：Web main `067d7eabb404d80a88ff47c7fc0220b4a43bfcd1` 已提交；真实 BFF `RUN_FINISHED` status/outcome/result、`RUN_ERROR` threadId/runId、工具 isError 曾被 Web strict schema 拒绝，Web writer 用真 owner wire 先 RED 后精确字段 GREEN，不改 passthrough；另以真实 Node SSE 首部后跨原总 deadline 的 RED/GREEN 证明流式空闲计时。Web Node22 `pnpm check` contract56/architecture32/unit1408、lint/typecheck/build PASS，独立只读审查0 P0/P1/P2。当前3310外部环境跑全 Web E2E 为9通过、1跳过、2失败（未认证 `/app` 无 rail；空503移动浏览器宽度差1px），不能写作全绿；此环境不具备真 IAM 登录。Root 下一门是固定新 Web gitlink 真 Chromium assistant DOM/reload，之后故障注入验证 `Last-Event-ID` 重连；未实跑前 R2c 不关闭。

R2c-Chat-DOM 子切片已过（2026-09-24）：Root 在固定 Web `067d7ea`/BFF `84a560a`/IAM `b35a9a5`/Agent `520ec181` 上复跑独占真HTTPS Chromium，单一 BrowserContext完成 IAM Email/Password→tenant→consent→Product Session→`/app` DOM composer POST202→真实 Agent worker→Web AG-UI 五帧→助手DOM可见→刷新后恰好一用户/一助手；BFF outbox/assistant 和 Agent执行终态一致，System/模型仍是严格fixture；测试自有PG/Redis/进程剩余均0。该子切片只证明正常实时流/重载，不证明受控断线 `Last-Event-ID` 恢复或用户当前3310可登录。R2c下一片保持恢复门 RED，随后再关浏览器闭环。

R2c-Chat-SSE恢复子切片已过（2026-09-24）：Root 唯一脚本writer增加测试自有 TLS proxy 首帧完整截断：首次 Chat `/events` 只转发一帧 id/data，同一 upstream chunk 内其余帧不外泄，故意省略终止 chunk 触发真实浏览器断流；第二次同 conversation `/events` 必须携第一帧 cursor 的 `Last-Event-ID`。Root 固定 Web `067d7ea`/BFF `84a560a`/IAM `b35a9a5`/Agent `520ec181` 的真 Chromium exit0，proxy cuts=1、首cursor=重连header、浏览器观察至少两次200 SSE，五帧唯一、assistant DOM及reload一对、owner SQL一致、测试自有PG/Redis/进程0。此完成 R2c 正常流+受控一次断线的固定fixture门；R2d A/B/C个人私有矩阵、R2e用户可见完整入口、真实provider及后续Wave仍独立待验。

W1C-Team 当前裁决（2026-09-24）：只读审查确认 IAM main `b35a9a5` 中三窄读 owner contract 已由 `68aa0da` 发布，OpenAPI 0.3.0 digest `e1a023d3ae9839c345d65ec91c3674bd105a9c27f65bb6ecb10f74c965340c54`；BFF main `84a560a` 已固定其生成 client、发布 `/v1/team/{members,invitations,roles}`，Root 真 OAuth→BFF→IAM R4 已验 19 case。故原“再实现 BFF 三读”的待办取消，不能重复写。Web `src/team/client.ts`、旧 `/api/team/*` 和 sealed team-session 仍要求本人团队、未入组邀请、邀请/成员写、切换以及旧字段，当前三读仅当前 tenant 且缺 email 等字段；直接换 URL 会破坏业务。下一片 IAM owner 先对这些缺失操作完成三文档门和契约/SQL设计，再按 IAM→BFF→Web 顺序实现并删除旧直连，不放宽默认个人私有。

W1C-Team-R2D 文档任务卡：IAM 唯一写入 Agent `iam_team_r2_design_writer` 进行中；基线 IAM `b35a9a5301219654ea344c03407fd355f58c481e`、`main` clean。写入仅限 IAM `docs/TECHNICAL_DESIGN.md`、`docs/API_CONTRACT.md`、`docs/DATA_MODEL.md`，Root 保留 Git index/commit、Root pin/台账和最终设计审查；不改 runtime、机器 OpenAPI、Schema、SDK、Web、BFF。基于 IAM 现有 Better Auth/组织/邀请事实和 Web 当前真实 UI，区分当前已发布的三读与目标缺失操作，裁决可信 issuer-session 与 user-delegated Product token 边界、owner、状态机/并发/幂等、权限、分页、API wire/错误、索引与 SQL 事务，并列出无法从现存事实确定的产品决策。完成条件：三文档对齐、无伪造当前态、无跨 owner SQL、Root 审查通过后才允许机器契约和实现切片。

W1C-Team-R2D 评审裁决（2026-09-24）：上述候选已由 IAM writer 交付为未应用 patch `output/iam-team-r2d/iam-team-r2d-candidate.patch`，独立只读评审结论“需修改，不通过文档门”。用户本轮固定单租户部署是当前产品边界；候选为旧 Web `me/teams`、`switch`、`personal|team` 造跨 tenant 自助目录、重授权切换、全局cursor/索引，属于目标偏移。IAM main 保持 clean、机器 contract/schema/runtime 不变。下一切片先收敛 Web Team 目标为**当前固定 tenant** 的成员/邀请/角色，复用 IAM/BFF 已发布三读；删除旧 sealed team-session、namespace换签与切换 UI，而非做兼容层。成员 email不默认公开，多角色按 owner wire 展示；写投影按 IAM user-delegated scope/权限经 BFF 串行发布。未入组者接受邀请优先复用已发布 issuer verified accept/reject 及直接邀请入口；仅产品确需 inbox 时再设计固定 tenant 的窄 pending 读，不预建全局列表、HMAC cursor或索引。IAM 三文档须按此裁决重写并由 Root 复核，之前的候选不允许作为实现依据。

R2f-RP-RETRY-DELETE（2026-09-24，已验收）：Owner `kokoro-app`，唯一 writer Root；基线 Web `24445a17614c6d3ed96c3faef40bff1f36538292`，main/clean。原 `/login` 可见中转页已删除，但 RP `signin` 尚保留 HTML Accept 失败时 303 跳转旧重试 URL。只在现有 RP route、相邻 unit/E2E 与 CURRENT 定点删除该分支；不新增页面、服务、数据或契约，也不改正常 302 OAuth。测试先 RED（旧 303 vs 预期 400），后 Web `a70dd24f75ce05f5e25f424c09c111dadffcdd96` GREEN：失败维持原状态/结构化错误、无 Location，`/login` 仍只有成功时直达 IAM。Node22 全 Web Vitest 1414/1414、lint、`tsc --noEmit`、登录聚焦 Chromium 5/5；完整 Web governance E2E 的未认证 rail logout 1 个失败，属当前 3310 无 Product Session 的环境前置而非本片放行证据。当前 3310 `/login` 真 HTTP 503/0-byte，旧 IAB 标签页曾保留过期 HTML 文档，两个旧标签均已导航离开；正式可用登录仍待 R2e 单租户/SMTP/HTTPS 入口组合，不把空 503 当闭环。Root 固定 Web gitlink/库存后须重跑来源与 main-only 门。

R2e-IAM-SMTP-VERIFY 测试卡（2026-09-24，IAM owner 已验收）：Owner `kokoro-iam`，唯一 writer `r2e_bff_verify_review`；基线 IAM `e36da9ecf8d62a364182949817431a8e2329d50a`、main/clean，Root 保留本仓 Git index/commit、Root 台账和跨仓组合。只许修改 IAM 既有 `test/integration/smtp-delivery.test.ts`，必要时同目录既有 fixture 作窄测试修复须先报告；不改 runtime、contract、Schema、lockfile或共享基础设施。当前该测试用真 TCP SMTP 只断言首次验证邮件发出，随后测试 magic link；用户首登所需的“真实验证邮件→点击 issuer URL→emailVerified→凭据登录 Session”尚未由真 SMTP 一条用例证明。方案 A 扩展现有 SMTP integration 顺序消费第一个 verification message（采用，复用隔离 PG/SMTP 与已有 auth 实例）；方案 B 在 Root 四服务 runner 内首次证明所有 IAM 内部语义（淘汰，跨仓失败难定位）；方案 C 新建专用 test/fixture 目录（淘汰，单文件职责已有归属）。测试先 RED：注册未验证不能登录；消费真实邮件 token 后可登录并确认 issuer Session，wrong/重复 token 的预期需按当前 Better Auth 1.7.3 真实语义断言，不臆造“一次性”。运行 IAM Node24 `pnpm exec vitest run test/integration/smtp-delivery.test.ts`（自有临时数据库、SMTP server），以及 format/lint/typecheck 或仓库标准门；运行资源结束后检查清理。交付文件清单、命令与结果，不操作 Git index/commit。Root 独立复跑后放行。此测试只证明 IAM owner，不代替 Web→BFF邮件 relay、OAuth client/固定租户/普通 IAB 登录组合。

R2f-RP-RETRY-ROOT 合同修复（2026-09-24，Root 唯一 writer）：Web `a70dd24` 已把浏览器 RP 错 CSRF 返回结构化 403/无 Location，但现有两个 Root 三服务 smoke 仍断言旧 303 重试页，导致真实组合会误红。只改既有 `scripts/e2e/run_web_bff_iam_oidc_smoke.py`、`scripts/e2e/run_web_bff_iam_product_session_smoke.py` 和前者相邻 Root 测试；一个共享断言检查 403、`rp_signin_rejected`、JSON/no-store、无 Location/Set-Cookie，两 runner 复用。不改 Web/IAM/BFF 运行代码、协议或新目录。TDD RED 为 helper 缺失，GREEN 聚焦 40 passed/76 subtests、Ruff format/check；真实固定来源 HTTPS 组合待 IAM writer 停写且各仓 clean 后运行，不能仅据单测声称跨仓通过。

R2e-IAM-SMTP 来源级联卡（2026-09-24，顺序进行）：IAM `c16a9bcddd19211eb1e9705c392f4e5cf96f494e` 已新增仅测试的真实 SMTP 首登证据，owner allowlist/vendor snapshot blob 实际未改；Root strict provenance 仍要求 BFF policy 的 `iamOwnerCommit` 精确等于 IAM gitlink，因此先由 BFF 唯一 writer 在既有 `contract/iam-relay-policy.json`、`src/http/routes/iam-protocol-relay.policy.ts`、相邻测试与三设计文档/CURRENT 重钉 IAM SHA，仅 provenance，不改版本/路由/头/限额。Root 验证 BFF JSON 与 TS 字节语义一致、IAM 两个 blob digest 不变、format/check 后提交 BFF main。再由 Web 唯一 writer 消费 BFF 新 policy 原始 blob/digest，更新既有 generated snapshot、`iam-relay-policy.ts`、契约测试、三设计文档/CURRENT；不得手改业务 relay/信任边界。Root 最后按 IAM→BFF→Web 的提交顺序 pin 三 gitlink、来源库存和 CURRENT，提交后重跑 policy/checkpoint/topology/main-only/Root tests 与隔离真 HTTPS Product Session runner。候选跳过 BFF/Web 直接放宽 Root policy verifier（淘汰：会使 runtime 来源漂移失去门禁）；本片无 API/SQL 变更，旧可见登录 UI 不恢复。每仓单 writer，Root 管理各仓 Git index/commit。

R2e-IAM-SMTP 来源级联验收（2026-09-24）：IAM `c16a9bc`、BFF `a50f987`、Web `5192ff0` 已按 owner 顺序发布 main，Root `a6cb4ece` 精确 pin 三 gitlink/库存；IAM allowlist/vendor snapshot digest 不变，BFF policy 1.1.0 除 IAM SHA 外语义不变，Web 生成快照与 BFF artifact 字节一致。Root source policy/checkpoint/topology/main-only、IAM `pnpm verify`+SMTP integration、BFF `pnpm check`、Web contract/architecture/lint/tsc/full unit 均通过。真 HTTPS Product Session runner因 Root 旧缓存字面断言先 RED，经 TDD 将 `no-store` 按 Cache-Control 指令检查后 PASS，测试自有资源0；此证明已预置测试用户的 OAuth/Product Session，不代替真实 SMTP经 Web relay 点击/固定租户开通或普通 IAB 可见入口。下一 Root 代码片是首次使用组合，先复用现有隔离 runner/fixture；不得把本轮三仓绿灯写成 R2e 整体闭环。Web 正式 Next typegen/build 也待隔离验证。


W1C-FIXED-TENANT-WEB-D／正式登录入口（2026-09-24，Web 代码门已验，三仓真组合待验）：Owner `kokoro-app` RP/Product Session，Web 唯一 writer 已交付 main `e8a98ec41a6bcd9151f1ffd4c3bc907e7c957de0`，Root 审查并提交；IAM `3231d2e9`、BFF `87f9d8d` 为固定 owner 来源。Web `/login` 保持纯服务端 OIDC 302 入口，成功直达 IAM 表单；可见“连接中／整页重试”、租户选择表单、浏览器 POST 均已删除。callback 与 refresh 以当前 Bearer 调 BFF `GET /v1/me`，严格比对 OIDC subject/server-only 固定 tenant，错误时不建立 session 或使旧 generation 失效。Root 独立已验 Web contract 57、architecture 34、Vitest 1424、lint、typecheck、隔离生产 build；Root first-login/tenant continuation runner 聚焦 26 passed/62 subtests。当前 3310 实测 `/login` HTTP503、空 body，仅 Web 常驻且 RP/BFF/IAM 未配置，故可见旧设计已清除，但 3310 可登录仍是 R2e 的未完成项；禁止把空 503 当成正式在线登录。Root 下一门：重钉 gitlink/库存后执行固定 SHA 隔离真 HTTPS 首登＋Product Session smoke，检查原租户邀请、固定 GET continuation、两次 `/v1/me`、资源清零；然后处理实际失败，不修改用户 3310 进程。


W1C-FIXED-TENANT-SMOKE-E（2026-09-24，待实现）：Root 固定 SHA 真三仓 smoke RED，IAM 禁用的 Better Auth `/iam/organization/invite-member` 返回 404；不得再走该入口。Owner 事实由 IAM internal `POST /internal/v1/tenants/:tenant_id/invitations` 创建，再由 issuer `POST /iam/v1/tenants/:tenant_id/invitations/:invitation_id/accept` 接受。先设计测试 host 受限命令或可验证的 user-delegated token 获取方式，禁止把服务密钥泄给浏览器/放宽生产 allowlist；按 IAM→BFF 来源 policy→Web artifact→Root pin 顺序提交，并重跑唯一固定租户的 SMTP→OIDC→Product Session/refresh 真链。用户 3310 Web-only 预览不因隔离 smoke 改动。


W1C-FIXED-TENANT-SMOKE-E 实现切片（2026-09-24，待真组合）：IAM 唯一 writer `iam_fixed_invite_fixture` 只改现有 OIDC test host/集成测试，main `7f39193fff97dbb1398cb536ded7dca0db354213`；BFF 唯一 writer `bff_policy_iam_pin` 只重钉 policy owner 来源，main `e0663a8c85f055c2bac5af894070fea8e24ff3ce`、artifact SHA-256 `70cc9704ecf6f61d616011a72447ff3df8c209b3a769d5e4009692b81329e96f`，route/header/cookie 语义不变；Web 唯一 writer `web_policy_bff_pin` 固定相同只读 artifact，main `d11768842d8b6eb7ca28a7cefc53a8ba8187e8ea`。Root 只改既有首登/三仓 smoke 和相邻测试：IAM test-only NDJSON `invite-verified` 经正式 internal owner invitation 创建，verified user 经 issuer 正式 accept；旧禁用的 Better Auth invitation endpoint 不再调用。Root 聚焦 28 passed/67 subtests；IAM 真 PG/Redis integration 23/23；BFF `pnpm check` 276 pass/1 skip；Web contract 57、architecture 34、lint、单 worker 全量 1424、隔离 typecheck/build PASS。Web 2-worker 全量曾出现一次 RP callback 500 的并发波动，聚焦 38/38 与单 worker 全量复跑通过；保留证据不掩盖。Root 下一门须提交三 gitlink/库存并跑固定 SHA 独立 HTTPS 首登→OIDC/Product Session/refresh，真实失败继续定位；3310 Web-only 进程不动。


W1C-FIXED-TENANT-SMOKE-E 真组合验收（2026-09-24，已验收本切片）：Root `b2ab78eb` 固定 IAM `7f39193fff97dbb1398cb536ded7dca0db354213`、BFF `e0663a8c85f055c2bac5af894070fea8e24ff3ce`、Web `d11768842d8b6eb7ca28a7cefc53a8ba8187e8ea`，执行 `scripts/e2e/run_web_bff_iam_product_session_smoke.py`（独立 HTTPS/真实 PG+Redis）返回 `status=passed`、`first_login=smtp_verified_then_oidc`、`product_session=active_then_ended`、`product_chat_proxy=verified`、`owned_resources_remaining=0`。旧禁用 Better Auth invite 404 已由 IAM 正式 internal invitation + issuer accept 路径替代；新验证用户进入**既有固定 tenant**，Web RP 对 BFF `/v1/me` 进行 callback/refresh 在线校验。Web 后续 `0d180225` 只更新 CURRENT 证据，Root 需重钉该 docs-only SHA 并复验来源/真烟测。此验收不包括用户 3310 Web-only 常驻入口、Team Product 写、Agent 聊天、支付或全部 Wave 0–7；保持后续任务开放。

R2c-FIXED-TENANT-CHROMIUM 任务卡（2026-09-24，Root 本片已真组合通过）：Owner 为 Root 跨仓 E2E，唯一 writer Root；基线 Root `6c9a4f464bcc0583070873c52f84146cbc217e25`，Web `0d180225`、BFF `e0663a8`、IAM `7f39193`、Agent `520ec181`，起始均 main/clean。范围只含既有 `scripts/e2e/run_web_chat_chromium_smoke.py`、`web_chat_chromium.mjs`、`run_web_chat_worker_smoke.py`、`run_web_bff_iam_product_session_smoke.py`、`run_bff_agent_worker_smoke.py`、相邻 Root tests 和本台账；不改子仓 API/Schema/生成契约、用户 3310 或共享服务。当前事实：旧 Chromium 仍等待已删除的租户下拉/POST，四仓来源钉在旧 SHA；Web 固定 RP 现在要求 `KOKORO_TENANT_ID`，旧组合没传；Agent worker 会在首轮前创建 request stream，失败清理没登记；外租户 C 不能取得固定 Product Session。采用复用现有 Root 编排/浏览器脚本并更新精确来源、固定 tenant env、GET-only 续接、失败清理与 A/B/C 证据（不建新入口）；淘汰恢复旧选择 UI、假登录、让外租户取得 Product Session 或删掉 C 负例。无新的业务数据/API；A 真 Chromium 登录→DOM Chat→AG-UI 恢复→reload，B 同 tenant 私有 404，C 在固定租户准入前 403 且无 Product Session；每次运行只清理自有 PG/Redis/进程。验收：先聚焦 RED→GREEN，再 Ruff/Root pytest/真隔离 Chromium/资源归零，提交后 Root 来源、topology/main-only 门；常驻 3310 仍独立未配置。

## W1C-Team-R5：固定租户成员/邀请写入闭环（2026-09-24，进行中）

| 项 | 裁决 |
| --- | --- |
| Owner/基线 | IAM 是 Member/Invitation/Role 唯一 writer，BFF 是 public Product 投影 owner，Web 是同源交互 owner；Root `68fdb66a`、IAM `7f39193`、BFF `e0663a8`、Web `0d180225`，均 main/clean。Root 保留跨仓顺序、审查和最终验收。 |
| 当前事实 | IAM internal OpenAPI/SDK 已有 invitation create/resend/cancel、member remove/roles replace/leave 与 verified issuer invitation accept/reject；Web OIDC fixture/client 只获 Team 三读 scope，BFF Team public 只有三 GET；Web 旧 `/api/team/*` 仍以 sealed team-session/principal 直连 IAM `/bff/*`，与当前 Product Session 不同轨。 |
| 目标职责 | 固定部署 tenant 的正式 Team 页面：经 Web Product Session→BFF user Bearer→IAM user-delegated scope/当前 membership/permission 完成邀请和成员管理；本人通过 IAM verified issuer link 收/拒邀请。无 tenant 切换、全局团队目录、成员间聊天/文件自动共享；角色管理仅承接页面真实的 owner/admin/member 操作，不借此扩张动态角色编辑。 |
| 放置/粒度 | 先在 IAM 现有 test-owned Web OAuth client fixture 和相邻测试补 `iam:member.write`/`iam:invitation.write`（采用；不改 owner schema/API）；再由 BFF 在现有 `team.ts`/IAM generated client 边界发布窄 Product mutation/OpenAPI/测试（采用，零 Team SQL/Redis）；最后 Web 原子替换现有 `src/team/client.ts`、`/api/team/*` 与 Team UI 到 Product 同源 adapter（采用）。淘汰在 Web 延长旧 sealed session、BFF 复制 IAM 表或另起 Team gateway。 |
| 依赖/API/数据 | IAM owner 写操作、稳定错误、source/digest 先核实并固定；BFF 不能把 IAM 409 一律压成 403，须按 owner 错误语义分别处理 `LAST_OWNER`、`INVITATION_CONFLICT` 等。邀请 create pending 唯一索引、Member Serializable 条件写和 IAM 审计仍由 IAM 负责；跨仓不共享 SQL。Web 申请 write scope 要等 IAM client 与 BFF 投影提交；Root 的 `SCOPES`/真实 OAuth fixture 同步重钉。 |
| 删除/验证 | 删除旧 `/api/team/{context,[...path]}`、sealed team-session 依赖、旧 `/bff/*` 消费及切换/namespace 残余，不保留 fallback。IAM Node24 `pnpm verify`+真实 OAuth/PG；BFF Node22 `pnpm check`+真 IAM HTTP；Web Node22 contract/architecture/lint/typecheck/test/build/Playwright；Root 固定 SHA 完整邀请→accept→读成员、改角/移除/最后 Owner 拒绝及默认私有负例，资源清零/main-only。 |

R5-IAM-SCOPE 第一张实施卡：唯一 IAM writer 只改 `apps/kokoro-iam/test/fixtures/internal-http-application.ts` 与 `apps/kokoro-iam/test/integration/web-oidc-flow-host.test.ts`，不改 runtime、机器 OpenAPI/SDK、Schema、BFF/Web/Root Git index；Root 当前 IAM `7f39193fff97dbb1398cb536ded7dca0db354213` 为起点并负责审查/提交。先添加失败断言，再让测试自有第一方 Web OIDC client 精确注册并签发两个既有 user-delegated write scope，负例不得让 machine/client-credentials 获得这些 scope。完成后交文件清单、测试实测、风险；Root 在冻结 IAM 工作树复验后串行提交。此卡只放通后续正式 Team 管理的测试 client，不单独宣称 Product 写能力完成。

R5-IAM-SCOPE 验收（2026-09-24）：IAM main `ad5224a9e0a3a31d1c593d214d37940d6923b2e7` 已提交并推送。唯一变更是测试自有 Web OAuth client 注册 `iam:member.write`、`iam:invitation.write` 和真实 Code+PKCE/claims/非 machine 负例；生产 IAM API、Schema、契约、角色语义均未改。Root 独立 Node24 `pnpm verify` exit0（确定性单测 741/741；breaking 检查既有 400 warning、0 error），显式复用已有 PG/Redis 的 `web-oidc-flow-host.test.ts` 24/24。此切片不是 BFF public 写或 Web Team 闭环。

R5-TEAM-SAME-ORIGIN-HTTP（2026-09-25，Root 独立验证切片）：复用既有 test-owned Web→BFF→IAM HTTPS Product Session runner，在现有 `/api/team/*` 同源入口增加匿名 401、已登录成员/角色读取、普通成员邀请列表 403、跨域及自报 tenant 的 mutation Web 侧阻断断言。固定 Web `63aca94`、BFF `da03b76`、IAM `ad5224a`，真实 PG/Redis、SMTP→OIDC→Product Session 组合 PASS，自有资源 0。该 runner 的旧 HTTP proxy 拒绝 chunked mutation，故 owner 写权限不以此门宣称真浏览器通过；BFF/IAM owner 写回归由各仓测试和既有 Team owner HTTP runner负责。后续仍须正式 Chromium Team DOM 与邀请邮件入口，不启动或改动用户 3310。

R5-INVITE-IAM-DESIGN（2026-09-25，进行中）：IAM 是 Invitation/recipient/issuer Session 唯一事实 owner；负责人 `r5_iam_invitation_contract_audit` 只写 `apps/kokoro-iam/docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md`，Root 审查并串行提交，基线 IAM main `ad5224a9e0a3a31d1c593d214d37940d6923b2e7` clean。目标是独立邮件点击旅程的三面文档门：已验证 issuer Session 的窄 context GET、固定 tenant、邀请收件邮箱/expiry/状态、已存在与新邮箱注册验证登录、accept/reject、邮件 canonical path，以及动态路径与 browser-private relay 的契约边界；现有 Prisma schema 无新增表/索引则明示。比较扩 Better Auth 宽 get-invitation、直接复用已入组 Product Team API、IAM owner 窄 GET 三案，采用 owner 窄 GET；不改 runtime/机器契约/Schema/其他仓，不触 3310。Root 同时只读核对 BFF 静态 relay 能否承载精确动态路径，不并发写 IAM；设计审查、`git diff --check`、内部三文档一致性是本片门，正式代码和真 PG/Chromium 属后续 owner 顺序切片。

R5-INVITE-IAM-CONTEXT 代码卡（2026-09-25，设计门已过，待 owner 实施）：同一 IAM 负责人继续独占 `apps/kokoro-iam`，Root 保留 Git index/提交。允许改现有 `src/modules/tenancy/organization/invitations/{invitation-responses.controller,invitations.service,invitations.repository,invitations.schema}.ts`、必要的现有 IAM guard/config/Better Auth 邮件 callback、相邻 unit/integration/contract 测试、owner 机器 OpenAPI 与生成 SDK、三文档及 CURRENT；不得改 Prisma schema、BFF/Web/Root 运行代码、共享基础设施或服务进程。先 RED 后 GREEN：实现 verified issuer Session + 受信 Origin + 固定 Product tenant 的 `GET .../context`，仅匹配收件人 active pending 可见最小字段，过期/错人/未知遮蔽按设计；两处正式/遗留邀请邮件 URL 一并切到 `/iam/interactions/invitation?id=`，无旧 alias。生成 owner contract 并审查浏览器私有 operation 的 SDK 表面，不手改生成物。验证 `pnpm format:check/lint/typecheck/contract:check/test/build`、真 PG/Redis `invitation-lifecycle`、SMTP 邮件 URL/错误/无写测试；owner 只交文件和实测，Root 复验再提交/pin。BFF 精确动态 relay 与 Web 新用户注册/表单属后续串行切片，不能据 IAM 代码片宣称邮件点击闭环。

R5-INVITE-IAM-CONTEXT 范围补充：IAM 机器 OpenAPI/SDK 由 0.3.0 增量发布到 0.4.0 后，允许同一 IAM writer 只在 `apps/kokoro-iam/contract/README.md` 定点校正历史 0.2.0 版本说明；不扩大为文档重写或改其他 contract。Root 复核版本与生成结果一致。

R5-INVITE-IAM-CONTEXT 审查门补充（2026-09-25）：Root 独立复跑 IAM `pnpm verify` 和真 PostgreSQL/Redis invitation-lifecycle+SMTP 聚焦 13/13，均 exit0；只读审查仍发现新 context GET 未进入任何限流路径（P1），动态角色 permission 损坏未按文档 503、guard 未拒绝 Idempotency-Key、OpenAPI 未声明实际安全/追踪响应头（P2）。IAM 唯一 writer 继续修复并补耗尽/限流依赖失败/损坏权限/契约测试；修复、Root 重验、提交前本卡保持进行中。BFF 独立只读审查仅准备后续精确动态 relay 设计，不并发写 IAM，也不以本仓测试替代邮件点击浏览器闭环。

R5-INVITE-IAM-CONTEXT owner 交付（2026-09-25）：IAM main `fc3170148543e5f3f6d094399ac0ba4e14c0fe61` 已由 Root 审查提交并推送，工作树 clean。独立复验 `pnpm verify` exit0（86 files/741 tests、format/lint/typecheck/contract/SDK/build）；真 PostgreSQL/Redis `pnpm test:integration` exit0（34 files/248 tests）。限流 30/60 秒、存储失败 503、动态权限损坏 503、Idempotency-Key 拒绝与 OpenAPI 响应头均已补测。BFF/Web 尚在旧 owner 来源，Root 暂不 pin 新 IAM gitlink 造成静态来源门虚绿；下一步先完成 BFF 三文档门与精确 relay，再按 IAM→BFF→Web 顺序 pin。

R5-INVITE-IAM-CONTEXT 机器可见性补片：Root 发现 accept/reject 原有 OpenAPI 没有 owner/visibility/stability/idempotency 扩展，IAM main `ac94f152daffa2293801ea4f56f98b3ae59452d7` 已为三条 invitation operation 统一补齐并发布 0.4.0 SHA-256 `a18d57172df841cb2f55aa845a3eeb519ddb5abc8bea1c2be74fbb7e0fb62416`。Root `pnpm verify` **742/742**、format/lint/typecheck/contract/SDK/build 通过；仅 machine metadata 变更，前一 IAM 全集成 **248/248** 是业务代码提交 `fc31701` 的证据，不冒称在 `ac94f15` 又重跑全集成。

R5-INVITE-BFF-RELAY 文档门：BFF main `d5ba4d03b1470ad08dfcbb90bc02c441c1275c3e` 已提交并推送四份现有设计/CURRENT（IAM 最终来源 `ac94f15`），明确静态窄 sign-up、三条独立动态 invite、验证邮件 success/四枚举 error 精确回跳、单租户/issuer Session/无 Product admission/无 SQL 及 policy 2.1.0 字段。BFF 唯一 writer 正在先 RED 再做机器 policy/vendor/client/relay 和 HTTP 负例；Root 并行只写自身 `scripts/governance/iam_relay_policy.py` 与相邻测试，保持旧静态子集门并对 2.1.0 精确核 owner OpenAPI/三模板/静态路由/错误枚举。两仓当前实现均未提交，不代表 Web 页面可用。

R5-INVITE-BFF-RELAY 下一 owner 卡（IAM owner 提交后派工）：BFF 基线 `da03b76e450018ffa00f812da461569a00a377b3` main/clean，Root 保留 index/提交，唯一 BFF writer 不碰 IAM/Web。当前 `/iam` 在 `src/bootstrap/server.ts` 先于 Product admission 分发，`iamRelayRoute` 与 Root verifier 只接受 Better Auth 静态 `AUTH_ROUTES`；IAM 邀请 context/accept/reject 是 Nest 动态 Controller，不可塞进该静态 map。比较「扩静态 policy 通配」与「同一 relay 中独立具名精确动态 matcher」，选择后者；只允许配置中固定 tenant、canonical 小写 UUID、`GET context`/`POST accept|reject`，拒 query/fragment/编码别名/多余段/方法、Authorization/Idempotency-Key、自报 tenant 与不适用的 body。三路要求精确 Web Origin、经过现有白名单过滤的非空 issuer Session Cookie 和现有服务身份；Web POST 还需自身一次性 CSRF。复用现有 transport 的大小/时间/取消门，响应限原生 status 与已批准头，所有结果 no-store/no-referrer/request-id；429 合法 Retry-After，禁泄露上游原文和开放重定向。IAM 邮箱验证返回新 `/iam/interactions/invitation?id=<UUID>` 时，还须在 `allowedLocation` 加**精确同源静态交互路径+唯一规范 ID**例外，否则现有 BFF 返回 502；不把 Web 页面加入 IAM owner route map。BFF 无新 SQL/Redis/Team Product endpoint。先把 BFF `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 与 IAM 已提交 0.4.0 机器契约对齐；重钉 IAM owner blob/vendor/generated 客户端来源，保留旧 Better Auth 静态子集门，Root verifier 另验三条精确动态模板/visibility/method 与篡改负例。单仓 RED→GREEN、Node22 `pnpm check`、Root 真 HTTPS 邮件点击与资源清零后方可标 BFF 接通；Web 消费与新邮箱浏览器旅程仍在后续切片。

R5-INVITE-BFF-RELAY 新邮箱依赖补充：Web 只读审查确认 IAM 静态 `AUTH_ROUTES` 已有 `POST /sign-up/email`，但 BFF policy 尚未准入，新邮箱无法独立响应邀请。BFF 本切片需另以**精确静态路由**加入 sign-up（与三条 Nest 动态 route 分开），只接受有界 name/email/password 和 Web server 构造的同源 canonical 邀请 callbackURL，严格 Origin/服务身份/响应 Location，不提供通用注册代理或外域回跳；Web 用一次性 CSRF 与真实表单消费。BFF/Web 双层还要对 IAM verify-email 回 `/iam/interactions/invitation?id=<UUID>` 放行同源唯一规范 ID。未补这条链时，不能因 context/accept/reject 三路通过而宣称新邮箱邀请闭环。

R5-INVITE-WEB-ENTRY 后续消费卡（须待 BFF 2.1.0 owner commit）：Web 基线 main `63aca94f93095722425340a0a95985e8796a5b33` clean；唯一 Web writer、Root 管 Git index/commit，不碰 IAM/BFF/Root 实现或用户 3310 进程。先将 Web 三设计文档的当前/目标、无 Web SQL 和单租户调用边界对齐固定 BFF policy artifact，再新建唯一静态 `/iam/interactions/invitation` Route Handler；不扩 `/iam/[...path]` 通配、不重用 OAuth 签名专属 `/auth/sign-in` 或已入组 Product `/login` 作为未入组入口。已有邮箱：邮件点击→独立真实 issuer 登录表单→verified Session context→组织/角色/到期预览→一次性 CSRF accept/reject；新邮箱：独立注册→SMTP 验证精确回跳→重新登录→同一预览/应答。accept **200** 后才启动 Product `/login`，reject 只显示完成；未知写结果不盲重试或据终态 404 假定成功。Web server-only service credential 只去 BFF，固定 tenant 从配置取，浏览器不报 tenant/actor；扩现有 CSRF/relay response 的精确邀请路径、no-store/no-referrer、四枚举验证失败固定文案、禁止 token/密码/原始上游错误入页面或日志。删除旧 `/auth/invitation` 假路径，不保留 alias/整页连接中重试。Web Node22 contract/architecture/lint/typecheck/test/build/e2e，Root 用真实 PostgreSQL/Redis/SMTP/HTTPS Chromium 覆盖已有/新邮箱、错误/错人/过期/并发、issuer Cookie Path 与 Product OIDC，测试自有资源归零后再宣布浏览器闭环。

R5-BFF-TEAM-WRITE 任务卡（2026-09-24，待派工）：Owner `kokoro-bff` public Product Team 投影，IAM 已发布 `ad5224a`（机器 internal OpenAPI 0.3.0 字节不变）；BFF 起点 `e0663a8c85f055c2bac5af894070fea8e24ff3ce` main/clean，Root 独占 BFF Git index/commit。唯一 BFF writer 可改本仓三设计文档/CURRENT、public OpenAPI、既有 Team route/IAM client/server 分发、IAM generated-client 生成配置/manifest/vendor/snapshots、相邻 unit/contract/integration/architecture 测试；不得改 IAM/Web/Root、BFF Team SQL/Redis、relay identity policy 或共享进程。先在 BFF 三设计文档明确当前三 GET/目标六写，机器 contract 再实现：`POST /v1/team/invitations`、`POST /v1/team/invitations/{invitation_id}/resend`、`DELETE /v1/team/invitations/{invitation_id}`、`PUT /v1/team/members/{member_id}/roles`、`DELETE /v1/team/members/{member_id}`、`DELETE /v1/team/members/me`。直接以经普通 Product admission 的当前 tenant 与 User Bearer 调 IAM owner 对应操作；body 只允许 IAM 已定义的 email/roles，禁止 actor/tenant 自报。`/me` 路由先于动态 member ID；生成 schema 校验 owner 成功/错误、上游限额/取消、no-store/request ID；owner `LAST_OWNER`、`INVITATION_CONFLICT`、`ROLE_NOT_FOUND` 等业务 409 不压成 403，也不对可能已提交的非幂等写盲目重试。成功按 IAM data 返回 BFF Product envelope；失败保持稳定业务码/status，未知或形状漂移 fail closed。public OpenAPI 的权限/错误/请求/响应/幂等语义与测试一致；不伪称 IAM 提供 idempotency receipt。先 RED 后 GREEN，本仓 Node22 `pnpm check`，Root 重跑 owner 组合后才放行。先提交 IAM owner 来源 pin，再轮到 Web 消费；未经过当前固定租户真实链路验证之前 BFF 写只能标为本仓通过。

R5-BFF-TEAM-WRITE 范围补充：唯一 BFF writer `r5_bff_team_write_owner` 正执行，可在现有 relay policy JSON/TS 只重钉 `iamOwnerCommit=ad5224a`，不改 version/route/header/cookie 语义；生成 IAM HTTP client 的 owner 来源依赖可按实际治理门重钉同 digest 并重生。允许改既有 `scripts/check-contract.mjs` 与相邻 governance test，对上述**六个精确 operationId** 声明 `x-kokoro-idempotency: none` 的窄例外；其它非 GET 仍强制 required。由于 IAM 没有 mutation receipt，BFF 不宣称幂等重放，客户端不得自动重试不确定结果，而需刷新/读回；例外必须先 RED 后 GREEN。Root 不并发写 BFF。

R5-Web-Team 只读审查结论（2026-09-24）：旧 `/api/team/context`、`currentNamespace`、`listMyTeams` 与 team-name rail 依赖跨租户旧模型，固定部署应删除；现有三 GET 是当前 tenant 的管理列表，**不是被邀请人的收件箱**。成员操作须由 `user_id` 改用 `member_id`，展示 `roles[]` 且不默认拥有其他成员 email/status；邀请用 `invitation_id/email/roles[]`，分页按 owner cursor。旧 `/api/team/[...path]` 直连 IAM `/bff/*` 及 sealed team-session，后续 Web 在 BFF 六写发布后一次性换成 Product Session→BFF，旧 auth/session-envelope 仍有别的消费者，不能在 Team 片误删整文件。BFF 409 `LAST_OWNER`/`INVITATION_CONFLICT` 需 UI 稳定呈现，UI 隐藏按钮不能替代 IAM 授权。只读 Agent 未改代码。

R5-INVITE-LINK 新增独立闭环门（2026-09-24，owner-first 设计中）：IAM 真实邀请邮件当前指向 `${IAM_WEB_ORIGIN}/auth/invitation?id=...`，Web 没有 `/auth/invitation`；BFF relay policy 2.0.0 也未发布 issuer Session accept/reject。被邀请者加入前没有固定 tenant Product Session，因此不能让现有 BFF Product Team 三 GET/六写或 `/login` 已入组闸假装是收件箱。先由 IAM 设计审查确认邮件 link 的固定 tenant 上下文、未登录/未验证邮箱/issuer Session/Origin-CSRF/接受或拒绝状态机；再按 IAM owner→BFF 精确 browser-private transport→Web 真实邮件入口/表单顺序发布。不得把内部 service secret 发给浏览器、恢复旧 Team `/bff/*`、构造假 Product Session 或为了表面闭环删除真实邀请功能。完成门为同一固定 tenant 邮件点击、未登录与已登录、accept/reject、过期/错误邮箱/重复提交、随后 Product OIDC，真实 PostgreSQL/Redis/Chromium 与资源清零；需要先通过三文档/API/SQL 门。`r5_iam_invite_entry_design` 只读审查中。

R5-INVITE-LINK 只读设计发现（2026-09-24）：IAM issuer Cookie 固定 `Path=/iam`，所以邮件旧 `/auth/invitation?id=` 即使新建 SSR 也收不到 Session；正式 canonical 邮件 URL 应由 IAM owner 改至 Web `/iam/interactions/invitation?id=...`，Web 静态 interaction route 负责有界 ID/issuer Cookie/Origin/一次性 CSRF/表单，Web→BFF 仅精确 browser-private relay IAM accept/reject，不走已入组 Product admission。IAM 已有 issuer accept/reject 写及 recipient email/expiry/tenant 校验；现有 `/auth/sign-in` 绑定 OAuth query 并自动 continue，不能拿来作邀请独立登录。Better Auth 宽 `get-invitation` 未保证 verified 且跨 tenant 字段过多，不作为窄详情；若页面需展示 tenant 名/角色/到期，IAM 须新增已验证 issuer Session 的窄 invitation-context GET（已有邀请 PK，可复用现有 schema，无新索引），再由 BFF/Web 消费。需同时处理未登录注册/邮箱验证入口及受限 callback，不能直接打开 Better Auth 任意路由。接受成功后才启动 Product `/login`；拒绝不建立成员关系，应显示完成状态，不宣称会取得 Product Session。当前为设计发现，非已发布代码或完成验收。

R5-INVITE-LINK 产品裁决：正式邀请不能让用户盲确认。采用 IAM owner 窄 context GET（仅 verified issuer Session、当前 fixed tenant/recipient/pending/expiry，返回受限组织名/角色/到期），不开放 Better Auth 宽 `get-invitation`。邀请允许尚未有账号的新邮箱：必须经受限 issuer 注册→SMTP 验证→重新凭据登录→context→accept，不能以测试 host 的预置用户替代真实浏览器流程；对已存在用户保留直接 issuer sign-in。IAM/BFF/Web 三仓按 owner contract 顺序另派小切片，拒绝后只显示结果，无 Product Session。此裁决仅限定当前单租户部署的用户旅程，数据库不加新角色/新 tenant 配置体系。

R5-BFF-TEAM-WRITE 本仓验收与 Web 来源交接（2026-09-24）：BFF main `dd605c99e9bb5c6669ec31e04e285e5f92b79ed0` 已由 Root 审查提交并推送。Root Node22 `pnpm check` 282 pass/1 skip、`pnpm format:check`、IAM owner/vendored OpenAPI 0.3.0 字节一致；独立只读审查无 P0/P1，但指出幂等 `none` 豁免只按 operationId 的 P2，Root 以测试先 RED、随后改为 method/path/operationId 精确三元组，15/15 contract governance 与全 BFF check 复跑通过。public OpenAPI SHA-256 `5ac3c225193f70b99555e87f765c13128e43343929611f446fe744b77eef7954`，BFF relay policy 2.0.0 仅重钉 IAM `ad5224a`、artifact SHA-256 `74893ba4e566e4824a278cd3ee1548030a33435f9b37b7026a8a7e943c080037`，无新 Team SQL/Redis/receipt。真实 IAM HTTP mutation 和 Web 浏览器尚未复验，不能宣布 R5 闭环。

R5-Web-policy-pin 任务卡（2026-09-24，待派工）：Owner `kokoro-app` browser-private IAM relay 消费快照；Web 起点 `0d1802250f94c2ac0f3dd28b5b486ca92a976a1c` main/clean，BFF owner `dd605c9`，IAM `ad5224a`。唯一 Web writer 只重钉 BFF `contract/iam-relay-policy.json` 的字节原样快照到 `src/generated/iam-relay-policy.json`、`src/lib/server/iam-relay-policy.ts` 来源/expected IAM SHA、相邻 contract test 和三设计文档/CURRENT 的当前来源段落；不改 policy 2.0.0 的 routes/headers/cookies/version/业务逻辑，也不改 Web Team UI/API/OIDC scope、Root/BFF/IAM、用户 3310 进程。先 RED（旧 pin 不匹配）、后 GREEN；Web Node22 `pnpm contract && pnpm test:architecture && pnpm lint && pnpm typecheck && pnpm test`，build 在隔离输出目录以免触共享 3310 `.next`。Root 审查、提交、pin gitlink/库存/来源门和真组合后，再派 Web Team Product cutover。

R5-Root-Team-HTTP 实组合卡（2026-09-24，Root 唯一 writer）：在既有 `scripts/e2e/run_bff_iam_oidc_smoke.py` 与相邻 `scripts/tests/test_bff_iam_oidc_smoke.py` 扩展固定 IAM `ad5224a`、BFF `dd605c9` 的隔离真实 PG/Redis/HTTP 验证；先让 OAuth Code+PKCE 获得刚发布的两项 write scope，再用 Product Bearer 对 IAM-backed BFF 六写执行邀请 create/resend/cancel、成员 roles replace/remove、最后 Owner leave 冲突；外租户/缺 Bearer 不可写。只读/写入对象由 test-owned IAM host 产生，Root 仅清理自有资源，禁止触用户 3310。先单测 RED→GREEN，再固定 SHA 真组合、Ruff/Root pytest；不据假 IAM HTTP 单测冒充真实 owner 链，不把此脚本当 Web browser/邮件点击已完成。

R5-IAM-BFF-TEAM-HTTP 验收（2026-09-24）：IAM `ad5224a9` test-owned Web OAuth client 两项 user-delegated 写 scope 已通过 Node24 741/741 与真 PG/Redis host 24/24；BFF 六写 `dd605c9` 发布后，Root 真 HTTP 首轮发现 IAM mutation 200 无 `Cache-Control`，BFF 错判为 502。BFF 唯一 writer 限定修复 `iam-team.ts` 与 `team-write.test.ts`，GET 仍要求 owner no-store，mutation 要求合法 JSON/request-id 但不假设 upstream cache header，BFF public 回包仍 no-store；RED→GREEN、Node22 283 pass/1 skip、`format:check` PASS，Root 审查提交 main `da03b76e450018ffa00f812da461569a00a377b3`。Root 已固定 Web `f86aefe`/BFF `da03b76`/IAM `ad5224a` 于 superproject `39383550`。真 Team HTTP OAuth Code+PKCE→BFF→IAM create/resend/cancel/roles/remove/LAST_OWNER、缺 Bearer 401、外租户 fixed-tenant set-active 403，最终 30 case PASS、自有资源剩余0。当前外租户门是登录时 403，**不**冒称已取得外租户 Token 并测试 Product Team route；Web Team Product UI/邀请链接仍待后续独立切片。Web policy pin 仅来源，版本/路由语义未变。

R5-Web-Team-Product-cutover 下一写入卡（待派工）：Owner `kokoro-app`，Web 唯一 writer，Root 负责审查/提交/真浏览器。基线 Web main `f86aefe7ea3e48bc7ac1e4f1aea99bbe6f15feb6` clean、Root pin `39383550`；BFF public OpenAPI commit `da03b76`/SHA `5ac3c225193f70b99555e87f765c13128e43343929611f446fe744b77eef7954`，IAM `ad5224a`。范围限现有 `src/team/client.ts`、`src/app/api/team/*`、`src/ui/team` 与相邻 auth Product Session/OIDC scope、测试和必要四份设计文档；先盘点并提交第8节放置表/旧功能映射，再实现固定租户 Product Session→BFF 三 GET/六写，删除 sealed team-session/旧 IAM `/bff/*`/切换租户 UI，但不删 auth/session-envelope 中其他用户。成员 `member_id` 与 `roles[]`、邀请 `invitation_id`、分页/error/无自动 retry、本人私有和权限失败必须严格按 owner wire。issuer 邀请链接/未入组注册属于 R5-INVITE-LINK，不能用管理邀请 GET 冒充 inbox。先 RED 后 GREEN，Web Node22 contract/architecture/lint/typecheck/test/build（隔离输出）与 Root 真实浏览器再放行；不触 3310 常驻进程。

R5-Web-Team-OIDC-scope 子切片验收（2026-09-24）：Web 唯一 writer 在 main `f86aefe` 上仅改 6 个既有文件，将 RP 的固定请求 scope 在三项 Team read 后精确追加 IAM `ad5224a` 已授权的 `iam:member.write`、`iam:invitation.write`；Auth.js Location 仍要求有序完整唯一，缺项/额外项/重复项拒绝。聚焦先 RED 2 fail、GREEN 13/13；Web contract57、architecture34、lint、全量 Vitest1426、隔离 typecheck/build PASS。Root 独立聚焦 13/13，审查并提交 Web main `732de58`。这只闭合 OIDC 申请边界，不改旧 Team API/UI，不冒称团队操作可用；下一片继续原子迁移 `src/team/client.ts`、同源 route 与 UI。用户3310进程/.next 未触。

R5-INVITE-LINK owner-first 审查验收（2026-09-24，只读未改仓）：IAM `ad5224a` 的正式邀请邮件在 `invitations.service.ts` 仍指向 Web `/auth/invitation?id=`；issuer Cookie 在 `better-auth.config.ts` 固定 Path=`/iam`，而 Web 无该 route，旧链接当前不可用。IAM `InvitationSessionGuard` 已检查受信 Origin、Session 与已验证邮箱；accept/reject 使用 invitation PK、tenant/recipient/expiry/pending 条件与 Serializable 事务，当前 Prisma 邀请表和索引可复用，不为 context GET 新建表。新邮箱 IAM sign-up/email、verify-email、sign-in/email 与 SMTP owner integration 已存在，但 BFF policy 2.0.0 未准入 sign-up、context、accept/reject，Web OAuth-bound `/auth/sign-in` 不能作为独立邀请登录页。下一 owner 写入卡：IAM 三文档先明确当前/目标，再在同一 invitation 模块增已验证 issuer Session 的窄 context GET（仅 pending 邀请的 tenant name/roles/expiry，错人/不存在不泄露）并把邮件链接改至 `/iam/interactions/invitation?id=`，扩机器 OpenAPI/真 PG/SMTP/负例；BFF 和 Web 按提交的 owner contract 串行消费。**协议待裁决点：**现有 BFF IAM relay policy 只认识静态 Better Auth `AUTH_ROUTES` 且 Root verifier 按该清单校验，动态 `/iam/v1/tenants/:tenant_id/invitations/:id/...` 不能直接塞进现有静态 routes 假装可用；IAM owner 发布 context 后须设计精确路径模板/固定 tenant/browser-private 独立入口及相应机器门，再由 BFF/Web 实现。禁止将宽 `organization/get-invitation` 开放给浏览器或把邀请 GET 混入已入组 Product Session。

R5-Web-Team-Product-cutover 执行控制更新：Web `732de58` 后的同仓唯一 writer 因生成 BFF Team client、旧 Team API/UI 原子迁移超出本轮等待时间被 Root 中断；当前 Web 工作树未提交，Root gitlink仍锁 `732de58`。先审查已写文件并保留有效成果；下一执行波要求定点阶段门和即时状态：① owner OpenAPI/Team generated client 重生与 contract 门，② 同源 Product Team route/client 的 RED→GREEN，③ UI/Settings/rail 旧模型原子删除，④ 本仓完整门＋Root 真浏览器。任一阶段出现阻断立即报告具体文件/命令，而不是在后台无限等待；最终门通过前禁止“Web Team 已闭环”表述。

R5-Web-Team-Product-cutover 当前验收（2026-09-25）：Root 接管唯一 Web writer，已完成上述①–④的本仓部分：BFF `da03b76` public OpenAPI SHA-256 `5ac3c225193f70b99555e87f765c13128e43343929611f446fe744b77eef7954` 固定生成 9 个 Team operation；旧 `/api/team/context`、namespace/inbox、IAM `/bff/*` 直连及 rail 团队名被替换；同源 Product Session→BFF user Bearer，UI 管理 `member_id`/`invitation_id`、角色数组和显式确认。独立只读审查发现的写成功后读刷新误报、跨页 actor/角色权限遗漏及邀请无法翻页已由 Root 修正，写成功读失败时阻断重复提交，成功离队不再依赖失效 membership 刷新。Node22 `pnpm check`：contract 69/69、architecture 34/34、lint、typecheck、Vitest 1459/1459、Next build 均 PASS；首次 build 的 generated `.js` 引用失败已改为 generator 无扩展名模块后复验通过。当时仍待提交 Web main、Root gitlink/库存重钉和隔离真浏览器 Team 场景；提交/pin 见下条。此时仍不标为跨仓闭环；IAM 邀请邮件入口另由 R5-INVITE-LINK owner-first 卡处理。

R5-Web-Team-Product-cutover 本仓放行记录：Web main `63aca94f93095722425340a0a95985e8796a5b33` 与 Root main `9e3fb3e602cc6ccd779424d3a77b9480fa1df618` 已按依赖顺序提交推送，Root pin/consumer inventory 更新；Root topology、checkpoint、IAM relay、main-only 四门 PASS，`python3 -m pytest -q scripts/tests` 696 passed/169 subtests。下一阶段仍是测试自有真实 Web→BFF→IAM Team 浏览器组合和独立邀请邮件入口，非本仓单测可替代。全仓 compatibility 仍有 11 个 broken edge + 1 个 illegal edge，ten-repository-standard 仍有 129 条，均保持开放而非把 Web 切片标为总体完成。

R5-INVITE-BFF-RELAY 代码交付（2026-09-25）：BFF main `d6dc8a0ea5a3fee7a4f54f01fefdeff0e28892e7` 已由 Root 复验、提交并推送；IAM 0.4.0 来源、policy 2.1.0、三条动态 invitation relay 和受限 sign-up 已落代码。独立审查未见 P0/P1，P2 的错误文案泄漏、可选 message 与邮箱/URI/时间格式已修复。Root Node22 `pnpm format:check && pnpm check` exit0（291 passed、1 skipped、0 failed、build 通过），`git diff --check` PASS。尚待 Root 精确 gitlink/policy 门与真实 IAM→BFF HTTP smoke；Web interaction/SMTP/Chromium 仍后续 owner 切片，本仓通过不等于用户邮件入口闭环。

R5-Root-INVITE-HTTP 验收（2026-09-25）：Root 固定 IAM `ac94f15`、BFF `d6dc8a0`，扩既有自清理 Code+PKCE runner，通过真实 PostgreSQL/Redis/HTTP 测收件人 issuer sign-in、两张邀请的 context/reject/accept 200，以及缺 Cookie、外租户、错误 Origin、非收件人、已消费 404 等拒绝；Root 独立复跑聚焦测试 33 passed/71 subtests，真实 runner 42 cases、资源剩余 0。仅证明 IAM→BFF，不包括 Web 静态页面、真实 SMTP 邮件点击或 Chromium；Web 仍按独立任务卡推进。

R5-INVITE-WEB-ENTRY A/B 交付（2026-09-25）：Web main `45388f620d2d255eee02073ffe5ef62102b6a430` 固定 BFF policy 2.1.0 与精确 verify-email 回跳；Web main `63a0c8523bc06a513ff897b9ed8326ac543a3895` 新增唯一静态邀请 GET、独立 issuer 登录/注册 POST、recipient-only context、一次性 CSRF、中文紧凑桌面/移动页。Root 独立 Node22 contract 69/69、architecture 34/34、全量 Vitest 1474/1474、lint/typecheck、隔离 production build exit0；原 `.next` 已原样恢复，临时 build 输出清理。预览的 accept/reject 真 POST 尚缺，Root 因此暂不 pin Web gitlink 或宣称三仓邮件旅程闭环；C 由同一 Web writer 继续。

R5-INVITE-WEB-REAL-HTTP 当前任务卡（2026-09-25，进行中）：owner 顺序 IAM→BFF→Web 已发布 main：IAM `7215223`（仅 test fixture SMTP 真实发信）、BFF `2f1fc33`（同字节 OpenAPI 与 policy 2.1.0 来源重钉）、Web `205c77b`（同字节 policy 消费重钉，正式邀请 GET/POST 的 C 代码基线 `5e4145a`）。Root 唯一跨仓验收 writer 只改 `scripts/e2e/run_web_bff_iam_invitation_smoke.py`、相邻 Root 测试、`docs/task.md`/`docs/progress.md`/`docs/CURRENT.md`、consumer inventory/gitlink；三子仓 index/提交由 Root 串行完成。真实 PG/Redis/SMTP/HTTPS runner 复用现有自清理 Product Session harness，至少证明已存在收件人从邮件精确链接进入 Web 表单→issuer sign-in→recipient context→accept/reject→接受后 Product OIDC，失败/重复提交 fail closed、自有资源 0；新账号注册/邮箱验证、Chromium DOM、过期/错邮箱和用户 3310 是后续独立门，不在本片结果中冒称通过。Root 脚本已加聚焦测试 4/4，真链待固定 gitlink 后复跑；只保留一个运行中的测试组合，不启动常驻服务。

R5-INVITE-WEB-REAL-HTTP 本片验收（2026-09-25）：Root main `0be2e385` 精确 pin IAM `7215223`、BFF `2f1fc33`、Web `205c77b`，来源 policy/checkpoint/topology PASS。新增 Root test-owned SMTP/HTTPS runner 与聚焦 4/4 单测，真实既有收件人邀请 accept 与 reject **分别 PASS**：逐字核对 owner 邮件链接、Web 独立 issuer 表单、BFF relay context、无 Product Session 预览、精确决定 POST、一次性 CSRF 重放 403、消费后 context 404；accept 进入 `/login` Product OIDC，reject 保持匿名，两次资源余量 0。Root runner 与最终记录随本片提交；本片不宣称新邮箱注册、Chromium DOM、错收件人/过期矩阵或 3310 常驻入口。

R5-INVITE-NEW-ACCOUNT 下一 Root 验收卡（2026-09-25，进行中）：Root `62c8da91` 与 IAM/BFF/Web `7215223`/`2f1fc33`/`205c77b` main/clean；Root 持有集成/Git，唯一代码 writer 子代理只扩现有 `scripts/e2e/run_web_bff_iam_invitation_smoke.py`、相邻 `scripts/tests/test_web_bff_iam_invitation_smoke.py` 和本台账，不改三 owner 运行代码或用户 3310。当前已有账号 accept/reject 的真 SMTP/HTTPS 已通过；新邮箱路径缺浏览器注册/验信证据。复用现有 test-owned IAM invitation、SMTP mailbox、隔离 Web/BFF/IAM/PG/Redis 生命周期，不新建平行 runner。目标：先有邀请后经 Web sign-up 表单（无 Product/issuer Session）→测试 SMTP 验证邮件精确回同一邀请 URL→未验证登录拒绝→验证后 issuer sign-in/context/accept→固定租户 Product OIDC；输出区分已有/新账号证据，资源0。若真实运行发现 owner 契约或页面缺口，按 IAM→BFF→Web 归属另开修复，不在 Root runner 伪造成功。

R5-INVITE-NEW-ACCOUNT 本片验收（2026-09-25）：唯一 Root runner writer 已交两文件并由 Root 提交 main `0d9ac3fb`；Root 独立 `ruff format --check`、`ruff check`、聚焦 pytest **6 passed/15 subtests**，并在固定 IAM `7215223`、BFF `2f1fc33`、Web `205c77b` 下逐次跑 `existing/new × accept/reject` 四个真实 PostgreSQL/Redis/SMTP/HTTPS 组合，均 exit0、自有资源余量0。新账号路径包含 Web sign-up 及一次性 CSRF 重放 403、未验证登录无 Session、验证邮件精确回原邀请、issuer 登录与 pending context；接受后 Product OIDC，拒绝保持匿名。只读规格/质量审查未见 P0/P1；Root 全量门已复验，已随本片提交。Chromium DOM、拒绝后成员资格负例、同一浏览器 Product 导航、错误收件人/过期矩阵和常驻 3310 不在本片结果中。

R5-INVITE-CHROMIUM-A 设计放置门（2026-09-25，待实施）：Owner 为 Root `scripts/e2e` 的三仓浏览器验收，唯一代码 writer 子代理、Root 负责审查/Git；基线 Root `0d9ac3fb`、IAM `7215223`、BFF `2f1fc33`、Web `205c77b` 均 main/clean。当前已有 HTTP CookieJar 的真实邮件四组合 runner 与 Web Chat Chromium TLS/Playwright harness，但没有邀请 DOM 证据。方案一在现有聊天 JS driver 中插入邀请流程（淘汰：把独立业务测试耦合进 AG-UI/Chat 状态机）；方案二扩现有邀请 Python runner，并在同一 `scripts/e2e/` 新建专用 Chromium JS driver（采用：唯一测试场景职责，复用已有随机 TLS 保留、host resolver、隔离 `.next` 和 finally 清理）。只读调用现有 IAM/BFF/Web owner contract，不新建应用进程类型、Schema/SQL/业务 API；禁止绕过 Web 同源 route 或用 fixture 直接植入 issuer/Product Session。先对已有账号的正式 SMTP 邀请完成同一 Chromium Context 页面渲染→凭据登录→真实预览→接受/拒绝，并要求接受后由 `/login` 继续 IAM 正式入口；新账号浏览器注册/验信与同一浏览器 Product Session 是后续 B 片，不拿 A 冒充全门。允许修改现有 Root 邀请 runner/相邻测试与新增 `scripts/e2e/web_invitation_chromium.mjs`；禁止改 `web_chat_chromium.mjs`、子仓、共享 `.next`、用户 3310。门禁：Ruff、Root pytest、JS 参数/DOM 测试、固定 SHA 真实 PG/Redis/SMTP/HTTPS/Chromium accept+reject、自有资源0；截图只作辅助证据。独立审查后 Root 提交。

R5-INVITE-REJECT-MEMBERSHIP 只读裁决（2026-09-25，待写入）：IAM `7215223` 的正式 Better Auth vendor snapshot 含 `GET /iam/organization/list`，BFF relay 2.1 不公开它；Root 只能在 test-owned IAM fixture 内复用拒绝时已有的 issuer Cookie，单次直达 IAM owner GET 并要求 HTTP 200/JSON 数组且固定 tenant ID 不在其中。401/429/503 或形状漂移都 fail closed；不靠新一次登录（同 IP 易触发限流）、不查询 IAM SQL、也不向正式 Web 加宽 `/organization/list` relay。该证据仅证明 owner 持久成员资格未授予，Web 层仍以拒绝完成页与无 Product Session 为独立可见证据。待 Chromium A writer 停止写现有 Root runner 后，由 Root 串行加入聚焦测试与真 reject 组合，避免并发编辑。

R5-INVITE-CHROMIUM-A / REJECT-MEMBERSHIP 验收（2026-09-25）：Root 串行接管已交付的邀请 runner 与 Chromium driver；真实浏览器首先发现 Web 邀请页 `no-referrer` 使同源表单 `Origin: null`/403，由 Web owner 精确改 `same-origin`，Web main `6290c11`、Root gitlink `e3272ac8`。之后固定 IAM `7215223`/BFF `2f1fc33`/Web `6290c11` 的 test-owned SMTP/PG/Redis/HTTPS/Chromium 已分别通过已有账号 accept 和 reject：同一 Chromium Context 有真实表单与 recipient-only 预览；accept 自动进入正式 IAM consent，确认后得到 Product Session；reject 保持 Product 匿名，两个终态均不可再作为 pending 邀请访问，资源余量0。HTTP CookieJar 的 existing reject 和 new reject 还复用当前 issuer Cookie 直查 IAM `/organization/list`，固定租户不在结果中；new accept 的验证邮件、Product OIDC 也在新 pin 下通过。此验收不等于新账号 Chromium、错收件人/过期矩阵或用户 3310 常驻可用。Root runner/文档/库存的最终提交与独立门禁见进度记录。

R5-INVITE-CHROMIUM-A 最终放行门（2026-09-25）：独立复核的两项 P1（Chromium reject 成员资格未验、失败日志潜在邀请 ID 泄漏）均修复；Chromium reject 现在直接核对 IAM owner 列表，且 JS/Python 只输出受控失败 stage。真实 accept/reject 重跑 exit0，自有资源0；Root Ruff、JS syntax、topology、contract checkpoint、全量 pytest **732 passed/184 subtests**；Web Node22 contract 69、architecture 34、Vitest 1483、lint、typecheck 与隔离 production build 通过。`test:e2e` 默认接管 3310，未执行；Root 十仓规范门仍有 135 条跨仓存量违规，不把本切片验收外推为全局闭环。下一 owner：Root 处理新账号 Chromium、错收件人/过期浏览器负例与正式 3310 可见入口；本切片提交后仍须记录精确 SHA。

W1-LOCAL-LOGIN-3310（2026-09-25，待提交）：Root main `65612422`、IAM `7215223`、BFF `2f1fc33`、Web `07a30fa` 的固定来源。Owner 是 Root `scripts/dev/serve_local_login.py` 前台开发验收启动器；Web/BFF/IAM 正式认证代码与 contract/SQL 不改，Root 保有共享 Git index。原目标 `http://127.0.0.1:3310` 经 IAM 唯一 writer 三层 TDD 查明被 Better Auth Web client `invalid_redirect_uri` 拒绝；IAM 临时改动已全部撤销。采用正式 OIDC 允许的非 loopback HTTPS origin `https://web-<run-id>.example.test:3310`，仅在独立 Chromium profile 映射到本机 127.0.0.1，TLS 例外限本次临时证书 SPKI；旧 IAB HTTP 标签不等于新入口。启动器复用自有 PG/Redis/IAM host/BFF/隔离 Next 生命周期，端口占用先拒绝、不杀他人进程；只从环境读取无密码的本地基础设施 URL，禁用 Chrome 代理，Chrome 提前退出即回收，重复 Ctrl-C 不打断清理。真实 HTTPS 3310 浏览器 `/login`→签名 IAM 邮箱/密码表单→临时账号→`/app` PASS；最终版本再经 Chromium `ignoreHTTPSErrors:false` 断言表单各 1、重试按钮 0。双 Ctrl-C 停止后端口、该 run 的进程/临时目录/PG/Redis 余量均 0。已有固定 SHA 三仓 SMTP/Product Session runner PASS、资源0。Root 全量 pytest 737 passed/184 subtests、拓扑 PASS、Ruff PASS；全仓标准门仍有 135 条存量违规。待门：提交后回归和最终可见入口；此切片不外推全部 Wave 0–7，Billing 仍最后。

W1-LOCAL-LOGIN-3310（提交后更新）：Root `569680405f15b7fbd2de0d932585bd0c4ba2fa5b` 已推送 main；main-only/clean PASS，当前联调 Chrome 的 HTTPS 3310 登录表单复验 PASS。当前前台是临时租户，正式单租户部署入口仍属后续集成任务；不将 fixture 代替整体产品闭环。

R5-INVITE-NEW-CHROMIUM（2026-09-25，待派工）：Root main `80406bcc`、IAM `7215223`、BFF `2f1fc33`、Web `07a30fa` clean；Root 是跨仓浏览器验收 owner，三子仓运行代码/contract/Schema 不改。现有 `scripts/e2e/run_web_bff_iam_invitation_smoke.py` 已有新账号 HTTP CookieJar 路径、已有账号 Chromium 路径，却在 `--account new --browser chromium` 明确报错；新账号真实 DOM 注册/邮件点击/登录/accept|reject 未证明。唯一写入 Agent 限改既有 runner、`web_invitation_chromium.mjs` 与相邻 `scripts/tests/test_web_bff_iam_invitation_smoke.py`，不得动 3310 前台、共享进程、子仓、Root Git index/分支/提交。采用复用本 runner 的 test-owned SMTP/PG/Redis/HTTPS 与 Chromium context（不建第二 runner）；先 RED，后以真实浏览器完成邀请邮件→注册→未验证登录拒绝→验证邮件精确回邀请→issuer 登录→收件人预览→accept/reject，accept 才可 Product OIDC，reject 保持非成员/匿名；明确新账号不同于已存在账号，可用不同页面阶段但不可用 CookieJar 代替新账号浏览器链。只清理本次资源，日志/证据不含 token、密码、邀请 ID。验收：聚焦 unit、Ruff、Node syntax、两种新账号 Chromium 真组合 exit0/资源0、Root 独立复验；若 owner 页面/契约缺口，先报告归属，不在 harness 假造成功。Root 审查与提交，之后再做错收件人/过期浏览器负例。此片不代表正式单租户部署入口或全 Wave 完成。

R5-INVITE-NEW-CHROMIUM 实测放行（待提交）：Root 唯一 writer 三文件实现后，独立只读审查指出的 Node 先退出但 Chrome 进程组残留、阻塞读取 stderr 和 consent DOM 竞态已修复，最新审查无 P0/P1。Root 聚焦 pytest **11 passed/18 subtests**、Ruff format/check、Node syntax、diff check PASS；按当前源码串行重跑新账号 Chromium accept/reject，均 `status=passed`、真实 SMTP 验信后返回原邀请、`owned_resources_remaining=0`，accept 有 Product Session，reject 无 Product Session 且 IAM owner 成员列表不含该租户。Root 全量 `scripts/tests` **739 passed/187 subtests**、拓扑与 W1B contract checkpoint JSON 均 PASS。旧 3310 前台未接管；错收件人/过期浏览器负例及正式租户常驻入口仍开放。待精确提交 SHA。

R5-INVITE-NEW-CHROMIUM 提交：Root main `655a99e1` 已提交并推送，提交后 main-only 12 仓 PASS、工作树 clean；本片固定来源未变，错收件人/过期 Chromium 仍待独立负例。

W1D-WEB-IAM-DIRECT-CUT（2026-09-25，下一 Web owner 切片）：Root `655a99e1`、Web main `07a30fa` clean。Owner 为 `kokoro-app` 的 Web RP/同源安全边界；IAM 仍唯一持有身份事实，BFF 唯一持有 `/iam` relay。当前 `src/lib/server/auth.ts` 是运行时唯一 `KOKORO_IAM_BASE_URL` 直连源，旧 magic-link/team-session/refresh/revoke 及 sealed `kokoro_session` 仅供旧 `/api/auth/logout|session-state`；正式 Auth.js `/api/auth/session|signout` 已独立承接，六条业务 route 只复用 `sameOriginOk`。目标是在不动正式 OIDC/Product Session/Redis CAS 的前提下，删除旧 `auth.ts`、`session-envelope.ts`、两条旧 auth route 和无效测试/配置文档引用；把 `sameOriginOk` 原语义迁到既有 `src/lib/server/` 下单责 `same-origin.ts` 并改六处 import。比较留下仅同源 guard 的瘦 `auth.ts`（名字继续误导，淘汰）与新同职责文件（采用）；不建新目录、第二 auth/RPC、兼容 alias、fallback 或 Web SQL。先由 Web 唯一 writer 收敛 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 当前态/目标态并经 Root 文档门，随后同一 writer 分代码切片：architecture 先 RED 断言生产源码没有直连 IAM/旧 route/封装 cookie，GREEN 删除；保留正式 Auth.js /logout/session 与六业务 route 的权限、CSRF、Bearer、同源错误行为。范围不含 BFF/IAM/Root运行代码、BFF public contract、Chat generated 消费或用户 3310 前台。验证 Web Node22 聚焦 RP/业务代理 + `pnpm check`、隔离 build/e2e，Root 真 HTTPS Product Session 登录/退出与 main-only/来源/compatibility JSON（仅此 illegal edge 应消失，11 broken 仍开放）。跨仓调用方向只 Web→BFF→IAM；无 Schema/新事务/缓存事实。Root 管共享 Web Git index、审查、提交、pin；Chat 生成消费随后串行，避免共同修改 session adapter。

W1-LOCAL-LOOPBACK-ENTRY（2026-09-25，登录入口优先修复，待 IAM 文档门）：Root main `73bdeda8`、IAM main `7215223` clean；Web main `07a30fa` 有 W1D 三份未提交设计文档，不能并发抢写 Web。用户实际打开的 `http://127.0.0.1:3310/login` 与正在运行的 HTTPS `.example.test` 临时联调入口不是同一 origin：HTTP 被 TLS listener 重置，普通 IAB 解析/证书也不具备专用 Chrome 的 host/SPKI 例外。已用 curl 验证专用 origin 的 `/login → /iam/oauth2/authorize → /auth/sign-in` 为 `302→302→200`、有真邮箱/密码表单；普通 IAB 直开该 origin 为 `ERR_CONNECTION_CLOSED`。不能再称当前用户页已闭环。只读核对 Better Auth 1.7.3 当前安装源码：显式 `application_type=native` 允许精确 HTTP loopback redirect，且与 `client_secret_basic`、PKCE/state/nonce 可组合；现有 IAM managed `user_delegated` 不传 `application_type`，默认为 `web` 并拒绝 HTTP loopback。**目标**是仅本地开发使普通浏览器同一 `http://127.0.0.1:3310` origin 的真实 Web→BFF→IAM OIDC 成立；生产 Web client 仍只使用非 loopback HTTPS，不增加无后端假表单、中转/重试页或 Web→IAM 直连。

W1-LOCAL-LOOPBACK-ENTRY 设计与派工门：IAM OAuth client 是唯一注册/回读/验证 owner；Root 启动器仅在 IAM owner 提交并验收后消费。采用“显式 dev-only native loopback client + 生产 web HTTPS 保持严格”（需 IAM 真 PG/Redis/授权码/secret 换码验证）而淘汰“继续把临时 `.example.test` 专用 Chrome 当用户入口”；不采用生产放宽 HTTP web redirect。IAM 唯一 writer 第一阶段仅可更新本仓 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md`，明确当前/目标、client application type、精确 origin/redirect/退出、持久化 readback、production 拒绝和失败恢复；Root 审查三文档一致后才给代码写入卡。预定代码文件集仅 IAM `src/modules/auth/oauth/clients/{oauth-client.schema.ts,oauth-client.service.ts,oauth-client.repository.ts,client-readback.policy.ts}`、`src/provisioning/provisioning.config.ts`、`scripts/provision-oauth.ts` 与相邻 unit/integration/contract 测试；若确需扩域必须先报告。无新业务表/schema/租户角色、无 BFF/Web wire API 变化；已有 client 不双写，Root 后续独立重建本次 test-owned dev 入口。验证 IAM Node24 `pnpm verify`、串行真实 PG/Redis OAuth client 注册/回读及 Code+PKCE+basic token、生产/公网 HTTP 负例、资源0；随后 Root 修改现有 `scripts/dev/serve_local_login.py` 和相邻测试，**先停掉自己创建的旧 3310 fixture 并核对资源清理，再在同一 3310 启动 HTTP loopback 组合**，用普通 IAB/Chrome 实测 `/login`→表单→`/app` 与退出。主控保留 Root Git index/commit、契约审查与最终浏览器验收。此片只修开发入口，正式常驻租户仍按单库 owner 顺序另验。

W1-LOCAL-LOOPBACK-ENTRY-IAM-CODE（2026-09-25，待 owner 实施）：IAM 三文档门已由 Root 独立审查，精确路径为 `/Users/nako/WebstormProjects/github/thefoxfairy/Kokoro/apps/kokoro-iam/docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md`；Node24 `pnpm contract:check`、`pnpm prisma:validate`、`git diff --check` PASS，IAM main 文档提交 `04ba44a` 已推送。IAM 唯一 writer 沿用同一负责人；起点 main `04ba44a` clean。**允许写入**：既有 `src/modules/auth/oauth/clients/{oauth-client.schema.ts,oauth-client.service.ts,oauth-client.repository.ts,client-readback.policy.ts}`、`src/provisioning/provisioning.config.ts`、`scripts/provision-oauth.ts`，以及 `test/unit/{oauth-client.service.test.ts,oauth-client.repository.test.ts,provisioning.config.test.ts,provision-oauth.test.ts}`、`test/integration/oauth-client-lifecycle.test.ts` 与必要 `docs/{CURRENT,RUNBOOK}.md`；如官方 readback 原生类型或相邻固定 Web client 测试确需改动，先报主控扩卡。排除 Prisma schema、generated、vendor、lockfile、其他仓和 3310。先在既有测试 RED 断言默认/显式 web 的 HTTP loopback、native 公网 HTTP、production native 和错误 redirect 拒绝；GREEN 实现显式 dev/test native+精确 127.0.0.1 callback/logout，Basic secret+PKCE/Code/refresh，官方和持久 readback 双核；生产 web HTTPS 保持。单仓 Node24 format/lint/typecheck/contract/schema/verify、串行真实 PG/Redis integration 与自有资源清零；worker 不操作 Git index/commit，由 Root 复验并提交。主控审查人 `/root`，后续 Root 启动器 writer 为 `/root`，依赖 IAM owner 提交与验收。未实施前不要改变旧 3310 服务。

W1-LOCAL-LOOPBACK-ENTRY-IAM-CODE 夹具扩卡（2026-09-25）：Root 只读核对发现现有 `test/fixtures/web-oidc-flow-host.ts`、`test/fixtures/auth-database.ts` 双层仅接受 HTTPS 非 loopback，`test/fixtures/internal-http-application.ts` 创建固定 Web client 时未传 application type；若不改，Root 无法用已存在的自有资源/清理边界在普通 3310 复验。因此同一 IAM writer 额外允许仅改上述三个 test fixture 与相邻 `test/integration/web-oidc-flow-host.test.ts`；必须显式 opt-in 精确本机 HTTP origin，不让现有 HTTPS fixture 或默认输入变宽，并在集成测试证明 opt-in native、无 opt-in HTTP 拒绝及自有资源清零。IAM 生产注册仍按上一卡，不新增第二 test host/长期 fallback。Root 尚未修改启动器或旧 listener。

W1-LOCAL-LOOPBACK-ENTRY-IAM-CODE 协议勘误与文档扩卡（2026-09-26）：只读审查与 Root 核对当前锁定 Better Auth 1.7.3 实现：`native` 的 HTTP loopback 授权 redirect 按标准匹配 host/path 时可忽略请求端口；此前卡片中“错误 redirect 拒绝”仅指非 loopback host、错误 path、未注册 scheme 等，**不含同一 loopback host/path 的不同有效端口**。这是开发/测试 native client 的协议行为，不为此改 vendor 或新建 authorize 旁路；注册/持久化仍只允许配置的单一 `127.0.0.1:3310` callback（测试使用独占端口），Web 启动器只请求这个已配置 URI，生产 `web` 仍为非 loopback HTTPS。token exchange 必须与授权请求的 redirect 一致，并要求服务端 Basic secret、PKCE verifier、state/nonce；无这些不得建立 Product Session。同一 IAM writer 现在额外获准修改既有 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md`，修正“端口绝对拒绝”等不实断言，增加 native 端口语义与真实授权/换码正负例；若 vendor 行为限制使其中断言无法成立，报告 Root，不以自造限制或放宽生产约束假装通过。仍排除其它 IAM 文件、Root/Web/BFF 代码及共享资源；Root 独立复验后提交。

W1-LOCAL-LOOPBACK-ENTRY-ROOT-HTTP（2026-09-26，IAM owner 已发布，Root 实施）：IAM main `6a55ffb` 已按 Node24 `pnpm verify` 757/757、真实 PG/Redis 两个集成文件串行 2/2 与 26/26、Prisma validate 验收；原 IAM writer 连接异常终止后由 Root 接管最终文档/复验/提交，同仓没有并行 writer。Root 是本片唯一 writer；仅改既有 `scripts/dev/serve_local_login.py`、`scripts/tests/test_serve_local_login.py`，如共用正式表单导航探针必须改 `scripts/e2e/run_web_bff_iam_first_login_smoke.py` 及其相邻测试（只增加可注入 HTTP transport，不放宽 HTTPS 现有门）；Root `docs/{task,progress,CURRENT}.md` 与 IAM gitlink/inventory 由 Root 串行 pin。比较继续用 TLS proxy+临时域名（用户普通 HTTP 标签不可达，淘汰）与直开固定 `http://127.0.0.1:3310` 独占 Next HTTP（采用）：无新代理进程、证书/SPKI、host 映射或假登录表单；IAM host 显式 `IAM_TEST_ALLOW_HTTP_LOOPBACK=1`，注册单一固定 callback，BFF/Web 同源配置和受信域一致。保留启动前端口占用保护、隔离 PG/Redis/临时 Next 与 Ctrl-C 自有资源回收；不清理别人的旧进程/数据。验证单测 RED→GREEN、Ruff、Root pytest、固定来源/拓扑门和普通浏览器 `/login`→真 IAM 邮箱密码表单→`/app`→退出；用户旧 HTTP 标签真正可打开前不得宣称闭环。正式单租户运营部署及其它 Wave 继续开放。

W1-LOCAL-LOOPBACK-ENTRY-ROOT-HTTP 验收更新（2026-09-26）：Root main `b67c2d67` 已提交推送 IAM gitlink、库存和 HTTP 直连启动器；Root 全量 `scripts/tests` **740 passed/187 subtests**、topology 与 W1B checkpoint PASS。普通 Codex IAB 直接打开 `http://127.0.0.1:3310/login`，眼见真实 IAM 邮箱/密码表单；以本次临时凭据提交、同意后进入 `/app`，点击“退出登录”后 Product Session 失效，再进 `/app` 被送回 IAM 登录。Issuer 的原生英文确认页仍需要 POST `/iam/oauth2/end-session/confirm`，Codex IAB 与受控 Chrome 均返回 `ERR_BLOCKED_BY_CLIENT`，不能把 issuer 最终确认当已验收；此页视觉也不符合正式产品，另立 Web/IAM owner 界面切片而不把它混进登录入口修复。旧 Root test-owned HTTPS fixture 遗留的独立 Next/Chrome 进程组已按精确路径/PID 停止，旧临时目录回收；当前 HTTP 3310 仍前台运行，不清理其资源。Root main-only 因 Web W1D 三份待提交设计文档为 FAIL，不冒称全仓 clean。

W1D-WEB-IAM-DIRECT-CUT 文档门交付（2026-09-26）：Web main `832bc8e` 已由 Root 接收同一 owner 的 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 并修正 `docs/CURRENT.md` 的旧 3310 状态，四文档提交推送、工作树 clean；Node22 `pnpm contract` **69/69**、`git diff --check` PASS。确认 GET session 是只读在线投影，POST session 才执行 CSRF + reserve/finalize CAS 和 BFF/IAM refresh；旧 auth/直连/封装 cookie 仍在代码，故只放行设计门，不标 W1D 代码或 compatibility 绿。下一步 Web 唯一 writer 按前述 W1D 任务卡执行 architecture RED→GREEN 的删除切片；Root 先 pin Web gitlink/库存并复验，再派代码写入，避免当前 HTTP 3310 运行源与消费者来源漂移。

W1D-WEB-IAM-DIRECT-CUT 代码任务卡补充（2026-09-26，待验收）：Root main `eb6fa617`、Web main `832bc8e`，两仓 clean；Web 唯一 writer、Root 审查/Git/集成，前台 HTTP 3310 属 Root，writer 不触碰。基线/设计门见上文与 Web 三设计文档；`docs/CODEBASE_MAP.md`、TypeScript/API/测试手册为入口。精确写入范围：Web `src/lib/server/{auth,session-envelope,same-origin}.ts`、旧 `src/app/api/auth/{logout,session-state}/route.ts`、Auth.js catch-all `src/app/api/auth/[...nextauth]/route.ts`、六个同源业务 route、直接依赖旧 helper 的测试和 architecture 测试、`Dockerfile`、`.github/workflows/release-image.yml`、`scripts/first-site-smoke.mjs`、五份 `.env*.example`、对应现态 `README.md`/`INDEX.md`/`src/lib/server/INDEX.md`/`docs/{CURRENT,SECURITY,RUNBOOK,deployment,first-site-live-runbook}.md`；若需越界先报 Root。先 architecture/真实路由断言 RED，再删除旧 helper、两条 route、配置与无效测试，保留正式 Auth.js Product Session/CSRF/OIDC、六条代理和负向恶意 cookie 安全测试。删除静态旧 route 后 catch-all 仍截获 URL，必须显式让 `/api/auth/logout|session-state` 返回设计规定的 404，不能以 405 假装已删。发布健康/first-site smoke 不再依赖旧 route，也不得用未配置 RP 时 503 的 `/api/auth/session` 机械替换；采用独立于 System/IAM 的有效公开页健康信号，保持 privacy/security header 断言并实测候选配置。冻结 `src/generated/proto` 是 ADR-0002 历史审计快照，本片不手改；架构扫描只限定活动运行源码。验证 Node22 `pnpm contract`、architecture、lint、typecheck、全量 test、build、e2e、真实 Next 旧 URL 404/公开页健康；Root 在固定 Web SHA 串行复验，随后 pin 来源并检查 compatibility illegal edge（其余 broken 仍开放）。不建 Web SQL、不改 BFF/IAM/Root运行代码，不启动/清理共享基础设施。

W1D-Web 补充裁决（2026-09-26）：Pixel 7 对 `/login` 空 503 文档的 1px viewport 舍入不是应用布局，E2E 改在实际可见的公开 `/` 校验无水平溢出，同时保留 `/login` 无可见中转/空 body 断言；允许改直接相关 E2E 文件。First-site smoke 必须区分公开页 liveness 与显式 live/preview 模式，live 仍检查 System manifest、`/app` 和敏感字段，不用旧 `session-state` 推断。实测 `/app` 现为 `no-cache` 而非私有 `no-store`，授权 Web writer 扩 `src/lib/server/security-headers.ts`、`tests/server/security-headers.test.ts`，使 `/app` 及其子路径私有 no-store、公开 `/` 不变；现有 `src/proxy.ts` 已调用该 helper，默认不改 proxy，若实测证明必须修改再报告。

W1D-Web Root 预审补充（2026-09-26）：代码切片已达到 Web `pnpm check` 69 contract/36 architecture/1474 Vitest、独立 E2E 11 pass/1 intentional skip 与生产 Next 旧 URL 404，但三权威设计文档的首页仍写“代码尚未实施/旧 route 仍在”，`src/ui/auth/use-session-state.ts` 与 `src/ui/auth/INDEX.md` 留有旧 session-state 注释。扩大原 Web 唯一 writer 的精确范围至 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL}.md` 首页当前态及 `src/ui/auth/{use-session-state.ts,INDEX.md}` 文案，只改已实现事实与对应验收状态，不重写历史段落或调整运行行为；Root 再审查与提交。Frozen generated/proto 不动。

W1D-Web Root 集成发现（2026-09-26）：原 Web writer 已完成并停止写入，Root 在同仓接管最后聚焦修正与 Git。独立 `pnpm check` 已通过 69 contract/36 architecture/1474 tests/lint/typecheck/build；但 Root 直接以 `KOKORO_E2E_PORT=3341 pnpm test:e2e` 重跑，Playwright webServer 错以无配置时设计为 503 的 `/login` 作 readiness URL，反复请求后无法开始测试；Root Ctrl-C 后确认自有 3341 子进程/listener 已清。扩精确文件范围至 `playwright.config.ts`：将 readiness 指向无需 IAM/System 的公开 `/`，不修改 `/login` 无中转/503 语义；先保留失败证据，再修复并在隔离端口重跑 E2E，绝不复用用户 3310。Root 也清理本次 Playwright report/test-results、自动生成的 next-env 改动，不纳入提交。

W1D-ROOT-COMPATIBILITY 放置门（2026-09-26，Web owner `71d408e` 已发布，Root 唯一 writer）：Owner 为 Root contract consumer inventory/checkpoint 治理，不是 Web/BFF/IAM 业务事实。当前 `verification/contracts/consumer-inventory.json` 的 13 处 Web 证据仍 pin `832bc8e`，并列 `EDGE-WEB-IAM-DIRECT` 违法边；当前 `w1b-iam.json` 是历史精确阶段，`EXPECTED_VIOLATION_IDS` 与测试仍要求该旧违法边。目标是将全部 Web 证据重钉到已提交 `71d408e` 的真实 commit/blob，删除唯一违规记录与允许集，同时保留 11 broken edge 的诚实红队列。目录方案比较：直接改历史 `w1b-iam.json`（篡改历史，淘汰）与在既有 `verification/contracts/checkpoints/` 新建 `w1d-web-iam-cut.json`（采用）；只增加这一份同类阶段快照，不建新目录/模块。依赖方向是 Root 从子仓固定 commit 读 blob，Web 不依赖 Root；不改 API/Schema/事务/tenant/Redis。删除项仅当前 inventory 的过期非法旁路，不删历史 W0B/W1B 事实快照。验证先写 Root checkpoint/compatibility 测试 RED，再改 `scripts/governance/contract_inventory.py`、inventory、新 checkpoint 与相关测试 GREEN；`verify-contract-compatibility.py` 仍因 11 broken 非零，`verify-contract-checkpoint.py --expected verification/contracts/checkpoints/w1d-web-iam-cut.json`、topology、main-only、全 `scripts/tests` 必须 PASS，且不能把 11 broken 当绿。

W1D-Web 与 Root compatibility 待提交验收（2026-09-26）：Web main `71d408e1a36fbe8c3ff7dc350311e5b4eeb8be23` 已提交推送、clean；原子删除旧 IAM 直连/sealed/两旧 auth route，同源 guard 独立，旧 URL GET/POST 独立生产 Next 均 404，`/app` private no-store；Root Node22 `pnpm check` 69 contract/36 architecture/1474 Vitest/lint/typecheck/build exit0，隔离 3341 E2E 11 pass/1 intentional skip，独立生产 Next 3342 liveness smoke PASS；自有端口/报告已清，用户 3310 隔离旧 Web 副本不动。Root inventory 的 12 处存续 Web commit/blob 已按新 SHA 重钉，旧非法边已删；新 `w1d-web-iam-cut.json` checkpoint/Root 聚焦测试 82/82、全 `scripts/tests` **741 passed/187 subtests**、topology/checkpoint PASS。完整 compatibility 当前仍 `FAIL`：16 edges、0 violations、11 declared broken，不冒称全绿。下一步 Root 精确提交/push 后复验 main-only/relay-policy；新 Web 与当前 HTTP 3310 三仓登录/退出组合尚未重跑，保持待验。

W1D Root 提交后下一 owner 切片（2026-09-26）：Root main `30e5683af69dde4f70f47d50bf68ac4aea637736`、Web main `71d408e` 均已推送/clean；main-only、topology、新 `w1d-web-iam-cut` checkpoint PASS，compatibility 仍 11 broken/0 illegal，标准门 134 既有违例。`verify-iam-relay-policy.py` 单项 FAIL：BFF policy 的 `iamOwnerCommit=7215223` 仍指向 IAM 旧 SHA，而当前 IAM gitlink 为 `6a55ffb`；Root 按固定新 IAM blob 比对 allowlist、Better Auth snapshot 与 OpenAPI 三个 SHA-256 **全部字节相同**，只是 provenance stale。下一任务须严格 IAM owner → BFF policy JSON/TS/生成与文档/测试 → Web vendored policy/provenance/测试 → Root 两 gitlink 和库存的顺序完成，不在 Root 放宽验证器；期间不碰用户 3310。新 Web 固定 SHA 真三仓浏览器登录/退出验收亦待独立执行。Billing 仍最后。

W1D-RELAY-PIN-BFF / P0（2026-09-26，待派工，BFF 单仓唯一 writer）：基线 Root main `5e4d862c`、BFF main `2f1fc3382df31ba107d7eb2b2b6a611fa893bc13`、IAM owner `6a55ffb4c22f0b155ddb83157735c0ace766701d` 均 clean/main；Root 保管共享 Git index/提交，writer 只改 BFF。Owner 为 BFF browser-private IAM relay consumer 与 IAM generated client 来源，不改变 IAM 身份事实、BFF Chat/SQL。当前 BFF `contract/dependencies/iam-http.json`、`contract/iam-relay-policy.json`、`src/http/routes/iam-protocol-relay.policy.ts`、`scripts/generate-{iam-http-client,iam-relay-policy}.mjs`、`openapi-ts.iam.config.ts`、vendor 路径、对应精确测试和 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL,CURRENT}.md` 仍指旧 IAM `7215223`；新 IAM 的 allowlist/snapshot/OpenAPI 三个固定 blob digest 逐一与旧值相同。目标只重钉 **已发布 IAM commit**，生成链/manifest/vendor路径/运行 policy 双事实一致，无 route/header/cookie/status/业务行为更改，不放宽门或手改 generated。目录方案：保留旧 SHA vendor 目录但改 manifest（来源身份矛盾，淘汰）；将既有 vendor artifact 原样搬到新 SHA 目录并验证与 IAM 新 commit blob 字节相同（采用），不建新模块/目录层。先三设计文档当前态对齐/contract 测试 RED，再改 producer-pinned refs、重新生成、删除旧 vendor 目录，覆盖 policy 不变断言；No SQL/Schema/Redis 变更。Node22 `pnpm format:check && pnpm check`、相邻 relay policy/transport 测试、`test:iam-relay:integration` 仅在隔离依赖可用时执行；Root 审查、固定 BFF commit/push 后才派 Web 消费，不并行改 Web，也不触 3310/共享基础设施。交付文件清单、真实命令/结果与风险。

W1D-RELAY-PIN-BFF 交付（2026-09-26）：BFF main `bc45632b8654db7e06eb9878bb4d7a609d12dc7b` 已提交并推送；Root 独立 Node22 `pnpm format:check && pnpm check` exit0（contract 28/28、全量 291 pass/1 既有 skip、lint/typecheck/build），vendor 与 IAM `6a55ffb` OpenAPI blob 完全同字节，生成的 16 个 client 文件无变化。只更新 IAM provenance，不改 wire/SQL/路由。外部 IAM integration 因无隔离 URL 未执行。Web consumer 与 Root gitlink/库存仍待串行完成。

W1D-RELAY-PIN-WEB / P0（2026-09-26，已派工，Web 唯一 writer）：基线 Web main `71d408e1a36fbe8c3ff7dc350311e5b4eeb8be23`、BFF owner main `bc45632b8654db7e06eb9878bb4d7a609d12dc7b`。Owner=Web browser-private relay policy 消费；现有 `src/generated/iam-relay-policy.json`、`src/lib/server/iam-relay-policy.ts`、相邻 contract 测试与四份 docs 仍是旧 BFF/IAM 来源。只从已提交 BFF artifact 原始字节重钉 policy 和 SHA-256，不改认证逻辑、UI、SQL、路由；沿用现有文件而非新建重复来源。依赖方向 IAM→BFF→Web，Root 最后固定 gitlink/库存；先聚焦 RED→GREEN，Node22 `pnpm check`，Root 独立复验。UI 切片待本来源切片提交后由 Web 单 writer 处理。

W1D-LOGIN-UI-POLISH / P0（2026-09-26，Web 唯一 writer）：用户已确认 `/login` 直达正式 IAM 邮箱/密码表单、删除可见连接/整页重试。Root 只读 3310/IAB 已见真实表单，当前 Web main `54ad9e85a113e8b43a8779283621971517ab17fc`；UI 归属 Web 既有 `src/app/auth/sign-in/route.ts` 与 `src/lib/server/iam-interaction-page.ts`，IAM 只拥有身份与签名交互。现有 Web `src/components/ui/{button,input,card}.tsx`/`globals.css` 为 shadcn 风格事实，但签名 Route Handler 输出 script-free HTML，未直接 import React 组件，且全英文/局部 token 重复。选择保持 Route Handler/一次性 CSRF/OIDC/POST 路径，只复用同一语义 token、组件尺度和层级，并收敛中文文案/错误位置/移动端与 focus；淘汰为“直接把 React 组件塞进 Route Handler”的失败尝试，也不新建可见中转页、第二路由或静态 mock。只修改上述既有 UI/表单文件和对应已有浏览器/contract 测试、必要 docs；无 SQL、API wire、Cookie、Redis binding 或浏览器脚本变化。验收 Node22 `pnpm check`、隔离真实 Next/Chromium 桌面/窄屏/移动 + axe + 错误态、Root 最后以固定 SHA 在浏览器核验；用户 3310 进程不由 writer 重启。Root 保管 Git index/commit。

W1D-LOGIN-UI-POLISH 交付（2026-09-26）：Web main `942d22e4d42ba3abfeb0407f738e8108b7a74eed` 已提交推送、clean，唯一签名表单/401 错误/中文布局已改，不改 OIDC/CSRF/POST。Root 独立 Node22 `pnpm check` exit0（contract 69、architecture 36、Vitest 1475、lint/typecheck/build），隔离真实 Next/Chromium 52/52、桌面/窄屏/移动与 axe PASS；截图已审阅。BFF main `bc45632` 与 Web 当前来源 commit/blob 已在 Root inventory 精确重钉，Root gitlink/文档待提交；3310 仍是旧隔离源码副本，重启/可见验收另记。

W1D-ROOT-PIN-LOGIN 验收（2026-09-26）：Root main `c189f2fc289d3e9fe4998917fe6667f9d82720d4` 已提交，精确固定 BFF `bc45632`、Web `942d22e`、146+12 处库存 commit/blob。提交后 relay-policy/topology/checkpoint/main-only PASS；Root `scripts/tests` 741 passed/187 subtests；compatibility 仍 16 edges/0 illegal/11 broken，十仓标准 134 violation。Root 精确停止旧 3310 独立进程并由当前 SHA 重建，HTTP `/login`→中文 IAM 表单 200、Codex IAB 临时账号登录→consent→`/app` 可见；当前测试租户是临时 fixture，不冒充正式部署。Issuer 最终退出/其他 broken edge 后续处理。

W1E-WEB-BFF-CONSUMER-AUDIT / P0（2026-09-26，只读）：基线 Root `da0b1cf76b1cf0ed4e5dbd3da3cd17b7b352f1e3`、Web `942d22e4d42ba3abfeb0407f738e8108b7a74eed`、BFF `bc45632b8654db7e06eb9878bb4d7a609d12dc7b`，均 main/clean。目标不是再改登录外观，而是盘点 `EDGE-WEB-BFF` 仍 declared broken 的每个 Product 请求/响应消费者，明确已生成 Team 与其余手写路径、AG-UI 独立事件协议、BFF public OpenAPI 覆盖/缺口；给出下一 Web owner 写入切片的精确文件集、契约依赖、删除项、测试与 Root 激活 edge 的完整条件。Agent 只读 Web/BFF/Root inventory，不改 Git/文件/DB/Redis/3310。Root 同时审查 W1 IAM workload 缺口，按 IAM owner 先行、BFF/Web 后消费，避免两边各自发明契约。

W1E-IAM-WORKLOAD-AUDIT / P0（2026-09-26，只读）：同一固定基线，IAM `6a55ffb4c22f0b155ddb83157735c0ace766701d`。目标核对已批准 Wave1 execution/workload authorization：IAM 当前公开 machine contract/运行权限/issuer token 与 Platform/Agent 期望间的差距、owner SQL 与 scope 来源，列出最小 owner-first 文档门和实现切片及可信负例；不把旧 Capability attestation 私有端点直接恢复，不提前写 Platform/Agent。Agent 只读 IAM/Platform/Agent/Root 文档和契约，不改文件/服务/共享数据。Root 审查两份报告后裁决依赖与下一唯一 writer，更新同一台账。

W1E 审查结论（2026-09-26）：Web→BFF 只读 Agent 在固定 Root `da0b1cf`/Web `942d22e`/BFF `bc45632` 上确认，Team 9 operations 已生成；Chat JSON、Scheduled、Hub、Share、System、Agent setup 与 Billing 仍有手写消费者，部分活跃路径不在 BFF public OpenAPI。AG-UI SSE 独立于 JSON client。`EDGE-WEB-BFF` 保持 broken；下一最小 Web owner 切片先处理已有 BFF OpenAPI 的 Chat JSON method/path/response 生成与同源 adapter allowlist，未发布路径先由 BFF owner 裁决或 Web 原子删除，Billing 最后。

W1E-IAM-PERMISSION / P0（2026-09-26，Root 唯一 writer，已提交 owner 子仓）：IAM `5c9cecf714c87234bbc9558665b23e09afa6e9f6` 在现有 Tenancy access-control catalog/固定角色/dynamic role schema 与当前态 permission policy 中实现 `platform:execute`，不新建 SQL 表、角色或授权旁路；OpenAPI `0.4.0` 角色列表可选字段与 generated SDK 同步，`pnpm verify` 86 文件/761 项、真实隔离 PostgreSQL role-lifecycle 2/2。BFF `017464480e603e3e5780c55597f8d40970589ef7` 从 IAM 当前机器契约重钉 vendor/16 文件生成链和 relay policy provenance，Node22 format/check 291 pass/1 既有 skip；Web `04fd9418df55bc5db3df9829b9e9cbba40d3a334` 原样消费 BFF policy，Node22 check contract69/architecture36/Vitest1475/build PASS。三仓 main/clean，Root `484a41277210e321416a3bc1e28e2b30d45d7a3f` 已固定 gitlink/库存并通过来源与拓扑门。此片只关闭权限目录与来源，不含 `/internal/v1/execution-authorizations/verify`、caller workload scope、Prisma Serializable decision/Audit 或 Platform consumer，W1 整体保持进行中。

W1E-IAM-EXECUTION-E2 / P0（下一 IAM owner 写入切片）：已接受的 IAM ADR-005 和三设计文档为 owner 边界，E1 proof/JWKS parser+verifier 已在源码，E2 需按 `caller tenant_machine/client/scope → exact proof/binding/tenant → 短 Serializable 当前态 Tenant/User/Member/Role/Permission + AuditEvent → 具名 endpoint/OpenAPI/SDK` 串行落地。只在 IAM 现有 `authorization/execution-authorizations/`、`auth/internal-access`、Audit 既有边界写入；无新库/角色/nonce 表，不碰 3310。先核对 Agent/Platform pinned artifacts、文档 SPEC/QUALITY 与代码负例，再 TDD 接线；完成 IAM 单仓全门和真实隔离 PG/Redis HTTP 后，才派 Platform consumer，Root 最后做跨仓重放/撤权验证。

W1E-IAM-EXECUTION-E2 派工门（2026-09-26）：Root `0144f23a`、IAM `5c9cecf714c87234bbc9558665b23e09afa6e9f6`，均 `main`/clean；IAM 唯一写入 Agent 负责 E2，Root 保留本台账、Git index/提交、审查和跨仓验收。可修改 IAM 已有 `docs/{TECHNICAL_DESIGN,API_CONTRACT,DATA_MODEL,CURRENT}.md`、ADR-005、`src/modules/{auth/oauth,auth/internal-access,authorization/execution-authorizations,authorization/authorization.module.ts,audit}`、`src/config`、`src/http`、相邻 `test/`、本仓 `contract/`/生成链及必要 `.env.example`；越界先报告。先修三设计文档里 Agent artifact “待发布”与现有 pin 的矛盾并冻结 caller policy：采用**专用 Platform tenant_machine client 的持久 purpose 标记**，provisioning 仅对此目的允许独占的 `iam:execution-authorization.verify` scope，Guard 再以当前 client 元数据核验；淘汰仅检查 tenant_machine+scope（普通机器客户端可冒用）和新数据库角色/表。E1 verifier 固定源与 `/authorization/check` 不变，E2 无 Web/BFF/Agent 写入。TDD 覆盖 scope/provisioning、bad proof、tenant/actor/subject/current role/撤权/disabled/audit 事务、JWKS 故障与 replay；Node24 IAM `pnpm verify` 和隔离真实 PG/Redis HTTP/资源清理后交付文件清单与证据，Root 独立复验并按 owner 顺序 pin。无 user 3310 进程操作，Billing 仍最后。

W1E-IAM-PLATFORM-INGRESS / P0（E2 后、Platform 原子 cutover 前的 IAM owner 设计/实现门）：Platform ADR-002 §9–13 还要求 IAM 发布 `https://kokoro.dev/resources/platform-internal`、四类 exact caller/scope（Agent execution、BFF projection/catalog、System global）、tenant/profile/client 绑定和 resource-server introspection credential。当前 IAM 无该 resource/scope/provisioning；它不同于 Platform→IAM E2 verifier 专用 client，不能把前者 token 借用为后者。由 IAM owner 在 E2 文档与 endpoint 验收后按三设计文档/机器契约另立唯一写入切片，先发布并生成 SDK，再允许 Platform 单一 atomic code release。Platform 旧 shared token、任意 tenant header、手写 attestation/JCS、缺 current-state receipt replay 仍属活跃旧实现，单换 IAM adapter 不算闭环；Agent/BFF/System/Storage/Web/Root 按 Platform 已发布 contract 串行 cutover 与六 owner sandbox，Billing 最后。

W1E-IAM-EXECUTION-E2 范围增补（首个 TDD RED 后）：IAM OAuth 注册入口还拥有 client provisioning/readback；为使 `purpose=platform_execution` 的专用 client 从持久创建到读取一致，唯一 IAM writer 可增加既有 `src/provisioning/provisioning.config.ts`、`scripts/provision-oauth.ts` 及其相邻 `test/unit/{provisioning.config,provision-oauth}.test.ts`，不得改其他 provisioning 模块。首轮 `oauth-client.service`/`token-claims` 两文件共 53 测试，3 个预期 RED（internal resource scope、purpose 解析、token 独占 scope），其余 50 pass；这是实施起点，不是 E2 GREEN 或运行闭环。

W1E-IAM-EXECUTION-E2 env 夹具增补：E2 运行时需要固定 Agent issuer 与 JWKS URL 的启动校验；新增必填环境后，既有源码生成与生命周期夹具须显式提供这两个值，因此扩唯一 IAM writer 至现有 `scripts/auth-generation.ts`、`test/integration/runtime/source-process.test.ts`、`test/unit/introspection-credential.test.ts`。这三处只补 test/generation 配置，不改变生成算法、运行资源或权限事实；`src/config/environment.schema.ts`、`.env.example` 已在原卡范围，后者不得重复声明。Root 复验时检查固定 URL 不接受任意 path/redirect 与 malformed issuer。

W1E-IAM-EXECUTION-E2 真实 OAuth RED 后范围增补：专用 client 在真实隔离 PG/Redis fixture 创建时由 Better Auth provider 拒绝 `invalid_scope iam:execution-authorization.verify`（HTTP suite 尚未运行，4 skipped）；provider 的 machine scope allowlist 实际位于 IAM 既有 `src/modules/auth/better-auth/better-auth.config.ts`。扩唯一 IAM writer 仅改此文件与紧邻必要测试，把已裁决专用 scope 加入 provider allowlist；仍由 OAuth schema、purpose、token-claims 和 current Guard 四重限制，不放宽普通 machine/user/operator。先保留 RED 再重跑真实 fixture；不改 Better Auth vendor snapshot 或源码。

W1E-IAM-EXECUTION-E2 独立预审修复门：E2 将 Agent JWKS client 从 E1 未装配代码接入正式 AuthorizationModule，当前 URL 仅校验固定 path，任意非 loopback HTTP 仍可作签名信任源。唯一 IAM writer 在已授权 `src/config/environment.schema.ts` 与相邻单测中加 RED→GREEN：正式远端只接受 HTTPS，开发/测试的 HTTP 仅限精确数字 loopback；禁止以普通内网 HTTP、`localhost` DNS、重定向或宽泛环境开关降低信任根。真实本地 JWKS HTTP、全门、no-store 与当前 3310 不受影响；Root 复验后再放行。

W1E-IAM-EXECUTION-E2 真实事务/调用方负例补门（2026-09-26，P1 验收、IAM 唯一 writer）：Root `0097bfc6`、IAM 仍以 `5c9cecf` 为未提交工作树基线；只写现有 `test/integration/execution-authorization-http.test.ts` 及为独立 fixture 必需的同目录 helper，首选不新增文件。独立只读审查 0 P0/P1，但指出真实 PG/Redis HTTP 尚缺 current caller 禁用/独占 scope/resource binding 漂移、Member remove 与 Audit 写入回滚。先为这些实际状态加尽量少的独立负例，确保返回码/no-store、未产生错误 allow 和 Audit 一致；若测试暴露生产缺陷先报告 Root 扩卡，不自行扩大源码写入。现有 5/5 必须保持，Node24 全 `pnpm verify` 与此 suite、test-owned DB/Redis 清理为交付门；Root 复验与精确提交。真实 Agent signer 跨仓、Serializable 冲突与 commit-unknown 注入属于后续 Root 固定版本组合门，不把本 test-only 切片口头冒充跨仓通过。

W1E-IAM-E2-BFF-SOURCE-PIN / P0（2026-09-26，BFF 唯一 writer）：IAM owner main `b720b6dc095b883237682102ca0a87ed6451a968` 已发布 internal OpenAPI `0.5.0`/SHA-256 `cddfec4cd3439d98f399254911232c447582a97e9b1d4c109139e68baaf030b9`；BFF main `017464480e603e3e5780c55597f8d40970589ef7` clean，只消费旧 IAM 0.4.0。按 BFF 既有 `docs/TECHNICAL_DESIGN.md` 的固定 vendor → `scripts/generate-iam-http-client.mjs` → generated client/manifest → TS relay policy → 派生 JSON 单向链重钉 owner 原始字节、版本、commit、digest；删除旧 vendor 目录，更新相邻来源测试与当前 docs。BFF 不承接 E2 verifier 业务、不放行新的浏览器 relay route，不改 public Product API、SQL/Redis、运行中 3310；生成类型新增 E2 operation 也不得进入无关业务路径。先以现有 provenance 测试 RED，再 `pnpm contract:generate:iam && pnpm contract:generate:iam-relay`、Node22 `pnpm format:check && pnpm check`、独立生成 drift 与 policy route/header/cookie 不变断言；主控复核后精确提交。Web 只有 BFF policy 提交后才能原始字节消费新来源；Root inventory/gitlink 在 IAM+BFF+Web 三 SHA 都固定后更新，`EDGE-CAPABILITY-IAM` 仍 broken，不能凭 BFF repin 激活 Platform。

W1E-IAM-PLATFORM-INGRESS-READ / P0（2026-09-26，只读并行审查）：基线 IAM `b720b6d`、Platform `9c88d0d`、Root `6174f2fd`；审查者只读 IAM 当前 OAuth schema/provisioning/Better Auth 1.7.3 vendor/native introspection、SDK 生成边界与 Platform ADR-002 §10，不修改文件/数据库/进程/Git。交付一个可执行的三设计文档门：四类 exact scope/client/profile/tenant 的持久 purpose 与签发限制、`platform-internal` resource 及 dedicated resource-server credential 的当前 readback、introspection 在 wire 上实际暴露的可信 caller/tenant/resource/scope/current-disabled 字段、generated client 的真实来源；若原生 introspection 信息不足，明确最小 IAM owner 具名 contract，而非让 Platform 猜 client_name/header。列测试与部署前激活门、禁止复用 Platform→IAM verifier client、新角色/数据库或通用配置中心。Root 负责裁决和下一 IAM 唯一 writer，不把只读结论冒充实现。

W1E-IAM-E2-BFF-SOURCE-PIN Root 范围裁决：BFF 当前 `openapi-ts.iam.config.ts` 的 operation include 是 BFF 实际调用面的最小授权，不因 IAM vendor 0.5.0 新增 E2 endpoint 而加入该 operation。完整 vendor 原始字节固定 0.5.0；BFF 生成的既有 16 operation、browser-private route/headers/cookies 都不变，只更新 owner provenance/digest 与确有 schema 差异的生成文件。E2 的真正消费方是 Platform；BFF 不携带或暴露 `verifyExecutionAuthorization` SDK symbol。生成/测试/文档超出本裁决的候选由 BFF writer 撤回后再交 Root 验收。

W1E-IAM-E2-WEB-POLICY-PIN / P0（2026-09-26，Web 唯一 writer）：BFF main `c586d0bdb42248f206b8adc92784e4c2140d2512` 已发布 browser-private policy `2.1.0`，原始 SHA-256 `ed476b63205c0eaf59106dc618c138df6110ef6240ce2be50b417fea8ec800e4`，IAM owner `b720b6d`/OpenAPI `0.5.0`；Web main `04fd9418df55bc5db3df9829b9e9cbba40d3a334` clean，仍固定旧 BFF。只更新 Web 已有 `src/generated/iam-relay-policy.json`（从 BFF 发布 commit 原始字节复制）、`src/lib/server/iam-relay-policy.ts` provenance/version/digest、`tests/contract/iam-relay-policy.test.ts` 与 CURRENT/三设计文档的当前来源；不改登录/邀请 route、组件、Auth.js/CSRF、Cookie、SQL/Redis、业务 API 或用户 3310。先来源断言 RED，再新 blob 与 runtime gate GREEN；Node22 `pnpm check`，必要时 `pnpm test:e2e` 仅在隔离 dev 端口，禁止接管 3310；Root 最终检查 snapshot 与 BFF blob 字节相同、policy 除来源字段外语义完全一致，再审查提交。IAM/BFF/Web 三 SHA 与 Root inventory/gitlink 都固定后才称消费来源闭环。

W1E-IAM-E2-WEB-POLICY-PIN 状态（2026-09-26）：Web `9fc2fefd2b7dad5fae040b670e7f420b3343defc` 已发布，Node22 `pnpm check` PASS（contract 69、architecture 36、test 1475、lint/typecheck/build）；BFF policy 原始字节一致，JSON 只改三处 IAM 来源字段。Root 三仓 gitlink/库存已进入集成验收；Platform ingress/consumer、Agent signer 与六 owner 真组合仍未完成，`EDGE-WEB-BFF` 仍 broken。

W1E-IAM-PLATFORM-INGRESS-DOC / P0（2026-09-26，IAM 唯一文档 writer、Root 审查）：基线 Root `706ac8ed`、IAM `b720b6dc095b883237682102ca0a87ed6451a968`、Platform ADR-002 `9c88d0d`；前置 E2 owner/API/SDK 已发布，四 Platform ingress caller 尚未发布。仅修改 IAM 既有 `docs/TECHNICAL_DESIGN.md`、`docs/API_CONTRACT.md`、`docs/DATA_MODEL.md`、`docs/CURRENT.md`，若确需新增 ADR 先向 Root 报告；不改源码、机器 contract、Schema、lockfile 或用户 3310。Owner 为 IAM auth/OAuth；唯一 writer 仍是 IAM，Platform 只消费。目标具名 `POST /internal/v1/platform-workload-tokens/introspect`（独立于 Platform→IAM E2 verifier）：专用 Platform `resource_server` Basic credential；严格 `{access_token,surface}`，四 surface/profile/purpose/scope/tenant/audience 矩阵固定，response 只返回可信 caller/client ID/tenant/profile/scope，不返回任意 token claims；native introspection 只作为 active 证据，JWT 精确 platform audience 与现有 Prisma client/resource/binding/Tenant 当前态另验。Tenant 只从已验证 bearer 投影，不从 body/header 自报。错误 401/403/429/503 与 no-store/request-id、deadline、Basic/token 保密明确；无新表/角色/跨 owner SQL。技术方案比较复用 IAM `auth/internal-access` 与独立 `auth/platform-ingress` 子边界，说明为何 Platform audience 验证不能放宽所有 IAM internal endpoint。文档门必须同时裁决四种持久 purpose、专用 client 注册/readback/轮换、method-specific Basic SDK、机器 OpenAPI `0.6.0` 目标、失败恢复与生成链；当前态/目标态分明。验证文档互相一致、Platform ADR-002 §9–13 字段一致、`git diff --check`；Root 放行后才拆 IAM provisioning 与 endpoint TDD 代码片，后续 Platform 原子 cutover 串行。只读审查者不得改文件；文档 writer 与 Web writer 不共享仓。

W1E-PLATFORM-CUTOVER-IAM / P0（依赖 IAM 文档门）：IAM 唯一代码 writer 按已批准三设计文档完成 Platform ingress resource、四 purpose 独占 scope/client、专用 resource_server Basic、具名 current-state introspection 与 OpenAPI/SDK；不借用 Platform→IAM E2 verifier client，不放宽旧 IAM internal audience，不增新表/角色。先 TDD 再 Node24 `pnpm verify`、真实隔离 PostgreSQL/Redis/JWKS HTTP 四 caller、坏/失效 token、禁用/跨 tenant/resource/scope/轮换矩阵，提交 owner SHA/digest；Root 独立复验后才派 Platform。

W1E-PLATFORM-CUTOVER-OWNER / P0（依赖 IAM ingress 发布）：物理仓 `apps/kokoro-capability` 单一 writer 在**同一可构建 release** 中替换唯一 `kokoro.platform.v1` Proto/generated、31 exact operation、typed IDs、24 request binding/15 command digest、IAM 固定 SDK ingress+E2、caller token provider 与当前 installed+enabled+active/receipt replay gate；同步删旧 Capability Proto/shared token/attestation/selector/手写 binding，不运行双轨。Prisma owner schema 保持唯一，不额外复制 IAM/Storage 事实。`pnpm platform-artifact:check`、contract/Buf intentional-break、新 descriptor baseline、schema/Prisma、format/lint/typecheck/test/build/smoke 均过且独占测试资源归零；发布代码后**不立即激活**。

W1E-PLATFORM-CUTOVER-CONSUMERS / P0（依赖 Platform owner 发布）：Agent→BFF→System→Storage→Web→Root 按各自 owner contract/调用方向串行固定同一个 Platform commit/Proto/SDK，Agent fresh proof/source_ref、BFF projection/catalog OAuth、System global operator、Storage allowlist、Web 仅 BFF public consumer、Root gitlink/部署/库存；每仓单 writer/本仓完整门，生产旧 Capability active refs 与 shared-token 路径归零，不借 consumer 局部门冒称激活。

W1E-PLATFORM-CUTOVER-ACTIVATE / P0（依赖六 owner 固定版本）：Root 在隔离同库多 schema/共享 Redis/真实 IAM JWKS/Storage 的六 owner sandbox 证明 execution allow、撤权后 completed receipt replay 拒绝、projection/catalog→Storage、global 认证后固定 unsupported、caller ID 换证 barrier 与 deny 零下游副作用；进程/数据库/Redis 自有资源归零后再协调 stop/switch/start。Root topology、契约库存、全仓适用门与真实浏览器/Agent 能力验证均要绑提交，Billing 仍最后。

W1E-IAM-PLATFORM-INGRESS-DOC 状态（2026-09-26）：IAM 四份设计文档由 Root 对照 Platform ADR-002、当前 IAM E2/机器 token 代码、TS/SQL/API 手册审查；`git diff --check`、四文档 Prettier 均通过，已独立提交 IAM main `592bc057a488087d9853357d359b64a09b0b73a9`。设计门通过，不代表 OpenAPI/SDK `0.6.0` 或 runtime 已发布。IAM 唯一 writer 已接续 `W1E-PLATFORM-CUTOVER-IAM` TDD；Root 暂不提升 IAM gitlink，待机器 contract 和运行代码成套发布，再按 IAM→BFF→Web 来源链重钉并独立验收。

W1F-LOGIN-SHADCN / P0（2026-09-26，已交付）：Owner `kokoro-app`，唯一 Web writer 完成真实 shadcn Page、Proxy 内部 HMAC rewrite、原路径 POST 与 401/429/503 同表单反馈；Root 独立审查并提交/推送 Web main `1fa25d2b4760dc428d3fcdf77628ba343a5bc3ff`。Node22 `pnpm check` contract 69、architecture 36、Vitest 1478、lint/typecheck/build exit0；3310 独立拷贝以明确文件热同步，未重启/重复启动服务，Root 全新 Chromium 的正式 `/login` 与错误态均通过。Root 本片只 pin Web gitlink/库存；IAM 0.6→BFF→Web provenance 与 Platform cutover 仍按上方依赖任务串行。

W1E-IAM-0.6-WEB-POLICY-PIN / P0（2026-09-26，唯一 Web writer）：前置 IAM main `a4c2b61467f1fc1772d6b6d8e98f081c090289fb`、BFF main `1105553cfc24d4f44a90f626132bc30323a77946` 已发布；BFF browser-private policy `2.1.0` 原始 SHA-256 `8f7d4f4cb6fa0ec34d2cce8702d8882d3270a316a6cbdb2d8bdaccefb9c6b4a1`。Web 基线 main `1fa25d2b4760dc428d3fcdf77628ba343a5bc3ff`、clean。只把 BFF 已发布 policy 原始字节复制到 Web 只读快照，更新运行 provenance/版本/digest、相邻 contract 测试与 CURRENT/三设计文档当前来源；不得修改刚验收的 shadcn 登录 Page、Proxy、OIDC/CSRF、业务 API、SQL/Redis 或用户 3310。先来源断言 RED→GREEN，证明 policy 除三个 IAM 来源字段外语义相同、无新 browser relay operation；Node22 `pnpm check`、Root 独立审查与复跑后明确提交/推送 Web main，最后 Root 一次性 pin IAM/BFF/Web gitlink 与库存并跑来源/拓扑门。Platform owner consumer 仍未激活，Billing 最后。

W1E-IAM-0.6-ROOT-PIN / P0（2026-09-26，Root 集成）：IAM `a4c2b614`、BFF `1105553c`、Web `40a20959` 均已在各自 main 发布；Root 在同一提交更新三 gitlink 与 `verification/contracts/consumer-inventory.json` 的 owner/consumer commit、真实 blob SHA 和旧 vendor 路径，保留 16 edge 中 11 declared broken、0 illegal 的诚实状态。静态 `verify-iam-relay-policy.py`、`verify-repository-topology.py`、W1D checkpoint、Root scripts/tests 与 main-only 在提交后重跑；若未通过则保留失败证据，不把来源固定当 Platform runtime 激活。下个代码 owner 为 Platform，单仓先更新过时三设计文档当前事实再按 ADR-002 做单一 `kokoro.platform.v1` atomic release，Billing 最后。

W1E-WEB-OIDC-FLAKE / P1（待独立稳定化）：Web `40a2095` 的 Root 首次并行全量 `pnpm check` 中，`tests/system/oidc-rp-next-http.integration.test.ts` 一个 pending-refresh 用例偶发 callback 200 而非 303；该文件隔离 38/38、第二次全量 1478/1478 通过。由后续唯一 Web owner 在不修改登录签名/CSRF业务语义的前提下复现资源/时间竞争、补确定性同步或隔离；不以重跑绿隐藏 flake。此项不阻塞 IAM provenance 纯来源 pin，但不能作为全 Web 稳定性关闭证据。
