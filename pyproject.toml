[project]
name = "kokoro-root-governance"
version = "0.0.0"
requires-python = ">=3.11,<3.14"
dependencies = []

[dependency-groups]
dev = [
  "pytest==8.4.2",
]

[tool.pytest.ini_options]
testpaths = ["scripts/tests"]
